# Build output (release zips are attached to GitHub Releases; Steam gets the packaged copy)
Source/**/bin/
Source/**/obj/
Source/**/*.user
tools/**/bin/
tools/**/obj/
1.6/Assemblies/
dist/
TestResults/

# Local environment (machine-specific paths and secrets — see .env.example)
.env
.env.*
!.env.example

# JS tooling
node_modules/

# Compatibility-investigation scratch (cloned reference mods, modlist, decompiles)
.hd-investigation/

# AI / agent folders
.claude/
.cursor/
.codex/
.aider*

# IDE / OS
.vs/
.idea/
*.suo
.DS_Store
Thumbs.db

# Guidance for Claude agents — local only, not shipped
CLAUDE.md
