# Hauler's Dream — Mod Compatibility Feature-Risk Map

Developer reference. A per-feature compatibility risk map for the mods Hauler's Dream (HD) is built to coexist with, produced by a multi-agent investigation: one investigator per feature family read the mod's **source** (cloned, or decompiled for closed-source) and its **bug-fix history**, traced every feature against HD's patch surface, then an adversarial skeptic verified each family's load-bearing claims, then a synthesis pass produced the plan at the end. For each mod feature: the vanilla mechanism it uses, which HD subsystem overlaps, the risk level, and the recommended fix — preferring a **general pattern** (covers any mod with that feature) over a mod-specific patch. See `COMPATIBILITY.md` for the user-facing summary.

**Risk legend:** `HIGH` = can break a feature / loop / strand items · `MEDIUM` = wasted work or a transient over-estimate · `LOW` = cosmetic or rare edge · `NONE` = no overlap.

## Families

- **storage** — HD is structurally well-defended against this storage family because every hot path validates candidate cells exclusively through vanilla seams (IsGoodStoreCell→NoStorageBlockersIn/GetMaxItemsAllowedI…
- **inventory-keepers** — HD already has a mature, well-architected keep-item system that severs the unload<->refetch loop for Simple Sidearms, Combat Extended, Smart Medicine, Dub's Bad Hygiene and Yayo's Combat 3. The single…
- **haul-redirectors** — The haul-redirector family (Common Sense, Keyz' Allow Utilities / Allow Tool "Haul Urgently", While You Are Nearby, While You're Up / Jobs of Opportunity) overlaps HD's two most sensitive seams: the W…
- **crafting-bills** — HD's batch-craft / bill-routing subsystems overlap heavily with this family but are, with one real exception, already well-guarded. The single high-risk item is GENERAL, not mod-specific: HD's full-re…
- **pathfinding** — Perfect Pathfinding (PP; real assembly/namespace "PerfectPathing", Mod class PerfectPathing.Mod, About name "Perfect Pathfinding (1.5 Update)", packageId jp.perfectpathing.updated) is a tiny mod: a [S…
- **vehicles** — HD's VF compat surface is reflection-only (8 AccessTools.TypeByName sites; no `using Vehicles`, no hard refs, no Vehicles.* type in any signature) and degrade-SAFE (VehicleFrameworkCompat.IsActive is …
- **abilities** — Better Autocasting for VPE (investigated separately; see its section below).
- **advanced_power_plus** — A pure content mod (power buildings + 3 research projects); its only HD contact point is the refuelable path, and that single bug (an NRE refueling its IMPASSABLE generators) is closed generally by HD's `pawn.Position` fuel-search anchor. Everything else is `NONE`.
- **construction** — Smarter Construction (investigated for issue #235). Four of its five patches are `NONE`; the fifth CAN throw an NRE from `WorkGiver_ConstructFinishFrames.JobOnThing`, which vanilla's work scan does not guard at the point it is called — so the pawn loses its ENTIRE work node every scan while still eating and sleeping. → GOTCHA: "can throw" is a decompiled possibility, NOT a finding that it DID throw in #235; that attribution was retracted (see `COMPATIBILITY.md` and the correction in `CHANGELOG.md`). HD's fix is general containment + a session quarantine of the faulting work giver + a visible alert, not SC-specific code.
- **drugs** — MODDED DRUG CONTENT vs RimWorld's own `DrugPolicy` (investigated for issue #232; no single mod is implicated and the reported mod is not installed here). The family risk is not a patch collision at all: vanilla's per-`ThingDef` policy indexer ends in a message-less `throw new ArgumentException();` for a drug the policy holds no entry for, and it is reached from `ShouldKeepDrugInInventory`, whose two callers (the per-think drop loop and the "Pick up" float menu) neither guard it. HD is NON-CAUSAL on the reported path — its postfix never ran — but it held the only guard position, and it had one instance of the same unguarded lookup in its own #229 withdrawal scan. HD's fix is containment at the seam + a single shared accessor that cannot reach the throwing indexer + a build guard, and explicitly NO repair of another mod's or RimWorld's saved data.

---

## Family: `storage`

HD is structurally well-defended against this storage family because every hot path validates candidate cells exclusively through vanilla seams (IsGoodStoreCell→NoStorageBlockersIn/GetMaxItemsAllowedInCell, CanStackWith, TryFindBestBetterStorageFor, Accepts) that all three mods patch — so modded acceptance/capacity/routing apply automatically, and the deposit-time re-gate (FindTargetOrDrop + PlaceHauledThingInCell IsGoodStoreCell re-validation) is the self-correcting authority that bounds any over-estimate to one-cycle churn rather than loss. Adaptive Storage Framework and LWM Deep Storage keep stored items SPAWNED on-map (in thingGrid / ListerThings.ThingsOfDef), so HD's sweep/route/stack/count features see and handle them correctly; Storage Network keeps items DESPAWNED inside server/terminal ThingOwners, so HD's ThingsOfDef-based features simply don't fire over network-only inventory (graceful degrade, handled by HD's opt-in SN bulk-load shim + SN's own ItemAvailability postfix and StartJob auto-spawn). The two genuinely live risks are: (1) HaulToStack's no-cell-reservation prefix racing multiple pawns onto the same LWM/ASF multi-stack cell, where LWM's added storing-time DELAY on PlaceHauledThingInCell widens the race window vs vanilla's instant place (the LWM author himself flags 'Haul To Stack? conflicting reports' and warns its toil chain 'could break' when moving many things at once) — still bounded by deposit re-gate, but more wasted trips; and (2) HD's group-space measure — since the #248 storage-commitment ledger this is StorageCommitments' evidence read (StorageCommitments.MeasureGroupUncached; formerly BulkHaul.StorageSpaceForDef) — calling the STANDALONE GridsUtility.GetItemStackSpaceLeftFor (which LWM does NOT patch — only the HaulToCellStorageJob-internal call is transpiled), so for LWM mass/weight-limited DSUs the per-cell space is over-estimated (count-only, mass-blind) — HD documents this as a safe upper bound that self-corrects at deposit. Both reduce to extra churn, never cargo loss or storage-mod-rule bypass. A third entry was added later (issue #236) and is rated MEDIUM by HD's CONTRIBUTION rather than by its symptom: ASF's own `IntFishSet` stored-item index can corrupt and throw out of vanilla `ThingGrid.RegisterInCell`, which is severe, but HD neither owns nor mutates that structure — the same no-cell-reservation prefix as risk (1) only makes the ASF-side bug easier to reach. It is upstream's to fix; HD's share of #236 was making its exception breadcrumb stop claiming innocence. No mod-specific patch is required; the general pattern (validate through vanilla APIs; treat non-spawned container contents as out-of-reach; let the deposit re-gate be authority) already covers all three and any future Building_Storage-derived mod. → NOTE (2026-08-09, issue #248): the cell-reservation and space-scan machinery this family references was rebuilt after it was written — `StorageEnroute` is deleted, and one claim ledger (`StorageCommitments`, claims keyed pawn+destination+def, every read `min(recorded, evidence)`, nothing scribed; contract in `docs/plans/phase1-storage-commitment-seam-design.md`) now answers "how much may go here" through two vanilla adapters (a `HaulAIUtility.HaulToCellStorageJob` postfix clamps counts, a `StoreUtility.IsGoodStoreCell` postfix gates cells). `Patch_JobDriver_HaulToCell_NoCellReservation` still exists but strips vanilla's destination reservation ONLY when a claim lands (no arbitration → vanilla reserves both targets as always). Risk (1)'s "several pawns price the same free space" half is therefore closed at plan time — concurrent haulers now see each other's claims — while the same-CELL convergence churn within a group's real capacity remains and is still bounded by the deposit re-gate. Risk (2) carries over unchanged: the evidence read still prices through the standalone count-only extension.

### Feature risk map

- `LOW` **Adaptive Storage Framework — Per-cell multi-stack capacity limit (configurable slots per cell, even per-cell varying)**
  - Mechanism: Transpiler on GridsUtility.GetMaxItemsAllowedInCell (StorageLimit.cs:15-72) replaces Building.MaxItemsInCell with ThingClass.GetMaxItemsForCell(in cell); ThingClass.cs:133 GetMaxItemsForCell + TotalSlots/_maxItemsByCell. Runs inside vanilla IsGoodStoreCell→NoStorageBlockersIn and inside GetItemStackSpaceLeftFor.
  - HD overlap: HaulToStack (FindStackCell, IsGoodStoreCell per cell), the StorageCommitments group-space evidence, ex-BulkHaul.StorageSpaceForDef (GetItemStackSpaceLeftFor + IsGoodStoreCell loop), all TryFindBestBetterStorageFor users
  - Fix (general / no-op): None needed. HD validates every candidate via IsGoodStoreCell (runs ASF's GetMaxItemsAllowedInCell) and prices space via the same GetItemStackSpaceLeftFor that calls it — ASF's per-cell limit is honored by construction. General pattern already in place.
- `LOW` **Adaptive Storage Framework — Storage acceptance / capacity gate (HasCapacityForThing, ContainsAndAllows, overridden Accepts)**
  - Mechanism: Prefix on StoreUtility.TryFindBestBetterStoreCellForWorker (StoreUtilityPatches.cs:30 → slotGroup.Settings.owner is ThingClass adaptive ? adaptive.HasCapacityForThing(t)); Transpiler on NoStorageBlockersIn (FixMissingValidStackDestinationCheck → IsValidStackDestination → ContainsAndAllows); ThingClass.cs:372 Accepts = HasCapacityForThing && base.Accepts.
  - HD overlap: BulkHaul (TryFindBestBetterStorageFor candidate gate), HaulToStack, StorageRouting (slotGroup.parent.Accepts), unload FindTargetOrDrop
  - Fix (general / no-op): None needed. HD routes all acceptance through TryFindBestBetterStorageFor / IsGoodStoreCell / parent.Accepts, all of which ASF patches; HD never bypasses them. General pattern.
- `NONE` **Adaptive Storage Framework — Stored items stay SPAWNED on-map (registered in ThingGrid normally; only hidden from rendering/overlay)**
  - Mechanism: RegisteredAtThingGridEvent.cs:13-35 transpiles ThingGrid.RegisterInCell to merely Notify_ItemRegisteredAtCell (items still added to grid); the mirror DeregisteredAtThingGridEvent.cs:13-50 transpiles ThingGrid.DeregisterInCell → Notify_ItemDeregisteredAtCell (+ re-queues the thing for haulable/mergeable rechecking when another mod moved it while spawned); NotifyItemStackChanged.cs:10-16 postfixes ListerMergeables.Notify_ThingStackChanged → Notify_ItemStackChanged. All three feed ThingCollection._validStoredThings (ThingCollection.cs:31, an IntFishSet). HideStoredThingsFromSectionLayerAndOverlayDrawer.cs only suppresses draw/forbidden-overlay. Items remain in listerThings.ThingsOfDef / listerHaulables.
  - HD overlap: TransportLoad.AddStoredClaimables (ThingsOfDef), CraftBatchPlanner.AvailableUnits, InventoryConstructDelivery.Gather, ConstructionBatch, RouteSelection (all ThingsOfDef-based)
  - Fix (general / no-op): No issue — ASF-stored items are visible to every HD ThingsOfDef/listerHaulables scan exactly like floor stacks, so AddStoredClaimables/ingredient counting see them correctly. Contrast with Storage Network (despawned).
- `MEDIUM` **Adaptive Storage Framework — `IntFishSet` stored-item index corruption (HD issue #236, ASF-side bug)**
  - Mechanism: ThingCollection._validStoredThings (ThingCollection.cs:31) is an IntFishSet — Fishery's open-addressing set whose chain links live in a SEPARATE `_tails` nibble array, not in the bucket array. Add/Remove/Contains run from ASF's three thing-grid notifiers (TryAddStoredCellWiseThing ThingCollection.cs:289, RemoveCellWise :544, ContainsAndAllows :449). OBSERVED: an insert's parent walk fails — FishSet.cs:480-501 GetParentBucketIndex → FishSet.ThrowHelper.cs:22-55 `InvalidOperationException: Failed to find parent index in IntFishSet` — from inside vanilla ThingGrid.RegisterInCell (ASF's transpiler), i.e. mid-`GenPlace.TryPlaceThing`, aborting the placement. INFERRED, not reproduced: the two structures desynchronise so that a chain the walk follows no longer leads back to the entry. The obvious candidate shape — a slot whose `_tails` nibble says EMPTY while the bucket still holds a live key — is probably NOT it, because Fishery has a dedicated throw for exactly that (FishSet.cs:283-291 TryReplaceValueOfMatchingKey: the bucket matches the entry, then `IsBucketEmpty` — which is `_tails.IsEmpty(i)`, Internal/Tails.cs:31 — fires `ThrowEmptyBucketIsTailInvalidOperationException`, "Bucket marked as empty was attached as tail to a key. This should never happen.") and #236 reported the parent-walk message instead. The precise corruption is unestablished; only the throw site and the reaching path are verified.
  - HD overlap: NON-CAUSAL. HD never touches _validStoredThings, the notifiers, or ThingGrid; it reaches ASF's transpiled register only by placing items through vanilla GenPlace, exactly as vanilla hauling does. `Patch_JobDriver_HaulToCell_NoCellReservation` (HaulToStack) is an EXPOSURE amplifier only: dropping vanilla's destination-cell reservation lets several pawns target one ASF cell, so register/deregister/stack-change notifications for that building arrive interleaved far more often. Sequence, not concurrency — all of it is main-thread.
  - Fix: none / upstream. Not HD's collection to patch, and degrading a working feature to hide another mod's bug is out of bounds. Player-side mitigation: turn "Haul to stack" off (restores vanilla's cell reservation → one pawn per cell → narrower window; a mitigation, not a fix). HD's side of #236 was making the exception breadcrumb honest (BlamePolicy / HdInvolvement.AncestorWrapper) so the crash report names ASF instead of asserting "This is NOT a Hauler's Dream bug" from a stack that structurally could not contain the evidence.
- `NONE` **Adaptive Storage Framework — ContentsITab / GroupContentsITab, GraphicsDef content-based graphics, selection/overlay tweaks, temperature, stat draw entries**
  - Mechanism: AddITabToLegacyStorages/StockpileZones, InitializeGraphicsDefs, AdjustThingTemperature, AdjustSelectionBracketPosition, PreventSelectionInRect/UnderMouse, ThingDefStatDrawEntries — UI/graphics/temperature only, no hauling/storage-search logic.
  - HD overlap: none
  - Fix (general / no-op): Purely cosmetic/UI/temperature; no overlap with any HD subsystem.
- `LOW` **LWM Deep Storage — Multi-stack-per-cell deep storage (MaxNumberStacks) — the core feature**
  - Mechanism: Prefix on Building.get_MaxItemsInCell (Patch_Building_MaxItemsInCell.cs → CompDeepStorage.MaxNumberStacks); Prefix on StoreUtility.NoStorageBlockersIn (Patch_NoStorageBlockersIn.cs → cds.StackableAt → MapComponentDS.CanStoreItemAt). Both feed vanilla IsGoodStoreCell and GetItemStackSpaceLeftFor (whose internal call inside HaulToCellStorageJob is transpiled to cds.CapacityToStoreThingAt).
  - HD overlap: HaulToStack (per-cell consolidation), the StorageCommitments group-space evidence (ex-BulkHaul.StorageSpaceForDef), TryFindBestBetterStorageFor users
  - Fix (general / no-op): None needed for acceptance/per-cell-count: IsGoodStoreCell→NoStorageBlockersIn (LWM prefix) gates every HD candidate, so a 'full' DSU cell is skipped. General pattern.
- `MEDIUM` **LWM Deep Storage — Mass/weight-limited DSUs (maxTotalMass per cell, maxMassOfStoredItem per item)**
  - Mechanism: CompDeepStorage.limitingTotalFactorForCell / limitingFactorForItem (CompDeepStorage.cs:227-314 CapacityToStoreThingAt). This count is exposed via the TRANSPILED HaulToCellStorageJob path (cds.CapacityToStoreThingAt) but NOT via the standalone GridsUtility.GetItemStackSpaceLeftFor (confirmed: LWM patches only the internal call, not the standalone method).
  - HD overlap: the StorageCommitments group-space evidence, ex-BulkHaul.StorageSpaceForDef (calls c.GetItemStackSpaceLeftFor(map, def) directly in StorageCommitments.MeasureGroupUncached — count-based, LWM-mass-blind)
  - Fix (general / no-op): Already handled by design: HD's group-space measure is a deliberate SAFE UPPER BOUND (since #248 held by StorageCommitments' evidence read; formerly documented at BulkHaul.StorageSpaceForDef) and the deposit re-gate (FindTargetOrDrop re-runs TryFindBestBetterStorageFor per carried stack; PlaceHauledThingInCell re-validates IsGoodStoreCell which runs LWM NoStorageBlockersIn→StackableAt incl. mass) deposits only what the mass-limited cell accepts and floor-drops/re-routes the rest — bounded one-cycle churn, never overfill or loss. No patch required. (Optional general improvement: if HD ever wants tighter pre-estimates for mass-limited storage, clamp the group-space measure using a vanilla SpaceRemainingFor probe — also LWM-patched — but this only trades churn for a more expensive scan and is not required.)
- `MEDIUM` **LWM Deep Storage — Storing-time delay (pawns take time to carefully store into a DSU)**
  - Mechanism: Postfix on Toils_Haul.PlaceHauledThingInCell (Deep_Storage_Pause.cs:53) wraps initAction with a TimeStoringTakes delay + sets defaultCompleteMode=Delay + WithProgressBar, only when the destination cell has a CompDeepStorage. The author comments (lines 114-117) that with many things at once 'this could break the entire chain'.
  - HD overlap: JobDriver_UnloadHauledInventory (uses Toils_Haul.PlaceHauledThingInCell, line 98) and HaulToStack's no-cell-reservation prefix (Patch_JobDriver_HaulToCell_NoCellReservation)
  - Fix (general / no-op): General pattern (no LWM-specific code). HD's unload uses the canonical PlaceHauledThingInCell, so LWM's delay wraps HD's deposits correctly (pawns wait as intended). The live concern is HaulToStack's no-cell-reservation letting 2+ pawns target the SAME DSU cell while LWM's storing delay widens the race window vs vanilla's instant place — increasing wasted trips. It cannot overfill or lose cargo (CarryHauledThingToCell fail-condition + PlaceHauledThingInCell re-run IsGoodStoreCell→NoStorageBlockersIn on arrival; the loser floor-drops, bounded churn). Recommend: confirm in-game with a multi-stack mass/count-limited DSU + multiple haulers (the LWM author's 'conflicting reports'). If churn is bad, the general fix is to make HaulToStack RESERVE the destination cell when the chosen slot group has a per-cell capacity of exactly 1 stack OR when the cell's building reports a storing delay — but the simplest robust general option is to keep cell reservation whenever GetMaxItemsAllowedInCell(cell)==1 for ALL storage (already the unstackable case), and only skip reservation for genuine multi-stack/multi-capacity cells where concurrent stacking is the intended win. → NOTE (2026-08-09, #248): partially superseded — the reservation strip is now conditional on a StorageCommitments claim landing (no claim → vanilla reserves), and the hand-written unstackable carve-out this referenced was absorbed by the ledger (one corpse claims one unit of one cell, so a second hauler finds no room instead of looping). The per-storing-delay reservation idea itself remains unimplemented; the deposit re-gate still bounds the same-cell race.
- `NONE` **LWM Deep Storage — Animals/low-intelligence pawns cannot haul to deep storage (and robot-mod whitelist)**
  - Mechanism: Postfix on StoreUtility.IsGoodStoreCell (Deep_Storage_CanCarryItemsTo.cs:152-269 Patch_IsGoodStoreCell) returns false for carriers below ToolUser intelligence when the cell can store >1 thing; a DynamicMethod whitelists Misc/Base Robots, PRF drones.
  - HD overlap: HaulToStack (IsGoodStoreCell per carrier), the StorageCommitments group-space evidence (ex-BulkHaul.StorageSpaceForDef; IsGoodStoreCell per carrier), StorageRouting, unload
  - Fix (general / no-op): Honored automatically — HD passes the carrier into IsGoodStoreCell, so LWM's per-carrier intelligence gate applies. HD's own EligibilityPolicy already restricts sweeps to humanlikes + allowed mechs anyway. General pattern.
- `LOW` **LWM Deep Storage — DSU over-capacity eviction (re-haul items out of an over-full DSU) + ShouldRemoveFrom logic**
  - Mechanism: Transpiler on StoreUtility.TryFindBestBetterStoreCellFor (Patch_TryFindBestBetterStoreCellFor_ShouldRemoveFrom.cs — sets foundPriority=Unstored when over capacity so any valid storage beats it). GATED OFF for PRF/PRF-lite/Extended Storage. Also ListerHaulablesTick transpiler (removes the break so all items in a DSU cell get haul-checked) — both marked todo1.4/disabled in current source.
  - HD overlap: BulkHaul (TryFindBestBetterStorageFor candidate gate), HaulToStack postfix runs on the same TryFindBestBetterStoreCellFor
  - Fix (general / no-op): None needed. When LWM marks an over-capacity item as needing relocation, it appears as a normal haulable that HD's sweep/route can pick up and HD re-validates the destination via the same vanilla APIs. HD's HaulToStack postfix on TryFindBestBetterStoreCellFor composes after LWM's transpiler (postfix only refines the cell within the room/priority LWM/vanilla already chose, re-gated by IsGoodStoreCell) — no conflict.
- `LOW` **LWM Deep Storage — MapComponentDS storage cache (DirtyCache on set_Position / SplitOff / TryPlaceDirect / Notify_Received/Lost)**
  - Mechanism: Patch_Thing_set_Position, Patch_Thing_SplitOff, Patch_GenPlace_TryPlaceDirect, Patch_Building_Storage_NotifyXedThing all DirtyCache so LWM's per-cell capacity cache stays current as items move.
  - HD overlap: Indirect — HD's HaulToStack/BulkHaul cause many SplitOff/place operations; if LWM's cache went stale the capacity gate could be wrong.
  - Fix (general / no-op): None needed — LWM hooks the standard mutation points (set_Position, SplitOff, TryPlaceDirect, Notify_Received/LostThing) that HD's hauls go through, so the cache stays current. NOTE prior-bug context: LWM historically had 'pawns haul zero items to storage' and 'cache doesn't clear if last item isn't full stack but is filled' cache bugs (see priorBugs). HD does not reintroduce these (HD keeps no DSU capacity cache of its own; it always reads live via IsGoodStoreCell/GetItemStackSpaceLeftFor).
- `LOW` **Storage Network — Virtual/digital network storage — items stored DESPAWNED inside server/terminal ThingOwners**
  - Mechanism: Building_StorageServer/Building_StorageTerminal absorb incoming items via innerContainer.TryAddOrTransfer after DeSpawn (Building_StorageServer.cs:935-937, Building_StorageTerminal.cs:722,857). Despawned contents are absent from listerThings.ThingsOfDef and listerHaulables. SN_ServerThingOwner caps by server MaxStacks. Patch_ListerHaulables_SkipOurTerminal stops the terminal being treated as a haul source.
  - HD overlap: TransportLoad.AddStoredClaimables/BuildPool (ThingsOfDef/listerHaulables, both spawned-only), HaulToStack, CraftBatchPlanner/ConstructionBatch/InventoryConstructDelivery ingredient counting (all ThingsOfDef)
  - Fix (general / no-op): General pattern (treat non-spawned container contents as out-of-reach) — HD's spawned-only scans simply never pick up network items, which is correct: SN materializes them at job start via its own StartJob auto-spawn. HD's floor-haul/sweep/stack features gracefully no-op over network-only inventory. The transporter/vehicle/portal bulk-load gap (manifest goods living in the network) is covered by HD's OPT-IN StorageNetworkCompat shim (default off). No further patch needed.
- `MEDIUM` **Storage Network — On-demand materialization of despawned stacks at a terminal (StartJob auto-spawn) with per-pawn-per-tick throttle + Force-Wait anti-loop**
  - Mechanism: Prefix on Pawn_JobTracker.StartJob (Patch_Pawn_JobTracker_StartJob_AutoSpawn.cs) replaces despawned targetA/B/C and targetQueueA/B entries with freshly-spawned stacks at the nearest usable terminal (TrySpawnFromStorageExact → SpawnFromTerminalForPawn). Throttled to one auto-spawn batch per pawn per tick; after 3 throttle hits/tick it force-ends the current job and inserts Wait(60) to break vanilla loops.
  - HD overlap: TransportLoad bulk-load drivers (HD stages claimable stacks in job.targetQueueB; SN's StartJob rewrites despawned queue entries — exactly what HD's SN shim relies on)
  - Fix (general / no-op): General/shim already correct: HD's AppendNetworkClaimables stages SN's despawned stacks in targetQueueB and SN materializes them at job start; HD's sweep toil skips any entry that fails to materialize (requires Spawned), so a non-materialized stack is a safe skip not a stall. RESIDUAL CONCERN to verify in-game: if HD stages MANY despawned network stacks, SN's per-pawn-per-tick auto-spawn throttle + 3-hit Force-Wait could interrupt HD's bulk-load job with a Wait(60), causing a hitch or a re-plan. This is bounded (recoverable) and the shim is opt-in/default-off. Recommend a multi-stack network bulk-load test under the opt-in. No code change unless the throttle visibly stalls loading.
- `LOW` **Storage Network — Network path-route re-targeting (haul to the cheapest-path terminal/server in the network)**
  - Mechanism: Postfix on StoreUtility.TryFindBestBetterStorageFor (Patch_TryFindBestBetterStorageFor_NetworkPathRoute.cs) — after vanilla picks a terminal/server, swaps to a network sibling with lower real path cost.
  - HD overlap: HD's Patch_StorageFilter_Funnel postfixes the SAME method; BulkHaul/unload call TryFindBestBetterStorageFor
  - Fix (general / no-op): None needed. Both are postfixes on TryFindBestBetterStorageFor; HD's funnel only VETOES (sets __result=false) in non-default Opportunistic/BeforeCarry contexts and is byte-inert otherwise, so it can at worst decline an SN-chosen network destination (leaving the item for normal hauling) — never produce an invalid cell or fight SN's routing on the normal path. Harmony postfix composition is order-independent here because HD only narrows. General pattern.
- `LOW` **Storage Network — Network counts toward construction/recipe/availability (ItemAvailability, ResourceCounter, RecipeCounter, ColonyThingsWillingToBuy, FindBestMedicine, food/joy/drug/dye search)**
  - Mechanism: Postfix on ItemAvailability.ThingsAvailableAnywhere (Patch_ItemAvailability_TerminalCount.cs) adds terminal GetTotalByDef toward construction availability; many other postfixes (RecipeCounter_CountValidThings, ResourceCounter_AddTerminals, RegionProcessor_TerminalItems, TryFindBestFoodSourceFor, etc.) surface network contents to vanilla AI.
  - HD overlap: InventoryConstructDelivery (ItemAvailability gate for offering construct-deliver jobs), CraftBatchPlanner/ConstructionBatch (ThingsOfDef counting), EatFromInventory
  - Fix (general / no-op): General pattern. HD's batch/construct counting uses ThingsOfDef filtered to Spawned+reservable+haulable floor stacks, so it counts ASF/LWM (spawned) correctly and naturally excludes SN's despawned network contents — these HD features then simply don't fire/plan over network-only inventory (vanilla + SN's own ItemAvailability/StartJob handle the actual fetch). This is a correct degrade (HD never over-promises a batch it can't physically reach), not a bug. No patch needed.
- `LOW` **Storage Network — Terminal/server reservation manager + reservation guards (DeliverResources_ReservationGuard, ReservationManager terminal patches, StartJob doomed-haul silence)**
  - Mechanism: Postfix on WorkGiver_ConstructDeliverResources*.JobOnThing nulls a job whose target is already reserved; ReservationManager.CanReserve/Reserve patches treat terminals specially.
  - HD overlap: InventoryConstructDelivery / Patch_ResourceDeliverJobFor_Routing (HD postfixes/redirects the same construct-deliver workgivers), HD's reservation usage
  - Fix (general / no-op): None needed. Both HD and SN postfix the construct-deliver workgivers; HD re-checks CanReserve before emitting its inventory-delivery job and the deposit goes into the site, so an SN reservation veto just makes HD fall back to vanilla. Recommend a sanity check that HD's construct-deliver routing and SN's reservation guard don't both null the same job into a no-op (low likelihood; both fail-open to vanilla).
- `NONE` **Storage Network — Trade beacon / caravan reachable-items / trade-sellable-from-network**
  - Mechanism: Patch_CaravanFormingUtility_AllReachableColonyItems, Patch_TradeUtility_AllLaunchableThingsForTrade, Patch_TradeDeal_InSellablePosition_AllowServerItems, Patch_Pawn_TraderTracker_ColonyThingsWillingToBuy expose network contents to caravan/trade.
  - HD overlap: HD bulk-load while forming caravan is gated off (StorageRouting.MayRoute checks IsFormingCaravan; BulkHaul gates on faction/quest-lodger). VehicleFramework/transporter loading uses HD's own ledger.
  - Fix (general / no-op): No meaningful overlap — HD stands down during caravan forming and does not enumerate network trade items.

### Prior bugs learned from (their bug-fix history)

- n/a to HD — **Weird cache bug making pawns haul ZERO items to storage** _(LWM Deep Storage git commits 5449c23 / 1b6c8e0 ('Fix for weird cache bug making pawns haul zero items to storage'))_ — LWM's MapComponentDS capacity cache returned zero free space when stale, so pawns wouldn't haul. HD keeps NO DSU/per-cell capacity cache of its own — it reads live via IsGoodStoreCell + GetItemStackSpaceLeftFor every probe (HD's only caches are per-tick plan/work memos keyed on game state, re-validated by IsGoodStoreCell on hit). So HD cannot reintroduce a stale-capacity-zero bug. The lesson reinforces: never cache modded storage capacity; always re-read through the vanilla seam — which HD already does.
- n/a to HD — **Cache doesn't clear if the last item in a unit isn't a full stack but is filled (partial-stack capacity miscount)** _(LWM Deep Storage git commit 41f357d ('Fix bug where cache doesn't clear if last item in unit isn't full stack but is filled'))_ — A partial-but-present stack in the last slot left the cache thinking the cell had room. This is exactly the partial-stack reasoning HD's HaulToStack does (CellHasPartialStackOf: other.stackCount < stackLimit && CanStackWith). HD does NOT cache this — it reads thingGrid.ThingsListAtFast live each call and re-gates with IsGoodStoreCell (which runs LWM's StackableAt). So HD's partial-stack top-up reflects LWM's live capacity, not a stale snapshot. Class of bug avoided by construction.
- n/a to HD — **Gizmo patch crashing on 1.5 caused by IL changes; multiple 'try statements to keep things running better'** _(LWM Deep Storage commits d468e83, c4f9c25)_ — LWM's own transpiler/UI fragility across versions — not a hauling-logic bug and not HD-reachable. Mentioned only to note LWM uses fragile transpilers on TryFindBestBetterStoreCellFor / HaulToCellStorageJob / ListerHaulablesTick; HD composes via postfix/prefix on the same methods, so if an LWM transpiler fails to apply on a future RW patch, HD's behavior degrades to vanilla cell selection (still safe), not a crash.
- ⚠️ applies to HD — **PlaceHauledThingInCell storing-delay toil 'could break the entire chain' when a pawn moves a lot of things at once (PUAH-style)** _(LWM Deep Storage Deep_Storage_Pause.cs:114-117 author comment; About 'Haul To Stack? I have conflicting reports'; README 'Plays well with Pick Up and Haul' (qualified))_ — DIRECTLY relevant: HD IS a native PUAH (multi-stack inventory unload) AND a HaulToStack mod. The LWM author explicitly flags both interaction classes as uncertain. HD's unload uses the canonical PlaceHauledThingInCell so the per-stack delay applies as intended, and the deposit re-gate bounds any failure to floor-drop+re-haul. The residual is extra churn/wasted trips when HD's no-cell-reservation races multiple pawns onto the same delayed DSU cell. Verify in-game; bounded, never loss.
- n/a to HD — **Over-capacity check disabled (hard) for Project RimFactory / PRF-lite / Extended Storage to prevent pawns getting stuck in a relocation loop** _(LWM Deep Storage Patch_TryFindBestBetterStoreCellFor_ShouldRemoveFrom.cs:51-63 Prepare())_ — LWM disables its own over-capacity eviction when another storage mod can't expose CompDeepStorage, because the two storage systems fight in a relocation loop. HD never evicts items from storage on its own (it only ADDS hauls/sweeps and re-validates destinations), so it cannot create that two-storage-system relocation loop. Lesson reinforced: don't relocate items OUT of a foreign storage mod's cells — HD's StorageRouting only relocates loose/lower-priority floor stacks toward storage, gated by WorthRelocating + priority eligibility.
- ⚠️ applies to HD — **Storage Network StartJob auto-spawn infinite-loop guard (Force-Wait after 3 throttle hits/tick)** _(Storage Network Patch_Pawn_JobTracker_StartJob_AutoSpawn.cs:50-53,73-89 (ForceWaitThreshold=3, ForceWaitTicks=60))_ — SN itself had to add a per-tick throttle + Force-Wait because repeated same-tick materialization attempts caused vanilla job loops. HD's SN shim stages many despawned stacks in one job's targetQueueB; SN materializes the whole queue in ONE StartJob (one batch = one throttle slot), so a single HD bulk-load job should not trip the 3-hits threshold. But if HD's job re-issues / re-plans repeatedly in a tick (work-scan churn), it could. The opt-in default-off shim limits exposure. Verify under the opt-in with a large network manifest.

### Top risks
- HaulToStack no-cell-reservation + LWM storing-time delay: multiple HD pawns racing onto the same LWM/ASF multi-stack cell, where LWM's added PlaceHauledThingInCell delay widens the convergence window vs vanilla's instant place — bounded by the arrival-time IsGoodStoreCell re-validation (loser floor-drops), but produces extra wasted trips. The LWM author himself reports 'conflicting' HaulToStack compatibility. (medium — churn, never loss) (2026-08-09, #248: the shared-space over-commitment half of this race is closed — the reservation strip now requires a StorageCommitments claim and concurrent haulers price each other's claims; what remains is the same-cell convergence churn.)
- HD's group-space measure (since #248 StorageCommitments' evidence read; formerly BulkHaul.StorageSpaceForDef) over-estimates space for LWM MASS/weight-limited DSUs because it calls the STANDALONE GridsUtility.GetItemStackSpaceLeftFor, which LWM does NOT patch (only the HaulToCellStorageJob-internal call is transpiled to CapacityToStoreThingAt). HD documents this as a deliberate safe upper bound; the deposit re-gate corrects it at deposit time. (medium — extra churn, never overfill/loss)
- Storage Network StartJob auto-spawn throttle (3 hits/tick → Force-Wait(60)) interrupting HD's opt-in network bulk-load if HD re-plans/re-issues the load job many times in one tick. Bounded + opt-in default-off, but a possible hitch/stall to verify in-game with a large network manifest. (medium)

### General-pattern recommendations
- VALIDATE-THROUGH-VANILLA (HD's stated and verified design): every candidate cell is gated by StoreUtility.IsGoodStoreCell (→ NoStorageBlockersIn, patched by all three mods + LWM's per-carrier intelligence postfix), CanStackWith, parent.Accepts, and space is priced via GetItemStackSpaceLeftFor / GetMaxItemsAllowedInCell. This makes modded acceptance, per-cell multi-stack capacity, and carrier rules apply automatically for ANY Building_Storage-derived mod — no per-mod patch. Confirmed in HaulToStack.FindStackCell, the group-space evidence read (now StorageCommitments; formerly BulkHaul.StorageSpaceForDef), StorageRouting.FindBeforeCarryStoreCell, unload FindTargetOrDrop.
- DEPOSIT-RE-GATE IS AUTHORITY: HD's plan-time space/availability estimates are deliberately conservative upper bounds; the actual put-away (FindTargetOrDrop re-runs TryFindBestBetterStorageFor per carried stack; Toils_Haul.PlaceHauledThingInCell re-runs IsGoodStoreCell on arrival) deposits only what each modded cell accepts and floor-drops/re-routes the remainder. This bounds every over-estimate or race to one-cycle churn, never overfill, never cargo loss, never storage-rule bypass — the single most important defense across this family.
- TREAT NON-SPAWNED CONTAINER CONTENTS AS OUT-OF-REACH: HD's sweep/route/stack/count features draw only from spawned floor stacks (listerHaulables / ThingsOfDef filtered to Spawned). Spawned-model mods (ASF, LWM) are seen and handled; virtual/despawned-model mods (Storage Network) gracefully no-op (their items are materialized by the mod's own on-demand spawn at job start, not pre-counted by HD). HD's only network-aware code is an explicit, reflection-only, opt-in, default-off shim — never a hard dependency.
- HD MOD-SPECIFIC PATCHES ARE NARROW AND COMPOSITIONAL: HD touches the shared storage-search methods only via Patch_StorageFilter_Funnel (postfix that ONLY vetoes/narrows __result in non-default contexts, byte-inert otherwise) and HaulToStack (postfix that refines the CELL within vanilla's already-chosen room/priority, re-gated by IsGoodStoreCell). Because HD only narrows (never expands or invents a cell), it composes safely with ASF/LWM/SN transpilers/prefixes/postfixes on the same methods regardless of Harmony order.
- NEVER EVICT FROM A FOREIGN STORAGE MOD'S CELLS: HD relocates only loose / lower-priority floor stacks toward better storage (StorageRouting.WorthRelocating + priority eligibility); it never pulls items OUT of a storage building to relocate them. This avoids the two-storage-system relocation loop LWM itself had to hard-disable for PRF/Extended Storage.

### Adversarial verification
**Confirmed real:**
- LWM mass-limited DSU space OVER-ESTIMATE is REAL (investigator risk #2). Verified: LWM/Patch_HaulAIUtility_HaulToCellStorageJob.cs:25-67 transpiles ONLY the internal GetItemStackSpaceLeftFor call inside HaulToCellStorageJob (-> mass-aware GetDSSpace -> CapacityToStoreThingAt). The STANDALONE Verse.GridsUtility.GetItemStackSpaceLeftFor (decompiled game asm /tmp/vGrids2.cs:300-317) is count-only (uses GetMaxItemsAllowedInCell) and is NOT patched by LWM. HD/BulkHaul.cs:769 calls cells[i].GetItemStackSpaceLeftFor(map, thing.def) directly, so HD's per-cell space for LWM mass-limited DSUs is mass-blind. Investigator correctly identified the seam. (As of #248 that call lives in StorageCommitments.MeasureGroupUncached, the assembly's single remaining GetItemStackSpaceLeftFor call site per the storage-commit-seam build guard — the seam is unchanged, still the standalone count-only extension.)
- ASF per-cell capacity is honored by construction (risk: low/none). Verified: ASF/HarmonyPatches/StorageLimit.cs:15-72 transpiles GridsUtility.GetMaxItemsAllowedInCell -> ThingClass.GetMaxItemsForCell(in cell). HD's GetItemStackSpaceLeftFor and IsGoodStoreCell both route through GetMaxItemsAllowedInCell, so ASF's (even per-cell-varying) limit applies inside HD's calls.
- ASF acceptance/capacity gates honored (risk: low). Verified: ASF/StoreUtilityPatches.cs:18-31 prefixes TryFindBestBetterStoreCellForWorker (-> adaptive.HasCapacityForThing) and :94-159 transpiles NoStorageBlockersIn (-> IsValidStackDestination/ContainsAndAllows). HD routes all acceptance through TryFindBestBetterStorageFor / IsGoodStoreCell, which ASF patches.
- ASF-stored items stay SPAWNED in thingGrid (risk: none). Verified: ASF/RegisteredAtThingGridEvent.cs:13-35 transpiles ThingGrid.RegisterInCell by INSERTING a notify call AFTER the vanilla list.Add — the Add is preserved, so items remain in thingGrid/ThingsOfDef and HD's CellHasPartialStackOf (HaulToStack.cs:243-258, reads ThingsListAtFast) and all ThingsOfDef scans see them.
- LWM ShouldRemoveFrom over-capacity eviction is DISABLED (priorBug 'two-storage relocation loop' appliesToHD:false is correct). Verified: in LWM/Patch_TryFindBestBetterStoreCellFor_ShouldRemoveFrom.cs:49 the [HarmonyPatch] attribute is COMMENTED OUT, and LWM uses harmony.PatchAll() (ModInit.cs:18) which is attribute-driven, so the transpiler never applies. Same for LWM/Patch_ListerHaulablesTick_ShouldRemoveFrom.cs:41 (attribute commented out).
- Neither ASF nor LWM patches JobDriver_HaulToCell.TryMakePreToilReservations or ReservationManager, so HD's no-cell-reservation prefix (HaulToStack.cs:73-125) has nothing to collide with. Verified by grep: ASF has zero hits; LWM's only JobDriver_HaulToCell-adjacent hit is Deep_Storage_Pause.cs (the PlaceHauledThingInCell delay postfix, which touches no reservations).
- HaulToStack no-reservation + LWM storing-delay race (risk #1) is bounded to churn, never COUNT over-fill or loss. Verified: at deposit Toils_Haul.PlaceHauledThingInCell (/tmp/vToilsHaul.cs:320-385, storageMode=true) calls TryDropCarriedThing(Direct) -> GenPlace.TryPlaceDirect (/tmp/vGenPlace.cs:321-369) which enforces GetMaxItemsAllowedInCell COUNT; on full it re-routes via TryFindBestBetterStoreCellFor / CanHaulAside (loser floor-drops). LWM's delay postfix (Deep_Storage_Pause.cs) places via the SAME placeStuff at tick 1, so the count re-gate still applies after the delay.
- Storage Network auto-spawn throttle/Force-Wait (risk #3) is bounded and unlikely to trip from one HD job. Verified: SN/Patch_Pawn_JobTracker_StartJob_AutoSpawn.cs:23-71 spawns the ENTIRE targetQueueB in ONE StartJob call (lines 66-69); the 3-hit throttle (lines 50-53, ForceWaitThreshold=3) counts StartJob calls per pawn per tick. HD's single bulk-load job = one StartJob = one slot, so it does not trip 3 hits unless HD re-issues the job repeatedly in one tick.
- HD Storage Network shim is genuinely opt-in/default-off, reflection-only, read-only at plan time. Verified StorageNetworkCompat.cs:36-115: gated on enableStorageNetworkBulkLoad (default OFF), AccessTools.TypeByName fail-open (active=false if SN absent or API renamed), NetworkStacksOfDef/UsableTerminals are read-only, materialization delegated to SN's own StartJob hook; JobDriver_LoadInBulkBase skips non-Spawned queue entries (safe skip not stall).
- HD's storage-filter funnel composes safely (only narrows) with SN's TryFindBestBetterStorageFor postfix. Verified Patch_StorageFilter_Funnel.cs:31-92: both Patch classes return early when feature off / no storage mod / Unload context (the default + HD's own unload), and otherwise only set __result=false (veto) — never invent a cell. HD's actual put-away runs in the allow-all Unload context, so it can never strand an item.
- Storage Network despawned-contents graceful no-op (risk: low) is correct. Verified the shim's premise in StorageNetworkCompat.cs:18-22 and that HD's spawned-only ThingsOfDef/listerHaulables scans naturally exclude SN's despawned network stacks; SN materializes them at job start via its StartJob hook.

**Refuted (not real / already handled):**
- OVERSTATED / PARTIALLY WRONG: the investigator's blanket claim 'never overfill or storage-mod-rule bypass' (summary + risk #2 fix + topRisks #2) does NOT hold for LWM MASS-limited DSUs. The deposit re-gate is mass-aware only at the FIND stage (NoStorageBlockersIn -> StackableAt -> CapacityToStoreThingAtDirect, CompDeepStorage.cs:252-307 enforces limitingTotalFactorForCell). But the actual PLACE — Toils_Haul.PlaceHauledThingInCell (/tmp/vToilsHaul.cs:347) -> TryDropCarriedThing(ThingPlaceMode.Direct) -> GenPlace.TryPlaceDirect (/tmp/vGenPlace.cs:321-369) — is MASS-BLIND: it clamps only by GetMaxItemsAllowedInCell (COUNT). So when an LWM mass-limited DSU cell has COUNT room but no MASS room, the find gate passes it (CanStoreAny passes if even one unit's mass fits, MapComponentDS.cs:182), and the place deposits the WHOLE carried count, pushing the cell OVER its mass limit by up to one carried stack. Vanilla never does this because HaulToCellStorageJob clamps job.count via LWM's mass-aware GetDSSpace transpile; HD bypasses that because HD's group-space measure (since #248 StorageCommitments' evidence read, MeasureGroupUncached; then BulkHaul.cs:734-774 StorageSpaceForDef) + the unload countToDrop (=InventorySurplus.SurplusOf, never destination-mass-clamped) carry the full surplus. NET: a real, narrow over-mass (storage-rule bypass), not just churn. Still NOT cargo loss (the over-mass items get re-flagged haulable via MapComponentDS.cs:158-159 Notify_Spawned), and bounded to LWM DSUs that set maxTotalMass/altStat — a minority config. So 'never loss' stands; 'never overfill / never storage-mod-rule bypass' is refuted for the mass dimension.
- MINOR: HD's own COMPATIBILITY.md:107 affirmative claim 'HD never over-fills a deep-storage cell' is inaccurate for LWM mass-limited DSUs for the same reason as above (the doc itself flags LWM as 'expected to compose, untested' at :112, so this is a documentation overclaim, not a verified guarantee).

**Missed by the first pass (folded into the plan):**
- MISSED (the concrete mechanism behind risk #2 being more than 'extra churn'): the investigator framed the mass over-estimate purely as a plan-time number that 'self-corrects at deposit time, never overfill'. The actual deposit path (TryPlaceDirect, mass-blind COUNT-only) means the over-estimate is NOT corrected for the item currently being placed — it physically over-fills the mass-limited cell by up to one carried stack. The self-correction is only for SUBSEQUENT items (the next find gate rejects the now-over-mass cell). This is a behavioral divergence from vanilla single-haul (which mass-clamps job.count). Concrete fix if desired: when pricing LWM mass-limited storage, probe via a mass-aware path (e.g. reflectively call CompDeepStorage.CapacityToStoreThingAt, or use Building_Storage.SpaceRemainingFor which LWM DOES patch — LWMDeepStorage/DeepStorage/Patch_Building_Storage_SpaceRemainingFor.cs exists) OR clamp the unload countToDrop to a per-cell mass-aware capacity. Magnitude is bounded (one stack), config-gated (only DSUs with maxTotalMass set), never loss — so likely a low-priority polish, but the 'never overfill' guarantee in both the risk map and HD's COMPATIBILITY.md should be corrected to 'never count-overfill; may transiently mass-overfill an LWM mass-limited DSU by up to one carried stack, self-correcting via re-haul'.
- MISSED detail (not a new risk, corrects an omission): SN ships a SECOND auto-spawn entry point the map didn't list — StorageNetwork/Patch_TryIssueJobPackage_AutoSpawn.cs (postfix on ThinkNode_JobGiver.TryIssueJobPackage). It rewrites only job.targetA (single target), NOT target queues, so it does NOT cover HD's bulk-load (which stages in targetQueueB and relies on the StartJob patch). Harmless to HD, but confirms HD's network bulk-load depends SOLELY on the StartJob queue path — if a future SN version changed StartJob queue handling, HD's network load would silently degrade to single-stack (graceful, already the fallback). Worth a note in the shim's resilience reasoning.
- MISSED nuance (LWM MapComponentDS cache currency under HD's high-throughput no-reservation hauls): the investigator marked the cache prior-bug class appliesToHD:false on the grounds that LWM hooks set_Position/SplitOff/TryPlaceDirect/Notify_Received-Lost. I verified those ARE the mutation points HD's deposits go through (TryDropCarriedThing -> GenPlace.TryPlaceDirect/Notify_ReceivedThing), so the conclusion is right — but note the cache rebuild in MapComponentDS.CellCache.Update (MapComponentDS.cs:146-160) re-reads thingGrid.ThingsAt live each rebuild, and HD keeps NO DSU capacity cache of its own (confirmed), so HD cannot reintroduce LWM's historical stale-zero/partial-stack cache bugs. Verdict unchanged (not affected), just now positively evidenced rather than asserted.

**Verdict:** The risk map is largely ACCURATE and well-grounded — I confirmed all three top risks' seams against actual mod source + decompiled vanilla, and confirmed the four general patterns (validate-through-vanilla, deposit-re-gate-authority, treat-non-spawned-as-out-of-reach, narrow-compositional-patches) hold. ASF (per-cell capacity, acceptance, spawned-in-thingGrid), the disabled LWM ShouldRemoveFrom/ListerHaulablesTick transpilers, the no-reservation race COUNT-safety, and the SN throttle/opt-in-shim assessments are all correct. The ONE substantive correction: the investigator's repeated 'never overfill / never storage-mod-rule bypass' guarantee is REFUTED for LWM MASS-limited DSUs — the deposit-time place (GenPlace.TryPlaceDirect) is mass-blind (count-only), so HD can transiently over-fill such a cell past its mass limit by up to one carried stack (vanilla doesn't, because it mass-clamps job.count via LWM's GetDSSpace transpile that HD's standalone GetItemStackSpaceLeftFor bypasses). This is bounded, config-gated to DSUs with maxTotalMass set, self-corrects via re-haul, and is NEVER cargo loss — so it downgrades from a clean 'never overfill' to a 'minor transient over-mass, churn, never loss'. The 'never loss' thesis and the no-mod-specific-patch-required conclusion both survive; only the absolute 'never overfill' phrasing (in the map AND in HD's COMPATIBILITY.md:107) needs correcting, and an optional polish (mass-aware group-space pricing via the LWM-patched SpaceRemainingFor or CapacityToStoreThingAt) would close it.

---

## Family: `inventory-keepers`

HD already has a mature, well-architected keep-item system that severs the unload<->refetch loop for Simple Sidearms, Combat Extended, Smart Medicine, Dub's Bad Hygiene and Yayo's Combat 3. The single shared choke point is InventorySurplus.SurplusOf (Source/HaulersDream/InventorySurplus.cs:32), which returns 0 ("keep the whole stack") for any item IsManagedKeepItem (line 416) recognizes, and KeepCountOf (line 462) honors the three vanilla keep sources (drug-policy takeToInventory, inventoryStock, packable food) plus the CE loadout reserve. The risk surface is SMALL because: (a) the only feature that can strip a foreign keep-item is the "unload all surplus" option (unloadAllSurplus, default FALSE — HaulersDreamSettings.cs:58); with it OFF, AdoptSurplusInventory only adopts player-ruled defs (PawnUnloadChecker.cs:119-121, :310), so keeper mods are safe by default; (b) HD never uses vanilla JobGiver_DropUnusedInventory (which is where SS/SM/ItemPolicy install their protective patches), it uses its own JobDriver_UnloadHauledInventory, which is precisely WHY a per-mod bridge is needed; (c) HD's tagging self-heal and scoop pick already exclude Simple Sidearms remembered sidearms (YieldRouter.InventoryStackOfDef:686, CompHauledToInventory self-heal:104). The ONE genuinely unprotected mod is ITEM POLICY (new, no shim): it keeps an arbitrary count of ANY ThingDef per pawn via its own Dictionary (ItemPolicyUtility.policies), re-fetches via JobGiver_TakeItemForInventoryStock (a TakeInventory job when pawn.inventory.Count(def) < N), and only protects DRUGS, and only against vanilla DropUnused, via a ShouldKeepDrugInInventory postfix — so HD's surplus unload (when unloadAllSurplus=true) WILL strip ItemPolicy-stocked items (drug and non-drug alike) and trigger the loop. The best GENERAL fix is two-fold: (1) replace HD's inline drug-keep mirror with an actual call to vanilla JobGiver_DropUnusedInventory.ShouldKeepDrugInInventory(pawn, thing) for drug items, which automatically honors EVERY keeper mod's postfix on that method (covers Smart Medicine's stocked drugs AND ItemPolicy's stocked drugs in one hook, replacing/backstopping the SM reflection shim for drugs); and (2) add a general "inventory-stock contract" reader that, in addition to vanilla inventoryStock, reflects ItemPolicy's per-pawn GetItemPolicyEntry(pawn, def) keep-count — covering ItemPolicy's NON-drug items (which have no vanilla hook). ItemPolicy is best handled as a small targeted reflection shim feeding the general KeepCountOf path, because its policy data is its OWN dictionary, not vanilla inventoryStock, and no general vanilla signal exposes it. Two Compositable Loadouts entries were added later (issue #233), and they invert this family's assumption: both faults are HD's own shim keeping too MUCH rather than too little, and over-keeping turns out to be the worse failure — an under-keep merely loops against the mod's re-fetch and self-corrects, while an over-keep of an item the mod would never re-fetch strands HD's own hauled cargo permanently, because nothing but HD would ever release it. Survival Tools Reborn was assessed later still and is the family's clearest illustration of point (a): it needed a shim (its tools sit in `innerContainer` and it re-fetches them from STORAGE by design, and neither the Grab Your Tool keep nor the Simple Sidearms keep could cover them), yet it was harmless at HD's shipped defaults for exactly the reason above — the toggle was off.

### Feature risk map

- `LOW` **Simple Sidearms — Keep remembered sidearms in inventory; re-fetch dropped ones (JobGiver_RetrieveWeapon -> ReequipSecondary job)**
  - Mechanism: CompSidearmMemory.rememberedWeapons (List<ThingDefStuffDefPair>) tracks wanted weapons; JobGiver_RetrieveWeapon.TryGiveJobStatic (rimworld/JobGiver_RetrieveWeapon.cs:15) re-equips any remembered weapon the pawn lacks from the map. SS ITSELF protects via a Pawn_InventoryTracker.FirstUnloadableThing getter postfix (intercepts/Intercepts_AI.cs:393) that skips remembered sidearms (removing the equipped primary first) and a JobGiver_DropUnusedInventory.Drop prefix (intercepts/Intercepts_AI.cs:361) vetoing drops of needed weapons. HD bypasses both because it uses its own unload driver.
  - HD overlap: InventorySurplus / SimpleSidearmsCompat
  - Fix (targeted patch): Already fully handled. SimpleSidearmsCompat (SimpleSidearmsCompat.cs) reflects rememberedWeapons (def,stuff) and InventoryKeepCount subtracts the equipped primary exactly like SS's own FirstUnloadableThing postfix (lines 198-209). SurplusOf's weapon branch (InventorySurplus.cs:70-99) keeps the wanted count and unloads hauled duplicates. IsRememberedSidearm guards the scoop pick and self-heal so HD never tags a sidearm. No change needed.
- `LOW` **Simple Sidearms — Auto-record equipped/picked-up weapons into rememberedWeapons (so a hauled weapon HD scooped could be mistaken for kit)**
  - Mechanism: Verb.Notify_EquipmentLost transpiler + JobDriver_Equip/DropEquipment patches (intercepts/Verb_Notify_EquipmentLost_Patches.cs, JobDriver_Equip_MakeNewToils_Patches.cs) record/forget weapons. SS only records EQUIPPED weapons, never inventory-hauled loose stacks, so an HD-scooped weapon is never auto-remembered.
  - HD overlap: CompHauledToInventory tagging / YieldRouter scoop pick
  - Fix (targeted patch): Already handled: YieldRouter.InventoryStackOfDef (line 573) and the GetHashSet self-heal (CompHauledToInventory.cs:132) call IsRememberedSidearm to never tag a genuine remembered sidearm; and for non-stacking weapons (stackLimit 1) the scoop tags the SPECIFIC scooped Thing, so a hauled duplicate is unloadable while the kept sidearm is not. No change.
- `NONE` **Simple Sidearms — Caravan formation pulls remembered weapons into a pawn's inventory**
  - Mechanism: Intercepts_Caravan.cs (lines ~287/322) TryTransferToContainer of biocoded/matching weapons into pawn.inventory.innerContainer during caravan/split. These are remembered-weapon defs, so they are covered by the rememberedWeapons keep.
  - HD overlap: InventorySurplus (only if unloadAllSurplus + at home)
  - Fix (general / no-op): Covered by the existing rememberedWeapons keep (same as the sidearm feature). Caravan/away maps are also outside HD's at-home unload scope. No change.
- `LOW` **Combat Extended — Loadout ammo/items kept in inventory; JobGiver_UpdateLoadout force-drops items the loadout doesn't cover and re-fetches missing loadout items**
  - Mechanism: CompInventory + Loadout (Jobs/JobGiver_UpdateLoadout.cs:331-360 GetExcessThing/GetExcessEquipment drop; GetPrioritySlot re-pickup). Utility_HoldTracker.Notify_HoldTrackerItem (Loadouts/Utility_HoldTracker.cs:41) lets a pawn HOLD non-loadout items so they aren't dropped.
  - HD overlap: InventorySurplus.KeepCountOf (CE loadout reserve) + CECompat.NotifyHeld
  - Fix (targeted patch): Already handled. CECompat.LoadoutKeepCount (CECompat.cs:265) reads the pawn's loadout slot counts (exact-def) into KeepCountOf (InventorySurplus.cs:303), and CECompat.IsCarriedAmmo (line 214) keeps ALL CE AmmoDef stacks. CECompat.NotifyHeld registers HD-scooped goods with CE's HoldTracker so JobGiver_UpdateLoadout doesn't dump them. No change.
- `LOW` **Combat Extended — Weight+Bulk carry caps (CarryWeight stat overrides Capacity; per-item Bulk vs CarryBulk) + encumbrance move-speed penalty**
  - Mechanism: CE postfixes MassUtility.Capacity; CompInventory.CanFitInInventory(Thing, out int, bool, bool) enforces weight+bulk; StatWorker_MoveSpeed applies CE's own encumbrance penalty (calls base first).
  - HD overlap: OverloadGate / StatPart_Overload / every pickup-count decision
  - Fix (targeted patch): Already handled. CECompat.MaxFitCount (line 165) clamps every pickup through CanFitInInventory; smart overload stands down (OverloadGate.NoOverload) and StatPart_Overload applies no factor under CE so penalties don't stack. No change.
- `LOW` **Combat Extended — HoldTracker cleanup never prunes its FIRST record (loops i>0)**
  - Mechanism: Utility_HoldTracker.HoldTrackerCleanUp (Loadouts/Utility_HoldTracker.cs:138 'for (int i = recs.Count-1; i > 0; i--)') never removes recs[0]; Notify_HoldTrackerItem re-adds count on top of current inventory amount (line 64 'GetMagazineAwareStackCount(...) + count'), so the first-scooped def's record can outlive its goods and inflate on re-notify.
  - HD overlap: CECompat.NotifyHeld (re-notify path)
  - Fix (targeted patch): Pre-existing CE bug HD already documents (CECompat.cs class summary). Mitigation is optional and CE-specific; impact is a CE-side stale record, not a black hole. Leave as documented follow-up; not a general pattern. Do NOT keep re-notifying the same def unnecessarily.
- `LOW` **Smart Medicine — Stock-up: doctors keep N of a medicine/drug in inventory and re-fetch when low (JobGiver_StockUp); SM protects stocked drugs from vanilla drop**
  - Mechanism: StockUpUtility per-pawn Dictionary<ThingDef,int> (StockUp/StockUpUtility.cs:52); JobGiver_StockUp re-fetches when below stock (skips when pawn.inventory.UnloadEverything); StockingUpOn(Pawn, ThingDef) (line 119) is the keep test. SM protects via DontDropStockedDrugs prefix on JobGiver_DropUnusedInventory.ShouldKeepDrugInInventory (StockUp/DontDropStockedDrugs.cs:23) — only the vanilla drop path, only drugs.
  - HD overlap: InventorySurplus / SmartMedicineCompat
  - Fix (targeted patch): Already handled. SmartMedicineCompat.IsStockedUp (SmartMedicineCompat.cs:55) reflects StockingUpOn; IsManagedKeepItem (InventorySurplus.cs:262) keeps the whole stack. NOTE the general improvement below: routing HD's drug-keep decision through vanilla ShouldKeepDrugInInventory would honor SM's own postfix and make this shim a backstop rather than the sole protection. SM also keeps non-drug medicine (herbal/industrial), which are not drugs — still covered by IsStockedUp. No urgent change.
- `NONE` **Smart Medicine — Use medicine from inventory (own/patient/other pawn/pack animal) for tending; StockDown drops EXCESS over stock count**
  - Mechanism: MedicineGrabbing / InventorySurgery pull medicine from any pawn's inventory; StockUpUtility.StockUpThingToReturn (line 188) + a StockDown job drop medicine when over the stock count.
  - HD overlap: EatFromInventory (food only) / OrganicInventoryShare (build mats) / InventoryShare
  - Fix (general / no-op): No overlap. EatFromInventory reads FOOD only (FoodUtility.BestFoodInInventory, EatFromInventory.cs:125; drugs gated by allowDrug). OrganicInventoryShare/InventoryShare only source CONSTRUCTION materials by the blueprint's needed def — medicine is not a build material. HD keeps the whole stocked stack (>= SM's count), so SM's own StockDown still trims the excess; HD never fights it. No change.
- `HIGH` **Item Policy — Per-pawn keep-N-of-any-ThingDef inventory stock; re-fetch via JobGiver_TakeItemForInventoryStock (TakeInventory job when count < N)**
  - Mechanism: ItemPolicyUtility.policies Dictionary<Pawn, ItemPolicy{Dictionary<ThingDef,int>}> (ItemPolicyUtility.cs:9); GetItemPolicyEntry(pawn, def) (line 84) is the keep count; JobGiver_TakeItemForInventoryStock.TryGiveJob (JobGiver_TakeItemForInventoryStock.cs:17) re-fetches when pawn.inventory.Count(def) < N (skips when pawn.inventory.UnloadEverything). It uses its OWN dictionary, NOT vanilla inventoryStock.
  - HD overlap: InventorySurplus.KeepCountOf / IsManagedKeepItem (NOT covered — no shim)
  - Fix (targeted patch): GENERAL+TARGETED. ItemPolicy is the one unprotected keeper. With unloadAllSurplus=ON, AdoptSurplusInventory (PawnUnloadChecker.cs:250) adopts an ItemPolicy-stocked stack as surplus and the unload ships it -> ItemPolicy re-fetches -> unload<->refetch loop. Fix: add a small reflection shim (ItemPolicyCompat) exposing GetItemPolicyEntry(pawn, def), and FEED it into the GENERAL keep path — add its count to KeepCountOf (InventorySurplus.cs:290) so the same inventoryStock-style 'wants N of def' contract is honored for ItemPolicy as it is for vanilla inventoryStock and CE loadout. This automatically covers drug AND non-drug stocked items and needs no per-item user rule. A targeted reflection shim is genuinely required because ItemPolicy stores in its own dictionary, not vanilla inventoryStock.
- `MEDIUM` **Item Policy — Protect stocked DRUGS from being dropped (ShouldKeepDrugInInventory postfix)**
  - Mechanism: ShouldKeepDrugInInventoryPatch postfix on JobGiver_DropUnusedInventory.ShouldKeepDrugInInventory (ShouldKeepDrugInInventoryPatch.cs:10) sets __result=true when GetItemPolicyEntry > 0. Only covers DRUGS and only the vanilla DropUnused path (which HD does not use).
  - HD overlap: InventorySurplus.IsManagedKeepItem (drug branch is inline mirror, not a real call)
  - Fix (general / no-op): GENERAL pattern: HD's IsManagedKeepItem currently INLINE-mirrors vanilla ShouldKeepDrugInInventory's logic (addiction/chem-dep at InventorySurplus.cs:260) but does NOT call the patched method, so it misses every keeper mod's postfix (ItemPolicy's drug postfix, SM's DontDropStockedDrugs). Change the drug branch to CALL JobGiver_DropUnusedInventory.ShouldKeepDrugInInventory(pawn, thing) (a vanilla public static, decompile-verified) for IsDrug items — this honors ALL such postfixes generically. This is the single highest-leverage general fix: it covers ItemPolicy drugs + SM drugs + any future ShouldKeepDrugInInventory-postfixing mod with one hook. Keep the inline addiction check as a same-result backstop.
- `HIGH` **Compositable Loadouts — HD's own CL shim shields APPAREL that CL would never re-fetch, permanently stranding apparel HD itself hauled into the pack (HD issue #233)**
  - Evidence tiers used below, because CL is NOT installed on this machine and could not be decompiled: **OBSERVED** = read in HD's own tree, or in the reporter's description/log (line-cited); **SOURCE-READ** = read from CL's public source at github.com/simplyWiri/Loadout-Compositing, NOT decompile-verified against a running build; **INFERRED** = neither, reasoned.
  - Mechanism: OBSERVED (HD tree) — an apparel stack falls through every earlier branch of `InventorySurplus.SurplusOf`: the Simple Sidearms branch is hard-gated on `IsRangedWeapon || IsMeleeWeapon` (`InventorySurplus.cs:132`, shifted from :107 when the Survival Tools branch landed above it), and `IsManagedKeepItem` (`:379-419`) has no apparel branch at all. It therefore lands on the fallthrough (`:149-153`), whose `keep` includes `CompositableLoadoutsCompat.KeepCount` via `KeepCountOf` (`:443`). That method matched **def-only** and summed CL's `Item.Quantity`, while the other side of the same subtraction (`YieldRouter.InventoryCountOfDef:709-718`) counts `innerContainer` **only** — so a WORN duster could never discharge the loadout's duster entry and the entire requirement was charged against the pack. `grep IsApparel` over `Source/` returned **zero** hits before this fix: HD had no apparel-awareness anywhere in its keep model. OBSERVED (report) — loadout of helmet/pants/duster, all worn; three EXTRA dusters bulk-hauled into the pack and never unloaded; removing "duster" from the loadout released all three at once; disabling HD's bulk loading prevents the strand. SOURCE-READ (CL) — CL excludes apparel from the items it fetches into inventory at two sites, `Loadout.DesiredItems` (opens `Items.Where(t => !t.Def.IsApparel)`) and `ThinkNode_LoadoutRealisation.SatisfyLoadoutItemsJob` (iterates `loadout.Items.Where(item => !item.Def.IsApparel)`); apparel is satisfied instead by `SatisfyLoadoutClothingJob`, a modified `JobGiver_OptimizeApparel` issuing `JobDefOf.Wear` against garments ON THE MAP; and CL actively wants the excess gone — `FirstUnloadableThing_Patch.ShouldDropThing` surrenders `currentQuantity - desiredQuantity`, a vanilla-unload hook HD's own unload driver never consults. INFERRED — with CL's default `quantity = 1` the pre-fix bug strands exactly ONE unit; stranding three needs `quantity >= 3` or the def named by three active tags (`Loadout.Items` is `Tags.SelectMany`, so duplicate entries sum).
  - HD overlap: CAUSAL, both halves. The keep is HD's own shim behaving exactly as written, and the cargo is HD's own: CL alone never puts apparel into `innerContainer` — HD's bulk-load does. HD was therefore the only thing that would ever have taken it back out, and it was refusing to. This is not a keeper mod fighting HD; it is HD pinning its own haul against a requirement the keeper had already satisfied elsewhere.
  - Fix (targeted patch): SHIPPED. `CompositableLoadoutsCompat.KeepCount` returns 0 for any `def.IsApparel`, short-circuited BEFORE `InitKeepApi()` and before the `keepApiOk` latch, so the decision is a pure vanilla read that holds whether or not CL's reflected API ever resolves and can never be attributed to (or disabled by) a CL fault. The rule itself is the Verse-free, unit-tested `CompositableLoadoutKeepPolicy.ShieldsDef`. Deliberately NOT generalised to `CECompat.LoadoutKeepCount` or `ItemPolicyCompat.KeepCount`: CE loadouts and Item Policy stock legitimately hold apparel and DO re-fetch it, so the carve-out is justified by CL's own source and only CL's. Safe even if the SOURCE-READ claims are wrong: worst case HD unloads a piece CL wanted, CL re-fetches it once, and the per-def unload rules plus the per-pawn keep pin remain — versus a guaranteed permanent strand today.
- `MEDIUM` **Compositable Loadouts — A WIELDED loadout weapon does not discharge its loadout entry, so a hauled spare of that def is pinned forever (HD issue #233, second half)**
  - Mechanism: SOURCE-READ (CL) — CL's own gear list is `Utility.InventoryAndEquipment(pawn)`, i.e. the pawn's inventory CONCAT `equipment.AllEquipmentListForReading`, so an equipped weapon satisfies its loadout entry. OBSERVED (HD tree) — HD's comparison counts inventory alone (`YieldRouter.InventoryCountOfDef:709-718` reads `innerContainer`), so the entry stayed fully charged against the pack and any spare of the same def HD hauled read as kept. Same bug class as the apparel entry above, different item class. OBSERVED (report) — live for this reporter: their log shows CL + Combat Extended but NO Simple Sidearms, so the `SurplusOf` weapon branch that would otherwise intercept every weapon before this keep (`InventorySurplus.cs:132`, gated on `SimpleSidearmsCompat.IsActive`) is inert for them.
  - HD overlap: CAUSAL, HD-side only — CL's model is self-consistent; HD's "have" was narrower than CL's.
  - Fix (targeted patch): SHIPPED, and applied INSIDE the CL term rather than on the shared "have" side. The comparison is `have_inventory - (K_drug + K_stock + K_CE + K_ItemPolicy + K_CL)`, so counting equipment for the CL term alone is algebraically identical to replacing `K_CL` with `max(0, K_CL - equipped)` — one term, no other keeper disturbed, `KeepCountOf`'s signature untouched, and no new reflection (`pawn.equipment.AllEquipmentListForReading` is vanilla). Decided by `CompositableLoadoutKeepPolicy.ContributedKeep`; mirrors `SidearmKeepMath.KeepForPair`, which subtracts the equipped primary from the Simple Sidearms keep for exactly this reason (documented at `InventorySurplus.cs:116-123`). Deliberately NOT gated on `(def, stuff)` equality — REQUIRED, not merely defensible: `wanted` (def-only entry match) and `have` (`YieldRouter.InventoryCountOfDef`, def-only) are both def-only in this branch, so a stuff-qualified `equipped` would be an ASYMMETRIC comparison, and the asymmetry lands on the COMMON case (permissive CL filter, steel longsword wielded, plasteel longsword hauled → no subtraction → spare pinned forever) in order to cover the rarer restrictive-`Filter` case. KNOWN RESIDUAL, and its cost depends on the "unload all surplus" setting: the match ignores CL's per-entry `Filter`, so a filter-restricted entry plus a def-matching but filter-FAILING wielded weapon under-keeps by one (CL wants 1 × longsword restricted to plasteel, the pawn wields a STEEL one, both sides match on def → keep 0 → the plasteel spare unloads). At the DEFAULT (`unloadAllSurplus` OFF) that costs ONE churn trip and stops — `CompHauledToInventory`'s self-heal builds `liveDefs` only from CURRENTLY TAGGED things (`CompHauledToInventory.cs:124-128,182-190`), so once HD's last tag of the def leaves the pack it cannot re-adopt the copy CL fetched back. With `unloadAllSurplus` ON it is a REPEATING LOOP: CL re-fetches the spare UNTAGGED and `PawnUnloadChecker.cs:110-112` calls `AdoptSurplusInventory(pawn, comp, adoptAll: true)`, which re-tags every stack with `SurplusOf > 0` and a destination (`:297-340`; the Simple Sidearms / Grab Your Tool guards at `:327-329` are inert for a CL-only user) → unloaded again → forever, until the `Filter` is honoured. Still strictly better than the pre-fix state for the same player (a churn loop self-corrects the moment the filter is honoured or the weapon is swapped; a strand never does) — but it is a loop, not a trip. `Filter.Allows` / `Item.CountIn` (and `Loadout.ItemsAccepting` / `Loadout.DesiredCount` / `IsValidLoadoutHolder`) are the follow-up, deferred because none of them can be verified without CL installed and each is a new silent-degrade surface.
- `MEDIUM` **Survival Tools Reborn — Work tools kept in `inventory.innerContainer` and re-fetched FROM STORAGE by auto-pickup + the tool optimizer; no existing HD keep covered them**
  - Evidence grade: **assembly-verified** — the repo at github.com/Jellypowered/SurvivalTools (commit `47f1e3e`) cloned beside the other compat-research clones, its shipped `1.6/Assemblies/SurvivalTools.dll` decompiled with ilspycmd (`SurvivalTools.SurvivalTool` and `SurvivalTools.SurvivalToolProperties` both present in the assembly's type list, matching the source tree). Steam 3554664966, packageId `jellypowered.survivaltools`, RimWorld 1.6, a permitted continuation of XeoNovaDan's Survival Tools with the SAME namespace and identity types. NOT installed on this machine, so no live in-game confirmation.
  - Mechanism: tools are `SurvivalTools.SurvivalTool : ThingWithComps` (`Source/SurvivalTool.cs:9`), identified by the mod's own two-part predicate `def.thingClass == typeof(SurvivalTool) && def.HasModExtension<SurvivalToolProperties>()` (`Source/SurvivalToolUtility.cs:411-412`). Held tools live in the pawn's inventory — `GetHeldSurvivalTools` is literally `inventory.innerContainer.Where(t => t.def.IsSurvivalTool())` (`:429-430`) — and only a humanlike may use them (`CanUseSurvivalTools`, `:418-424`, requires `RaceProps.Humanlike && !IsMechanoid`). Re-fetch is DEFAULT-ON and twofold: an auto-pickup postfix on `JobGiver_Work.TryIssueJobPackage` queues a `TakeInventory` for the best map tool before a gated job (`Source/AI/AutoToolPickup_UtilityIntegrated.cs:11`), and `JobGiver_OptimizeSurvivalTools` rescans every 3600-14400 ticks (`Source/AI/JobGiver_OptimizeSurvivalTools.cs:12-13`) — and its acquisition policy explicitly accepts a STORED tool as a source, `ToolIsAcquirableByPolicy` returning true for any `tool.IsInAnyStorage()` (`:275`). The mod's own protection does not reach HD: it guards a single vanilla seam, a postfix on `Pawn_InventoryTracker.FirstUnloadableThing` (`Source/Harmony/Patch_Pawn_InventoryTracker.cs:13`), which HD's own unload driver never consults. It defines a Pick Up And Haul / CE awareness flag, `ModCompatibilityCheck.OtherInventoryModsActive` (`Source/ModCompatibilityCheck.cs:56`), that NO code reads — it has no live handling for inventory-hauling mods at all, so the whole burden is HD's, exactly as for Grab Your Tool.
  - HD overlap: `InventorySurplus.SurplusOf` — and NEITHER neighbouring keep covered it. The Grab Your Tool branch (`InventorySurplus.cs:87`) requires `def.equippedStatOffsets` (`GrabYourToolCompat.cs:85-86`), and no tool def has any: `grep equippedStatOffsets` over the mod's defs hits only the tool-belt apparel, because the work-stat factors flow through the mod's own `StatPart_SurvivalTool` rather than vanilla equipped offsets. Meanwhile all 22 concrete tool defs carry melee `<tools>` and `category=Item`, so vanilla `ThingDef.IsWeapon`/`IsMeleeWeapon` are TRUE and the Simple Sidearms branch (`:132`) swallowed them with `RememberedCount == 0` → `SurplusForPair` = the whole tool. `IsManagedKeepItem` and `KeepCountOf` produce nothing for a tool def either. Net: `SurplusOf` returned FULL surplus for every carried survival tool, with or without SS installed. What made it harmless at HD's defaults is the tag gate alone — the unload pass is tag-scoped, a tool the mod fetched via `TakeInventory` is untagged, and `unloadAllSurplus` ships `false` (`HaulersDreamSettings.cs:58`). Turn that option ON and `AdoptSurplusInventory` tags the pawn's whole toolkit (the #222 adoption guard excluded SS sidearms, GYT tools and interaction food — a survival tool matched none of the three), HD ships it to storage, and the mod's auto-pickup fetches it back at the next gated job: the canonical unload↔re-fetch loop, same class as #20 (SS) and #200 (CL).
  - Fix (targeted patch): SHIPPED, as `SurvivalToolsCompat` + four one-line call sites, deliberately the exact shape of `GrabYourToolCompat`. Two cached `AccessTools.TypeByName` lookups (`SurvivalTools.SurvivalTool`, `SurvivalTools.SurvivalToolProperties`), no per-call invoke and NO read of the mod's own state (assignment tracker, forced-handler, wear counter all untouched), so it is read-only and MP-deterministic; absent → both null → every call site short-circuits false → byte-identical without the mod. The identity test reproduces the mod's own `IsSurvivalTool` pair without a reflective invoke: `toolType.IsInstanceOfType(thing)` (broader than `def.thingClass ==`, so a fork that subclasses the tool class is covered) plus a walk of the vanilla `Def.modExtensions` list for the properties extension (same walk as `DbhCompat.IsKeptDrink`) — the extension half is kept because a def without it is not a tool the mod manages or would re-fetch, so keeping it would pin a stack for no one. Present-but-unresolved is warned once through `HDLog.Warn` and FAILS CLOSED to "not a tool", the cheap direction here: the cost is a self-correcting churn loop, whereas failing open pins arbitrary inventory. The rule itself is the Verse-free, unit-tested `SurvivalToolKeepPolicy.KeepsCarriedTool`. Call sites: the new `SurplusOf` branch (`InventorySurplus.cs:106`) sits ABOVE the Simple Sidearms branch — load-bearing, since the tools are `IsMeleeWeapon` and SS would otherwise intercept them first — plus the adoption guard (`PawnUnloadChecker.cs:372`), the tag self-heal's `excludeFromTag` (`CompHauledToInventory.cs:217`), and the scoop's first-of-def pick (`YieldRouter.cs:704`). Keep-ALL rather than count-precise, on purpose: the mod polices its own excess — an idle over-limit drop on the inventory tick (`Source/Harmony/Patch_Pawn_InventoryTracker.cs:53-88`, gated on `curJob == null` and `heldCount > SurvivalToolCarryCapacity`) plus the optimizer's dedup/downgrade drops (`Source/AI/JobGiver_OptimizeSurvivalTools.cs:83,95,133`), all of which route through `DequipAndTryStoreSurvivalTool` and STORE to a stockpile rather than littering — so there is no count to model, and reading its per-pawn assignment filter would add reflection risk to shave nothing. Deliberately does NOT defer to a matching SS remembered weapon the way the GYT branch does — a survival tool CAN be equipped, so SS can remember one, and SS's count-aware keep would then unload every copy above the remembered count, while the optimizer carries SEVERAL tools on purpose and would fetch each back. Over-keeping cannot strand HD's own cargo (the #233 failure mode) because the branch carries the same `!hdSwept` guard as its siblings: a loose tool HD bulk-hauled off the ground stays unloadable, so the keep can only ever pin a tool the pawn acquired for itself — the set the mod would re-fetch anyway — and an explicit per-def "Unload always" rule still wins ahead of it.
- `NONE` **Survival Tools Reborn — everything else HD touches (audited, no code change)**
  - Mechanism: its `MassUtility` postfixes (`Source/Harmony/Patch_MassUtility.cs:21`) force `WillBeOverEncumberedAfterPickingUp` → true at the tool carry limit; HD reads that in exactly one place, the WYU-parity stand-down (`StorageRouting.cs:142`), so HD occasionally declines to relocate a loose tool closer to a consumer — a conservative no-op with vanilla haul proceeding, and HD's own trip math reads capacity/mass directly and is untouched. Co-patching `JobGiver_Work.TryIssueJobPackage` composes in either order: checked mechanically, 14 of HD's 16 job defNames match NO substring pattern in its `StatsForJob` fallback chain (`Source/SurvivalToolUtility.cs:304-387`), and the two that do — `HaulersDream_OverloadConstructDeliver` and `HaulersDream_ConstructDeliverBuild`, on `construct`/`build` → `ConstructionSpeed` — are symmetric with the mod's own XML, which gates every `workType=Construction` WorkGiver including vanilla's deliver-resources ones (`1.6/Patches/Core/WorkGiverDefs/WorkGivers.xml`): hardcore mode blocks toolless delivery with or without HD, and HD ships no WorkGiverDefs (only `Defs/JobDefs`) for that patch to reach. HD flows that bypass its `HasJobOnThing/Cell` tool gate (the construct tether's playerForced `FinishFrame`, `ConstructTether.cs:41-42`) are vanilla-forced-order parity: vanilla's own right-click Prioritize skips the same probes, so a toolless hardcore pawn stalls at ×0 speed either way, which is the mod's enforcement model (`StatPart_SurvivalTool.NoToolStatFactor` = 0 in hardcore, `Source/Stats/StatPart_SurvivalTool.cs:31`), visible and player-initiated. Tool mass is ordinary 2-2.5 kg inventory mass and the mod patches neither `Capacity` nor `GearAndInventoryMass`, so HD's smart-overload and `StatPart_Overload` are undistorted in both directions. Yield/degradation paths do not overlap (it hooks its own damage/stat seams, HD hooks `GenPlace`).
  - HD overlap: none requiring a change — bulk-hauling loose tools to a stockpile is desirable and is where the mod's own `DequipAndTryStoreSurvivalTool` (`Source/SurvivalToolUtility.cs:932`) puts excess anyway.
  - Fix (general / no-op): documented, no HD change. NOT HD'S TO FIX, per the standing rule: the hardcore ×0-speed stall on forced/gate-bypassing work is the mod's enforcement design, and its unread `OtherInventoryModsActive` flag means its inventory-mod handling was never built — `FirstUnloadableThing` is the only unload seam it guards. The HD-side keep above makes both questions moot for a player running the two together.
- `LOW` **Dub's Bad Hygiene — Carried water/drink kept in inventory; drink-from-pack need re-fetches it**
  - Mechanism: WaterExt DefModExtension on drink defs (DbhCompat reads it); DBH's drink-from-pack need re-fetches dropped water.
  - HD overlap: InventorySurplus / DbhCompat
  - Fix (targeted patch): Already handled. DbhCompat.IsKeptDrink (DbhCompat.cs:46) detects the WaterExt mod-extension; IsManagedKeepItem keeps the whole stack. No change.
- `LOW` **Yayo's Combat 3 — Consumable ammo carried in inventory; YC3 re-stocks/reloads from it (caravan-return pawn could stall in HD's unload churning ammo)**
  - Mechanism: yy_ammo_category ThingCategoryDef + yy_ammo defName prefix (def-based detection, fork-proof).
  - HD overlap: InventorySurplus / YayoCombatCompat / unload anti-churn guard
  - Fix (targeted patch): Already handled. YayoCombatCompat.IsCarriedAmmo (YayoCombatCompat.cs:44) keeps all YC3 ammo; the unload driver's skippedThisJob guard (JobDriver_UnloadHauledInventory.cs:154) prevents the caravan-return freeze on any un-movable stack. No change.
- `MEDIUM` **(general / any keeper mod) — Any mod that keeps items via its own per-pawn 'wants N of def' tracking and re-fetches them**
  - Mechanism: Mod-specific dictionaries + a re-fetch JobGiver, plus (commonly) a ShouldKeepDrugInInventory postfix for drugs. None of these are visible to HD's vanilla-keep model.
  - HD overlap: InventorySurplus.KeepCountOf / IsManagedKeepItem
  - Fix (general / no-op): GENERAL PATTERN (the durable fix): (1) call vanilla JobGiver_DropUnusedInventory.ShouldKeepDrugInInventory for drug items so every drug-keeper mod's postfix is honored automatically; (2) keep the existing inventoryStock + drug-policy + CE-loadout count contract in KeepCountOf and ADD a small registry of reflection-detected 'keep-count providers' (vanilla inventoryStock today; ItemPolicy GetItemPolicyEntry; extensible) so a 'wants N of def' contract from any future mod plugs in without a bespoke SurplusOf branch. This turns each new keeper into a one-line provider registration rather than a new IsManagedKeepItem clause.

### Prior bugs learned from (their bug-fix history)

- ⚠️ applies to HD — **Colonists unloading their own Simple Sidearms sidearm (unload<->reequip loop)** _(HD commits ef74084 / d7ca315 (#20))_ — HD's OWN prior bug, already fixed via SimpleSidearmsCompat. This is the canonical bug-class for the whole family: HD strips a kept item, the mod re-fetches it, HD re-adopts. Any new keeper (ItemPolicy) reintroduces the SAME class unless covered by SurplusOf's keep path.
- ⚠️ applies to HD — **Keeping the wrong NUMBER of sidearms (kept every same-type weapon, or never unloaded a hauled duplicate matching the equipped primary)** _(HD commits 1222a9f / 18f0ae9 / ce07def / 704fe59 (#21))_ — Already fixed: InventoryKeepCount subtracts the equipped primary (mirroring SS's own FirstUnloadableThing postfix) and the scoop tags the specific Thing. Lesson for ItemPolicy: the keep must be COUNT-aware (keep N, unload the excess) not keep-all, or a hauled duplicate is hoarded. ItemPolicy's GetItemPolicyEntry gives the exact N — use it as a keep-count, like InventoryKeepCount.
- ⚠️ applies to HD — **Pawns freezing in 'unloading inventory' over Yayo's Combat 3 ammo (caravan-return stall)** _(HD commits 64b72e5 / 074ed5c (#16))_ — Already fixed via YayoCombatCompat + the skippedThisJob anti-churn guard. Same risk for ItemPolicy: if HD repeatedly tries to unload an item another mod is re-grabbing, the pawn churns. The skippedThisJob guard backstops it, but the real fix is to not adopt the kept item at all (the KeepCountOf fix).
- ⚠️ applies to HD — **Inventory unload loops with keep-mods + random-drop of un-haulable items** _(HD commits 12d5245 / fe5369d (#12), 14e255e (#9))_ — Already fixed: replaced a keep-filter with per-item unload rules + CE-loadout keep + the cannot-unload red alert black-hole net. The per-item 'KeepAll/KeepAtMost' rules (InventorySurplus.cs:49) are the user-facing escape hatch for ANY unprotected keeper (incl. ItemPolicy today) until a general shim lands.
- ⚠️ applies to HD — **Smart Medicine stocked drugs dropped by vanilla DropUnusedInventory (1.3->1.4 they moved protection from a TryGiveJob transpiler to a ShouldKeepDrugInInventory prefix)** _(Smart Medicine DontDropStockedDrugs.cs (code comments lines 13-19))_ — SM and ItemPolicy BOTH protect drugs by patching the SAME vanilla method ShouldKeepDrugInInventory. HD reimplements that method's logic inline instead of calling it, so HD silently misses both postfixes. The general fix (call the vanilla method for drugs) is what makes HD honor whatever these mods decide — exactly mirroring how they coordinate with each other through that shared vanilla hook.
- n/a to HD — **Smart Medicine default unlimited-surgery setting overwritten** _(Smart Medicine commit 6ee8083)_ — SM-internal settings bug, unrelated to hauling/inventory. No HD interaction.
- ⚠️ applies to HD — **Build-from-inventory / DoBill could cannibalize a pawn's own pre-loaded ingredients or earmarked caravan cargo** _(HD commits f805fe8 / 362276f / 25b0155)_ — Already guarded: OrganicInventoryShare excludes IsFormingCaravan holders, forbidden cells, in-vehicle holders, and mid-HD-batch holders (OrganicInventoryShare.cs:54-68). For keeper mods this matters if a kept item's def is ALSO a construction material (e.g. a stocked component): OrganicInventoryShare reads the WHOLE innerContainer by def and would source a kept item as build material. Low-probability (keepers stock weapons/ammo/medicine/drugs, rarely build mats) but worth noting — consider excluding IsManagedKeepItem stacks from the organic build source too.

### Top risks
- COMPOSITABLE LOADOUTS APPAREL STRAND (high, issue #233, FIXED): HD's own CL keep shim charged a loadout's APPAREL entry entirely against the pack while the WORN garment satisfied it invisibly, so apparel HD had bulk-hauled was pinned in the pack permanently — HD put it there and HD was the only thing that would take it out. The wielded-weapon half of the same shim (medium) pinned a hauled spare of a weapon the colonist was already holding. Both are fixed in `CompositableLoadoutsCompat.KeepCount` via the Verse-free `CompositableLoadoutKeepPolicy`; the residual is the def-only match ignoring CL's per-entry `Filter` — one churn trip at the default, but a REPEATING loop with `unloadAllSurplus` ON, because `AdoptSurplusInventory(adoptAll: true)` re-tags the copy CL fetched back (never a strand either way; see the `MEDIUM` bullet).
- A KEEPER SHIM MUST KEEP EXACTLY WHAT THE KEEPER WOULD RE-FETCH — the durable lesson of #233, and the first time this family's failure ran the OTHER way. Every prior bug here (HD #20/#21/#16/#12) was HD keeping too LITTLE and fighting a mod's re-fetch. #233 is the first case where a shim kept something the keeper could NEVER re-fetch, and the cost was worse than a loop: a permanent strand of HD's OWN cargo, with no re-fetch to make it self-correct. When adding or widening any keeper shim, do not ask "does the mod want this def?" — ask "would the mod FETCH THIS UNIT BACK INTO INVENTORY if HD shipped it?", and check the mod's fetch path for exclusions (item class, equipment/worn satisfaction, filters) rather than reading its wanted-list alone.
- ITEM POLICY (high): no HD shim. With unloadAllSurplus=ON, HD adopts and ships ItemPolicy-stocked items (drug AND non-drug) -> ItemPolicy re-fetches via JobGiver_TakeItemForInventoryStock -> classic unload<->refetch loop. The only current mitigation is unloadAllSurplus defaulting OFF and the per-item KeepAll/KeepAtMost rules. Needs a general keep-count provider that reads ItemPolicy.GetItemPolicyEntry(pawn, def). (Since closed: `ItemPolicyCompat` ships and feeds `KeepCountOf`.)
- HD's drug-keep is an INLINE MIRROR, not a call (medium): IsManagedKeepItem reimplements ShouldKeepDrugInInventory's logic instead of calling it, so it silently misses every keeper mod's postfix on that method (ItemPolicy's drug postfix, Smart Medicine's DontDropStockedDrugs). Calling the vanilla method for drug items is the single highest-leverage general fix.
- OrganicInventoryShare build-from-inventory reads the WHOLE innerContainer by def (low/medium): if a kept item's def is also a needed construction material (e.g. a stocked component/medicine used in a modded recipe), HD could source the pawn's kept stock as build material. Currently unfiltered by IsManagedKeepItem; low probability but a clean general guard would close it.
- CE HoldTracker first-record-never-pruned quirk (low): re-notifying CECompat.NotifyHeld for the first-registered def inflates a stale CE HoldRecord (CE's own i>0 cleanup bug). HD already documents it; impact is CE-side, not a black hole.

### General-pattern recommendations
- KeepCountOf as a 'wants N of def' contract registry: HD already aggregates drug-policy takeToInventory + vanilla inventoryStock + CE loadout into one keep-count (InventorySurplus.KeepCountOf:290). Generalize it into a list of reflection-detected keep-count providers so any mod with a per-pawn 'keep N of def' policy (ItemPolicy today; future loadout/stock mods) plugs in as a one-line provider instead of a new SurplusOf branch. This is the durable, mod-agnostic shape — covers both drug and non-drug keepers and is count-aware (keeps N, unloads the excess) which the loop-fix history (1222a9f) proved is the correct behavior.
- Honor vanilla coordination hooks instead of reimplementing them: drug-keeper mods (Smart Medicine, ItemPolicy, and vanilla addiction) all coordinate through JobGiver_DropUnusedInventory.ShouldKeepDrugInInventory(pawn, drug) (decompile-verified public static). HD should CALL it for drug items rather than inline-mirroring its body, so it transparently inherits every mod's postfix. General rule: where vanilla exposes the exact decision method other mods patch, call it — don't duplicate its logic.
- Whole-stack keep + !hdSwept guard for unknown keepers (already in place): IsManagedKeepItem keeps the WHOLE stack of a recognized keep-item but ONLY when HD did not itself scoop it (the !hdSwept guard, InventorySurplus.cs:254), so an HD-tagged stack of the same def never becomes a black hole. Any new keeper provider must respect this same guard — keep the pawn's OWN kit, but never pin an HD-scooped stack.
- Default-off the destructive option: unloadAllSurplus defaults FALSE, so foreign keep-items are safe by default and only the opted-in player can hit a keeper loop — and even then the per-item KeepAll/KeepAtMost rules are the universal escape hatch. Keep this as the safety floor while general shims fill in.
- Reflection-only, fail-open, lazy-init compat bridges (the established CECompat/SimpleSidearmsCompat/SmartMedicineCompat/DbhCompat/YayoCombatCompat idiom): no hard assembly reference, AccessTools.TypeByName precondition, members null-guarded, warn-once on API drift, behaviour-identical when the mod is absent. Any ItemPolicy shim must follow the same idiom.

### Targeted patches (only where a general pattern can't cover it)
- ItemPolicy reflection shim (REQUIRED, targeted): create ItemPolicyCompat mirroring the other *Compat bridges. Reflect _ItemPolicy.ItemPolicyUtility.GetItemPolicyEntry(Pawn, ThingDef) -> int (the per-pawn keep count). It is genuinely mod-specific because ItemPolicy stores its policy in its OWN static Dictionary (ItemPolicyUtility.policies) rather than vanilla inventoryStock, and no general vanilla signal exposes a non-drug per-pawn keep count. Wire its count into the GENERAL KeepCountOf aggregate (InventorySurplus.cs:290) so it is honored exactly like vanilla inventoryStock — making the shim feed the general path, not add a new SurplusOf branch. This single shim closes the one high-risk gap and covers drug AND non-drug ItemPolicy stock.
- CE HoldTracker first-record inflation mitigation (OPTIONAL, targeted, low priority): if HD ever observes a stale CE HoldRecord, avoid re-notifying the same def via CECompat.NotifyHeld more than necessary; this works around CE's own i>0 cleanup bug (Utility_HoldTracker.cs:138). Documented follow-up only — not required, no black-hole risk.

### Adversarial verification
**Confirmed real:**
- ITEM POLICY UNPROTECTED (HIGH, REAL by construction). Verified: InventorySurplus.KeepCountOf (C:/Users/Arthur/Sync/Projects/RimWorld Mods/HaulersDream/Source/HaulersDream/InventorySurplus.cs:289-305) reads ONLY pawn.drugs.CurrentPolicy takeToInventory + pawn.inventoryStock.stockEntries + CECompat.LoadoutKeepCount. It never reads Item Policy's store (ItemPolicyUtility.policies, a static Dictionary<Pawn,ItemPolicy{Dictionary<ThingDef,int> data}>, ItemPolicy/decompiled/_ItemPolicy/ItemPolicyUtility.cs:9). There is no ItemPolicyCompat shim in the HD source tree. So for an Item Policy non-drug stocked def, keep=0 -> SurplusOf returns the whole stackCount -> AdoptSurplusInventory tags it (PawnUnloadChecker.cs:250) -> the unload pass ships it -> Item Policy re-fetches via JobGiver_TakeItemForInventoryStock (decompiled: when pawn.inventory.Count(def) < N it makes a JobDefOf.TakeInventory job, ItemPolicy/decompiled/_ItemPolicy/JobGiver_TakeItemForInventoryStock.cs:41-55). Classic unload<->refetch loop. CAVEAT on severity: it is gated behind unloadAllSurplus which DEFAULTS FALSE (HaulersDreamSettings.cs:36, confirmed) — with the toggle OFF, AdoptSurplusInventory only adopts defs with an explicit per-item RuleProducesSurplus rule (PawnUnloadChecker.cs:244), so out-of-the-box Item Policy stock is safe. The risk is real only for a user who turns on 'unload all surplus' (or sets a keep-at-most/always-unload rule for a stocked def). The per-item KeepAll rule is the current escape hatch.
- ITEM POLICY DRUG SUBSET ALSO UNPROTECTED (the 'medium' map item, REAL but it is the SAME gap as the high item, not separate). Item Policy protects its stocked DRUGS only via a postfix on JobGiver_DropUnusedInventory.ShouldKeepDrugInInventory (ItemPolicy/decompiled/_ItemPolicy/ShouldKeepDrugInInventoryPatch.cs:10-16, sets __result=true when GetItemPolicyEntry>0). HD does NOT call that vanilla method; its IsManagedKeepItem drug branch (InventorySurplus.cs:259-261) only checks AddictionUtility.IsAddicted/HasChemicalDependency. So an Item-Policy-stocked drug the pawn is NOT addicted to is missed -> adopted/shipped under unloadAllSurplus=ON, same loop. Verified vanilla ShouldKeepDrugInInventory IS public static (asm decompile RimWorld/JobGiver_DropUnusedInventory.cs:46), so the proposed 'call it for drug items to inherit every keeper postfix' fix is mechanically sound.
- SIMPLE SIDEARMS family (low/none, ALREADY HANDLED) — confirmed accurate. SimpleSidearmsCompat reflects CompSidearmMemory.rememberedWeapons (SimpleSidearmsCompat.cs:64-75); SurplusOf's weapon branch keeps the count-aware (def,stuff) keep and unloads hauled duplicates (InventorySurplus.cs:70-99); InventoryKeepCount subtracts the equipped primary exactly like SS's own FirstUnloadableThing postfix (SimpleSidearmsCompat.cs:198-210); IsRememberedSidearm guards both the self-heal (CompHauledToInventory.cs:132) and the scoop first-of-def pick (YieldRouter.InventoryStackOfDef:573). The MemoryApiOk false fallback keeps all carried weapons. No HD change needed.
- COMBAT EXTENDED family (low, ALREADY HANDLED) — confirmed. CECompat.IsCarriedAmmo keeps all CE AmmoDef stacks (CECompat.cs:214-221); LoadoutKeepCount feeds KeepCountOf with exact-def loadout counts (CECompat.cs:265-283 -> InventorySurplus.cs:303); MaxFitCount clamps pickups through CompInventory.CanFitInInventory (weight+bulk); NotifyHeld registers scooped goods with CE HoldTracker; OverloadGate/StatPart_Overload stand down under CE. All reflection-only, fail-open. The documented CE HoldTracker i>0 first-record quirk is a CE-side stale record, not an HD black hole — correctly classified low/optional.
- SMART MEDICINE family (low/none, ALREADY HANDLED) — confirmed against SmartMedicine source. StockUpUtility.StockingUpOn(this Pawn, ThingDef) exists (StockUp/StockUpUtility.cs:119, plus a (Pawn,Thing) overload at :117); HD resolves the (Pawn,ThingDef) overload (SmartMedicineCompat.cs:45) and IsManagedKeepItem keeps the whole stocked stack (InventorySurplus.cs:262). Medicine-grabbing/StockDown do not overlap HD's food-only EatFromInventory (FoodUtility.BestFoodInInventory, medicine is not ingestible food) — confirmed.
- DUB'S BAD HYGIENE + YAYO'S COMBAT 3 (low, ALREADY HANDLED) — confirmed. DbhCompat.IsKeptDrink detects the WaterExt DefModExtension (DbhCompat.cs:46-57); YayoCombatCompat.IsCarriedAmmo matches yy_ammo_category / 'yy_ammo' defName prefix (YayoCombatCompat.cs:44-54); both feed IsManagedKeepItem keep-whole-stack with the !hdSwept guard so HD-swept loose stacks stay unloadable. Reflection/def-based, fail-open. No change.
- PRIOR-BUG CLASS still latent for any new keeper — confirmed accurate framing. The unload<->refetch loop (HD #20), wrong-keep-count (#21), and caravan-return freeze (#16) bug classes are all reachable for Item Policy because it is the one keeper with no shim. The skippedThisJob anti-churn guard (JobDriver_UnloadHauledInventory) backstops the freeze but the real fix is the keep-count provider so the kept stock is never adopted.

**Refuted (not real / already handled):**
- 'HD's inline drug-keep silently misses Smart Medicine's DontDropStockedDrugs postfix' (map topRisks #2 and the Smart Medicine priorBug note) — REFUTED. HD does NOT rely on the inline addiction mirror for Smart Medicine: IsManagedKeepItem calls SmartMedicineCompat.IsStockedUp(pawn,def) DIRECTLY (InventorySurplus.cs:262), which reflects StockingUpOn. SM-stocked drugs (and non-drug medicine) are already kept whole-stack via that shim, independent of the addiction check. The only drug-keeper actually missed by the inline mirror is Item Policy (no shim) — so the 'misses EVERY keeper mod's postfix' framing is overstated; in practice it misses exactly one mod's drugs, which is the same Item Policy gap already captured as the high risk. The 'call vanilla ShouldKeepDrugInInventory' change is still a reasonable generalization/backstop, but it is NOT closing a live Smart Medicine hole — that hole does not exist.
- Implicit map assumption that the inline mirror is HD's SOLE drug protection and is therefore a standalone medium risk — REFUTED as a separate finding. It collapses into the Item Policy gap. The vanilla addiction/chem-dependency case the inline mirror covers is itself a vanilla-parity ADD (FirstUnloadableThing's count model omits addiction), not a re-implementation that drops postfixes for already-shimmed mods (SS/CE/SM/DBH/Yayo are all handled by their own direct shim calls, not by ShouldKeepDrugInInventory).

**Missed by the first pass (folded into the plan):**
- IMPLEMENTATION HAZARD in the recommended ItemPolicyCompat fix (MISSED): the proposed reflection target ItemPolicyUtility.GetItemPolicyEntry(Pawn,ThingDef) is NOT a pure read — it calls GetPawnPolicy(pawn) which does `if (!policies.ContainsKey(pawn)) policies[pawn] = new ItemPolicy();` (ItemPolicyUtility.cs:65-72, :84-92). So every HD KeepCountOf probe for a pawn with no policy would INSERT an empty ItemPolicy into Item Policy's static dictionary — growing it for every pawn HD ever scans (every colonist/mech, every surplus pass), polluting Item Policy's saved data (ItemPolicyUtility.ExposeData scribes the whole dict, :46-49). The shim must first check `ItemPolicyUtility.policies.ContainsKey(pawn)` (or reflect the policies dict directly) and only call GetItemPolicyEntry for pawns that already have a policy, else return 0 — otherwise the 'general keep-count provider' introduces a memory/save bloat side effect. The investigator's fix description ('reflect GetItemPolicyEntry(Pawn,ThingDef)->int') does not mention this.
- RE-FETCH WIRING NOT XML-VERIFIED (MISSED gap in the evidence chain): the whole Item Policy loop hinges on JobGiver_TakeItemForInventoryStock actually being inserted into the colonist think tree (a ThinkNode_JobGiver is inert unless an XML PatchOperation adds it). The Item Policy workspace copy is an ASSEMBLY-ONLY decompile (ItemPolicy/decompiled/ has no XML/ThinkTree patch), so the re-fetch is confirmed only by class shape (ThinkNode_JobGiver + PawnStateTracker interval + JobMaker.MakeJob(TakeInventory)), not by the actual think-tree insertion def. The loop is highly likely real (dead code otherwise) but this last link was inferred, not read. Worth confirming against the installed mod's Defs/ before treating the loop as certain.
- EatFromInventory / drug self-consumption vs Item Policy stocked DRUGS (MINOR, not in map): EatFromInventory allows drug ingestion from inventory for non-teetotalers (allowDrug at EatFromInventory.cs:80,137). If a pawn has Item-Policy-stocked recreational drugs in inventory, HD could feed the pawn one as 'food' during a downtime job, dropping the stocked count below N and prompting an Item Policy re-fetch. This is benign (it's the pawn legitimately using a drug, same as vanilla need-driven use) and not a loop, but it is an interaction the map's 'no overlap' EatFromInventory verdict did not explicitly consider for the drug case. Low/none severity.
- CE NON-LOADOUT ammo black-hole edge (MINOR, not flagged): CECompat.IsCarriedAmmo keeps ALL carried CE ammo regardless of loadout count (CECompat.cs:214), so a pawn that traded for or was manually handed a large NON-loadout ammo stack would have it pinned in inventory permanently even with unloadAllSurplus=ON (only the per-item 'always unload' rule frees it). This is a deliberate conservative choice (defer to CE) and the escape hatch exists, so it is acceptable — but it is a real, undocumented over-keep that the map's 'keeps the right amount' phrasing glosses over.

**Verdict:** The map's CENTRAL conclusion is correct and well-evidenced: Item Policy is the one genuinely unprotected inventory-keeper, and the loop is real (gated behind the default-OFF unloadAllSurplus toggle), while Simple Sidearms / Combat Extended / Smart Medicine / Dub's Bad Hygiene / Yayo's Combat 3 are all genuinely already handled by HD's existing reflection shims (verified line-by-line against each mod's source plus HD's current code). However the map OVERSTATES the 'inline drug-mirror' risk: HD already protects Smart Medicine (and SS/CE/DBH/Yayo) drugs via DIRECT shim calls, so the inline ShouldKeepDrugInInventory mirror does NOT silently drop SM's postfix — the only drug-keeper it misses is Item Policy, which is the same high gap already identified. The 'call vanilla ShouldKeepDrugInInventory' change remains a sound generalization but is not closing a live Smart Medicine hole. Two things the map MISSED matter for implementation: (1) the recommended GetItemPolicyEntry probe has a dictionary-mutating side effect (GetPawnPolicy auto-inserts an empty entry) that would bloat Item Policy's saved data unless the shim pre-checks policies.ContainsKey(pawn); and (2) the Item Policy re-fetch think-node wiring was inferred from the assembly, not read from XML (no defs in the workspace copy). Net: implement the Item Policy keep-count provider (the durable fix), guarding the side effect; the vanilla-drug-method call is optional polish, not an urgent fix.

---

## Family: `haul-redirectors`

The haul-redirector family (Common Sense, Keyz' Allow Utilities / Allow Tool "Haul Urgently", While You Are Nearby, While You're Up / Jobs of Opportunity) overlaps HD's two most sensitive seams: the WorkGiver_HaulGeneral.JobOnThing funnel (HD's BulkHaul postfix) and the Pawn_JobTracker.TryOpportunisticJob seam (HD's EnRoutePickup postfix). Confirmed against HD's code: HD composes safely with all four in the common cases BY CONSTRUCTION, because HD's redirect postfixes only ever UPGRADE a non-null vanilla HaulToCell job (never null it, never claim-then-not-produce), and HD's TryOpportunisticJob postfix only acts when vanilla returned null AND the job def allows the opportunistic prefix (so it can't double with vanilla's own opportunistic haul, and it self-excludes HaulersDream_BulkHaul which sets allowOpportunisticPrefix=false). HD already ships dedicated, decompile-grounded compat for two of the four: Patch_HaulUrgently_BulkHaul (Keyz/Allow Tool urgent giver) and CommonSenseCompat (cede the DoBill ingredient gather when CS's haul-all option makes it the gatherer — re-keyed for issue #243). The genuine residual risks are: (1) WYU AND HD both attaching to TryOpportunisticJob with DIFFERENT mechanisms (WYU transpiles the body to early-RETURN its own job when settings.Enabled; HD postfixes) — WYU's transpiled return pre-empts HD's postfix from ever seeing __result==null when WYU produces a job; running both en-route features at once is redundant, not crashing. (2) WYAN postfixes JobGiver_Work.TryIssueJobPackage and OVERWRITES __result with a nearby job — the SAME method HD postfixes (twice) for opportunistic unload; Harmony postfix ordering decides whether HD's unload-divert sees WYAN's substituted job or vanilla's. (3) The mark-for-unload CYCLE: Common Sense's CompUnloadChecker + JobGiver_UnloadYourInventory prefix vs HD's JobGiver_UnloadYourInventory postfix — both want to drive the same vanilla unload seam; CS's changelog explicitly records a "10 jobs per tick" fix and a pick-up-and-drop cycle bug here. HD largely sidesteps it (HD no longer sets UnloadEverything, and its postfix only substitutes when its own job can progress), but the gather cede — HD standing down exactly when CS's haul-all option makes it the gatherer — is the load-bearing stand-down. Overall: 0 high-confidence crash risks against current HD; the watch-items are double-redirect job-thrash and unload-cycle interplay, both already mitigated but worth a couple of GENERAL hardening guards rather than new mod-specific patches.

### Feature risk map

- `MEDIUM` **While You're Up / Jobs of Opportunity — Opportunistic haul-on-the-way (signature feature): when a pawn starts a job, prefix a haul of a loose item lying along the path to its destination.**
  - Mechanism: Harmony TRANSPILER on Verse.AI.Pawn_JobTracker.TryOpportunisticJob (OpportunityDetour.cs:22-48): inserts a settings.Enabled gate + a call to its own TryOpportunisticJob that RETURNS its own Job (HaulToCellStorageJob, or a PUAH HaulToInventory job) before vanilla's listerHaulables logic. Trip-ratio math in CanHaul (OpportunityDetour.cs:210-300). Called from Pawn_JobTracker.StartJob (decompile-verified, Assembly-CSharp StartJob ~line 338).
  - HD overlap: HD's Patch_Pawn_JobTracker_EnRoutePickup is a POSTFIX on the same TryOpportunisticJob (EnRoutePickup.cs:63-92). WYU's transpiler makes the method early-return its own job when settings.Enabled, so HD's postfix sees __result != null and bails (EnRoutePickup.cs:77). When WYU declines, HD's postfix runs. They don't both inject on one call; WYU wins when enabled.
  - Fix (general / no-op): GENERAL: HD's postfix already correctly no-ops when __result != null (any foreign opportunistic-job producer is respected). Treat WYU as redundant-with-HD, not conflicting: document that running both en-route features doubles nothing (WYU's hand-haul vs HD's inventory pickup are mutually exclusive per call) and recommend turning one off. Optionally add a one-time startup notice when WYU's CodeOptimist.JobsOfOpportunity type is present AND HD enRoutePickup is on. Do NOT add a mod-specific suppress — HD's __result!=null guard is the correct general composition.
- `LOW` **While You're Up / Jobs of Opportunity — Haul-before-carry for BILLS: when a pawn goes to DoBill, first haul a surplus of the first ingredient to a store cell closer to the bench.**
  - Mechanism: Inside the same transpiled TryOpportunisticJob, branch on job.def==DoBill && settings.HaulBeforeCarry_Bills (OpportunityDetour.cs:63-83) -> BeforeCarryDetour_Job (BeforeCarryDetour.cs:90-136) returns a HaulToCellStorageJob or PUAH job.
  - HD overlap: HD EnRoutePickup.ResolveJobTarget also special-cases DoBill (EnRoutePickup.cs:528-542) and HD's InventoryDoBill / BillPrepGather / CraftBatchPlanner reroute bill ingredients. Overlap is conceptual; mechanically WYU's transpiled early-return pre-empts HD's TryOpportunisticJob postfix.
  - Fix (general / no-op): GENERAL: same TryOpportunisticJob composition — WYU's transpiled return pre-empts HD's postfix, no double-injection. HD's bill-routing features act on different seams (ResourceDeliverJobFor / DoBill driver), not TryOpportunisticJob. No change; verify HD's bill features still build a valid job when WYU prefixed an ingredient haul (HD's gates re-validate live, so a stolen/moved ingredient just declines).
- `MEDIUM` **While You're Up / Jobs of Opportunity — Haul-before-carry for CONSTRUCTION SUPPLIES: when delivering resources to a blueprint/frame, first move the surplus stack to a store cell closer to the build site.**
  - Mechanism: Harmony TRANSPILER on WorkGiver_ConstructDeliverResources.ResourceDeliverJobFor (BeforeCarryDetour.cs:29-87): injects BeforeSupplyDetour_Job after FindAvailableNearbyResources, returning a haul job.
  - HD overlap: HD patches the SAME method: Patch_ResourceDeliverJobFor_Routing + InventoryConstructDelivery / ConstructionBatch. Two mods modifying WorkGiver_ConstructDeliverResources.ResourceDeliverJobFor (WYU transpiler, HD postfix/transpiler) is a real co-patch.
  - Fix (general / no-op): GENERAL: confirm HD's ResourceDeliverJobFor patch is a POSTFIX (or a transpiler that does not collide with WYU's inserted IL region). A postfix composes with WYU's transpiler cleanly (WYU's early-return short-circuits before HD's postfix; when WYU declines, HD's postfix runs on vanilla's job). If HD also transpiles this method, verify disjoint IL anchors and pin Harmony priority. Add an assertion that HD's patch tolerates a non-vanilla-shaped job (leaves it untouched). No mod-specific reference needed.
- `LOW` **While You're Up / Jobs of Opportunity — PUAH+ enhancements (only with Pick Up And Haul): route opportunistic/before-carry hauls into PUAH's bulk inventory giver; haul-to-equal-priority storage; smarter unload ordering (FirstUnloadableThing).**
  - Mechanism: Patches PUAH's WorkGiver_HaulToInventory.HasJobOnThing/JobOnThing/AllocateThingAt, StorageSettings.Priority getter postfix (PuahMiscPatches.cs:69-81), ListerHaulables.ThingsPotentiallyNeedingHauling postfix (PuahMiscPatches.cs:114-125), PUAH's UnloadYourHauledInventory.FirstUnloadableThing prefix. All gated on havePuah (Mod.cs:46-54).
  - HD overlap: HD is a PUAH REPLACEMENT (ships no PickUpAndHaul.* type), so WYU's havePuah is FALSE with only HD present and all these patches no-op. The non-havePuah-gated StorageSettings.Priority/ListerHaulables postfixes only mutate while reducedPriorityStore/thingsInReducedPriorityStore are set, which is a PUAH-only path — inert without PUAH.
  - Fix (general / no-op): GENERAL: none needed — WYU's PUAH features self-disable without PUAH; the ListerHaulables postfix only adds when its PUAH-only list is populated (never without PUAH). Document 'do not run HD + PUAH + WYU together' (already HD's stance). If a player co-installs PUAH (unsupported), those paths become PUAH's, independent of HD — no HD crash.
- `MEDIUM` **While You Are Nearby — Priority-boost / nearby-work substitution: when a colonist's chosen job is FAR away, re-scan its WorkGivers for a CLOSER job and substitute it, so pawns prefer nearby work.**
  - Mechanism: Harmony POSTFIX on JobGiver_Work.TryIssueJobPackage (JobGiver_Work_TryIssueJobPackagePatch.cs:16-171): if the result job's target is farther than longDistanceThreshold (default 32) and not emergency/prioritized, walks WorkGiversInOrderNormal from the chosen giver's index, runs GenClosest.ClosestThingReachable within maxSearchRadius (12), and if a closer thing is found OVERWRITES __result = new ThinkResult(scanner.JobOnThing(...)). excludedNearbyWorkTypes defaults EMPTY (HaulGeneral eligible), 5% vanilla-chance, priorityBoost 1.
  - HD overlap: DIRECT co-patch of JobGiver_Work.TryIssueJobPackage — HD postfixes the same method TWICE (Patch_JobGiver_Work_OpportunisticUnload, HarmonyPatches.cs:147-203; + Patch_OpportunisticLoadDeposit). (a) WYAN's substituted nearby haul re-enters HD's WorkGiver_HaulGeneral.JobOnThing postfix via scanner.JobOnThing(...,false), so it becomes an HD bulk sweep (intended/fine). (b) HD's OpportunisticUnload reads __result.Job to decide runOver/divert; postfix ordering is unspecified, so HD may decide against the pre- or post-substitution job.
  - Fix (general / no-op): GENERAL: HD's TryIssueJobPackage postfixes already recompute from the CURRENT __result each call and only ACT when the pawn carries tracked items AND the current job warrants it (HarmonyPatches.cs:157-191), so they're robust to a rewritten result. Pin HD's two TryIssueJobPackage postfixes to a LATER Harmony priority (run-last) so they observe the FINAL substituted job — a general 'react-after-replace' stance, not WYAN-specific. Confirm a WYAN-substituted HaulGeneral job composes with HD's BulkHaul postfix (it does). Regression check: a far HaulGeneral job substituted by any 'nearby work' mod still yields a valid HD bulk job and HD's unload-divert does not loop.
- `LOW` **While You Are Nearby — Exclusions / tuning: per-work-type opt-out for nearby substitution and long-distance trigger; vanilla-chance to keep vanilla behavior occasionally.**
  - Mechanism: SimpleSettings.cs: excludedNearbyWorkTypes / excludedLongDistanceWorkTypes (both default EMPTY), longDistanceThreshold=32, maxSearchRadius=12, priorityBoost=1, vanillaChancePercentage=5. ShouldTrigger gates (JobGiver_Work_TryIssueJobPackagePatch.cs:182-224) bail on NoJob/emergency/non-colony-worker/prioritized/mental-state/short-distance.
  - HD overlap: Determines HOW OFTEN WYAN rewrites the job HD's postfixes observe. Default-empty exclusions mean Hauling is in scope, so a far haul is the most common substitution target — exactly the jobs HD's BulkHaul postfix and OpportunisticUnload care about.
  - Fix (general / no-op): GENERAL / DOC: recommend adding 'Hauling' to WYAN's excludedNearbyWorkTypes ONLY if thrash is observed; not required (composition is sound). No HD code change; the run-last priority pin is the only code-side mitigation.
- `HIGH` **Common Sense — Advanced 'haul all ingredients' DoBill rework: replaces JobDriver_DoBill.MakeNewToils so a crafter carries ALL ingredients into inventory in one trip.**
  - Mechanism: Harmony PREFIX (return false) on JobDriver_DoBill.MakeNewToils (JobGiver_DoBIll_Rework.cs:18-419) installed whenever adv_haul_all_ings || adv_cleaning (both default true). Pawns pick ingredients into inventory then deposit at the bench; sets CompUnloadChecker.ShouldUnload on residue.
  - HD overlap: DIRECT functional overlap with HD's bill-ingredient routing (JobDriver_InventoryDoBill, JobDriver_BillPrepGather, JobDriver_BatchCraft, BillRouteGate, InventoryConstructDelivery). Both want to own how ingredients reach the bench. CS re-deposits HD's in-inventory ingredients onto the bench floor -> HD re-unloads & re-gathers -> infinite gather->bench->unload loop (the documented risk).
  - Fix (targeted patch): ALREADY MITIGATED (genuinely mod-specific): CommonSenseCompat (CommonSenseCompat.cs) reflection-reads CommonSense.Settings.adv_haul_all_ings and CEDES the gather — HD does not convert automatic bills to BillPrepGather when CS will actually gather (CommonSenseCedePolicy.CommonSenseGathersIngredients), fail-CLOSED if the field can't be read. KEEP, with the keying CORRECTED 2026-08-09 (issue #243): this entry originally read "ceding on EITHER toggle is the safe superset HD already uses", and that superset shipped a starve — adv_cleaning alone takes the driver but its else branch hands collecting straight back to vanilla's own CollectIngredientsToils (JobGiver_DoBIll_Rework.cs:426-432), so HD standing down there meant NOBODY gathered. The cede now keys on adv_haul_all_ings alone (the only option under which CS pockets ingredients, :380); driver ownership — either option, CommonSenseCedePolicy.CommonSenseOwnsDoBillDriver — survives only for the "Batch even with Common Sense active" opt-in, and the BatchCraft conversion itself runs under CS by default (its MakeNewToils override is a subclass CS never patches). targeted=true because the loop is unavoidable without reading CS's specific settings field.
- `MEDIUM` **Common Sense — 'Hauling over bills' (hauling_over_bills, default true): when a pawn is about to DoBill, opportunistically haul a nearby loose stackable item to storage first if it shortens the trip.**
  - Mechanism: Harmony PREFIX on Pawn_JobTracker.StartJob (OpportunisticTasks.cs:154-228; Hauling_Opportunity at :112-146): for a DoBill newJob, builds a HaulGeneral JobOnThing for a queued ingredient/nearby item and EnqueueFirst's it ahead of the bill. Gated on hauling_over_bills + jobQueue empty + allowOpportunisticPrefix.
  - HD overlap: Calls haulGeneral.Worker.JobOnThing(pawn, target.Thing) (OpportunisticTasks.cs:138), which runs HD's WorkGiver_HaulGeneral.JobOnThing POSTFIX — so the opportunistic haul CS injects becomes an HD bulk sweep. The enqueued job then hits StartJob -> TryOpportunisticJob (HD's EnRoutePickup postfix), but HD's job is HaulersDream_BulkHaul (allowOpportunisticPrefix=false) so no recursion.
  - Fix (general / no-op): GENERAL (covered by HD's existing invariants): CS EnqueueFirst's a HaulGeneral job that HD upgrades to a bulk job — composes because HD's postfix only UPGRADES a valid HaulToCell job and never nulls it. Note HD's CommonSenseCompat reads adv_haul_all_ings (the gather cede) and adv_cleaning (only for the driver fact behind the batch opt-in) but NOT hauling_over_bills; this path neither replaces the DoBill driver nor gathers into inventory (it only enqueues a prefix haul), so HD's gather cede does not disable it. HD's AlreadyHaulingIntoInventory guard (EnRoutePickup.cs:141) prevents stacking a second pickup on the in-flight bulk job. No change; add a verification note (CS-enqueued HaulGeneral -> HD bulk sweep -> unload -> then bill, benign).
- `NONE` **Common Sense — Clean-before-work / clean-after-tending / in-between cleaning: opportunistically inserts a Clean job before a work job or after delivering food / tending.**
  - Mechanism: Pawn_JobTracker.StartJob PREFIX (clean_before_work) and EndCurrentJob PREFIX (clean_after_tending) EnqueueFirst a Clean job (OpportunisticTasks.cs:154-278); the DoBill rework also appends a cleaning toil (JobGiver_DoBIll_Rework.cs:386-399).
  - HD overlap: None — Clean jobs are not haul/inventory jobs and don't touch HD's lister/comp. HD's TryIssueJobPackage / TryOpportunisticJob postfixes ignore non-haul jobs; CompHauledToInventory is unaffected.
  - Fix (general / no-op): No action. Cleaning is orthogonal to hauling/inventory; HD's seams early-out on non-haul/non-yield jobs.
- `HIGH` **Common Sense — Mark-for-unload + 'unload marked items': inventory items (esp. via the DoBill rework) get CompUnloadChecker.ShouldUnload; a dedicated UnloadMarkedItems job drops only the marked items, replacing vanilla UnloadYourInventory.**
  - Mechanism: CompUnloadChecker (UnloadChecker.cs); JobGiver_UnloadYourInventory.TryGiveJob PREFIX returns a CommonSenseJobDefOf.UnloadMarkedItems job when any marked item exists (UnloadChecker.cs:114-127); ThingOwner<Thing>.TryAdd postfix marks WasInInventory; PutBackToBackpack transpiles CleanupCurrentJob's TryDropCarriedThing -> SneakyMoveToInventory (puts a carried marked item back into inventory on interrupt rather than dropping).
  - HD overlap: HD POSTFIXES JobGiver_UnloadYourInventory.TryGiveJob (Patch_JobGiver_UnloadYourInventory, HarmonyPatches.cs:87-131) to substitute HD's storage-aware unload. CS PREFIXES the same method (return false). Prefix runs first: if CS marked items, HD's postfix sees __result = CS's UnloadMarkedItems job (non-null) and, if HD has tagged surplus stock, may try to substitute HD's unload OVER CS's. CS's TryAdd postfix also marks EVERYTHING added to inventory as WasInInventory.
  - Fix (general / no-op): GENERAL hardening (the canonical cycle class — CS's changelog records a '10 jobs per tick' fix and a pick-up-then-drop cycle here): HD's postfix already self-limits to substitute only when a tagged stack is in inventory, reservable, and genuinely SURPLUS (HarmonyPatches.cs:101-121) — the anyUnloadable gate that stops the flag-stuck re-issue loop. STRENGTHEN: HD's postfix should DEFER to a foreign custom unload job — substitute ONLY when __result.def == JobDefOf.UnloadInventory (the vanilla job), never when another mod already produced a custom unload job (CS UnloadMarkedItems, PUAH UnloadYourHauledInventory). Because HD cedes the gather exactly when CS's haul-all option is on (#243 narrowed the cede to that case), HD doesn't put bill ingredients into inventory in the one configuration where CS's rework marks gather residue for unload; under cleaning-only, HD's gather runs but CS's driver is not re-depositing (its else branch is vanilla's own collect, and vanilla's toils set no ShouldUnload marks), so the gather/unload cross-contamination stays avoided. This is a GENERAL 'don't override a foreign custom unload job' rule, not CS-specific.
- `LOW` **Common Sense — Simple Sidearms compat: forgets a sidearm when its weapon is unloaded (Sidearms_Utility.ForgetSidearm in the unload driver).**
  - Mechanism: UnloadChecker.cs:270-278 — the UnloadMarkedItems driver calls Sidearms_Utility.ForgetSidearm; SideArmsPatch.cs guards SS interactions.
  - HD overlap: HD has its own SimpleSidearmsCompat.IsRememberedSidearm used to AVOID tagging/unloading a pawn's remembered sidearm (YieldRouter.InventoryStackOfDef:558-582; CompHauledToInventory.GetHashSet self-heal:132). Both mods independently respect SS; no shared state.
  - Fix (general / no-op): GENERAL: HD already avoids unloading a remembered sidearm by construction (it never tags a same-def stack that IsRememberedSidearm), so CS's ForgetSidearm never sees an HD-tagged sidearm to forget. No change; well-handled class.
- `LOW` **Keyz' Allow Utilities (and Allow Tool) — Haul Urgently: designate items for high-priority hauling; a dedicated WorkGiver_HaulUrgently hauls them (one stack per trip, or via PUAH if installed).**
  - Mechanism: WorkGiver_HaulUrgently : WorkGiver_Scanner whose JobOnThing returns a single-stack HaulAIUtility.HaulToStorageJob via a JobOnThingDelegate (WorkGiver_HaulUrgently.cs:42-47). Designation KAU_HaulUrgentlyDesignation; Toils_Haul postfixes remove the designation on deposit (Toils_Haul_Patch.cs). NOT routed through WorkGiver_HaulGeneral.JobOnThing.
  - HD overlap: HD's BulkHaul postfix patches WorkGiver_HaulGeneral.JobOnThing, which the urgent giver bypasses — so without compat, urgent hauls would NOT bulk-sweep.
  - Fix (targeted patch): ALREADY HANDLED (correctly mod-specific): Patch_HaulUrgently_BulkHaul postfixes KeyzAllowUtilities.WorkGiver_HaulUrgently.JobOnThing AND AllowTool.WorkGiver_HaulUrgently.JobOnThing (resolved by string, Prepare()-gated, applied by HD's resilient patcher) and runs the SAME BulkHaul.TryBuildBulkJob upgrade. Genuinely targeted because the urgent giver is a distinct type off HD's funnel. KEEP. The upgrade inherits all HD gating (declines non-HaulToCell, e.g. the genebank CarryGenepackToContainer job, automatically). targeted=true.
- `NONE` **Keyz' Allow Utilities (and Allow Tool) — 'Do Not Haul' designation: marks items that should never be auto-hauled.**
  - Mechanism: KAU_NoHaulDesignation + a POSTFIX on HaulAIUtility.PawnCanAutomaticallyHaulFast that returns false for designated things (HaulAIUtility_Patches.cs:26-58). (Allow Tool has an equivalent.)
  - HD overlap: HD's BulkHaul pool/sweep, YieldRouter sweep, and EnRoutePickup ALL gate candidates through HaulAIUtility.PawnCanAutomaticallyHaulFast (BulkHaul.cs:671, YieldRouter.cs:249, EnRoutePickup.cs:289). So a 'Do Not Haul' item is automatically excluded from every HD sweep BY CONSTRUCTION.
  - Fix (general / no-op): No action — HD honors KAU_NoHaulDesignation automatically because every HD intake candidate passes through PawnCanAutomaticallyHaulFast, which Keyz postfixes. The ideal general composition (documented in HD COMPATIBILITY.md).
- `NONE` **Keyz' Allow Utilities — Designation/selection tools: harvest-all, cut-grown, strip-mine, select-similar, finish-off, claim-all-doors, select rotting corpses, etc.**
  - Mechanism: Designators + WorkGiver_FinishOff/JobDriver_FinishOff + Plant/Zone/Selector/CompForbiddable Harmony patches. Designator_HaulUrgently keeps items in ListerHaulables after a vanilla Cancel (CHANGELOG v1.2.4 / issue #24).
  - HD overlap: None on hauling/inventory seams. Harvest/mining/strip produce yields via GenPlace -> HD's YieldRouter may scoop them (intended). Keeping urgent items in ListerHaulables makes them VISIBLE to HD's sweep pool (desired).
  - Fix (general / no-op): No action. These produce work/yields HD's YieldRouter handles via its GenPlace.TryPlaceThing hook; no special handling needed.
- `LOW` **Keyz' Allow Utilities — PUAH compat sub-mod: rebinds WorkGiver_HaulUrgently.JobOnThingDelegate to PUAH's WorkGiver_HaulToInventory when PUAH is installed.**
  - Mechanism: PUAHMod.cs:22-29 sets JobOnThingDelegate to PUAH's bulk-into-inventory giver. Hard-references PickUpAndHaul (only loads if PUAH present).
  - HD overlap: HD ships no PickUpAndHaul.* type, so this rebind never targets HD; HD is a PUAH replacement. If PUAH IS co-installed (unsupported), urgent-haul becomes a PUAH inventory job (not HaulToCell) and HD's TryBuildBulkJob declines it.
  - Fix (general / no-op): GENERAL: HD's urgent-haul postfix declines a non-HaulToCell job automatically (TryBuildBulkJob requires JobDefOf.HaulToCell), so a PUAH-rebound urgent job is left alone. The historical 'PUAH + Haul Urgently acting funny' is PUAH-driven, not HD's. No change; document the unsupported PUAH co-install (already in HD COMPATIBILITY.md).

### Prior bugs learned from (their bug-fix history)

- ⚠️ applies to HD — **'allegedly fixed a problem that caused 10 jobs per tick issue' (Common Sense change.log 1.3.4).** _(CommonSense change.log)_ — The canonical risk class for this family: a job-giver/StartJob patch that injects a job which immediately gets superseded/re-issued, tripping vanilla's StartJob 'started 10 jobs in one tick' guard (Assembly-CSharp Pawn_JobTracker.StartJob ~line 267). HD injects on the same seams (JobGiver_Work.TryIssueJobPackage, WorkGiver_HaulGeneral.JobOnThing, TryOpportunisticJob, JobGiver_UnloadYourInventory). HD mitigates by (a) only UPGRADING a non-null vanilla job, (b) gating its unload substitution on anyUnloadable so a no-progress unload job isn't re-issued forever (HarmonyPatches.cs:101-121), (c) en-route's AlreadyHaulingIntoInventory + allowOpportunisticPrefix no-recursion guards. Residual exposure: HD's unload-substitution overriding a foreign custom unload job (CS UnloadMarkedItems) in a way that leaves a flag stuck — addressed by the 'defer to foreign custom unload job' general guard.
- ⚠️ applies to HD — **'made so if an option to mark items for unloading is switched off pawns won't pick up and drop items that are marked for unloading in a cycle anymore. It's a mod order issue with a mod that I don't even know a name of' (Common Sense change.log 1.3.9).** _(CommonSense change.log)_ — DIRECTLY relevant: CS itself hit a pick-up-then-drop CYCLE caused by another mod re-hauling items CS marked for unload (CompUnloadChecker.ShouldUnload). HD is exactly the kind of mod that could re-scoop a CS-marked item: YieldRouter sweep / BulkHaul pool / EnRoutePickup add loose items to inventory. HD's guards: it only sweeps items with a BETTER storage destination (HasScoopDestination / TryFindBestBetterStorageFor) and not IsInValidStorage, and respects PawnCanAutomaticallyHaulFast. Crucially HD only sweeps GROUND items while CS marks INVENTORY items, and CS's PutBackToBackpack puts a carried marked item BACK to inventory (not the ground), so the cycle is largely avoided. Worth a verification pass that an HD-swept item CS later marks doesn't ping-pong.
- n/a to HD — **'fixed switching off cleaning gizmo doing nothing' / 'safe wander doesn't let walk in vacuum' / 'fixed pawns using wrong primary material to make an item' (CS 1.3.9/1.3.5).** _(CommonSense change.log)_ — Internal CS correctness fixes (cleaning gizmo, vacuum wander, recipe primary-material ORDER in the DoBill rework). Not cross-mod hauling/inventory interactions with HD. The 'wrong primary material' fix lives in CS's own DoBill driver, which HD never patches (HD converts at the work-giver level, and its batch driver overrides MakeNewToils in its own subclass) — so HD does not reintroduce it.
- ⚠️ applies to HD — **'don't haul extra things closer when bleeding' (While You're Up commit 23c7b43).** _(WhileYoureUp git log)_ — WYU added a bleeding gate to its opportunistic/before-carry hauls (AmBleeding, Mod.cs:109; OpportunityDetour.cs:91). HD ALREADY has the parity feature: YieldRouter.FitToStartHaul (C1 bleeding gate, default ON) gates scoop/sweep/bulk/en-route INTAKE on bleed rate (YieldRouter.cs:440-447; applied in BulkHaul.cs:152, EnRoutePickup.cs:136, YieldRouter.IsCandidate). HD learned this independently; appliesToHD true = HD correctly implements the same guard, no reintroduction risk.
- ⚠️ applies to HD — **'fixed repeating hauls on the same tick' (WYU 8a58943); '5000d42 fix possible NREs at load from in-progress inventory hauls'; '85b9573 prevent chains of: opportunity -> unload -> opportunity'.** _(WhileYoureUp git log)_ — WYU hit (a) duplicate hauls in one tick (CatchLoop/lastFrameCount + AlreadyHauling, BaseDetour.cs:24,46-51), (b) NREs from in-progress inventory hauls at load, (c) an opportunity->unload->opportunity infinite chain (the :RepeatOpportunity 5-tick cooldown, OpportunityDetour.cs:97). HD's analogues: (a) per-tick plan memo + AlreadyHaulingIntoInventory (BulkHaul planCache; EnRoutePickup.cs:141); (b) ClearPlanCache on FinalizeInit + transient (non-scribed) in-flight state in CompHauledToInventory; (c) EnRouteMutexPolicy.MustStandDown stands en-route down at the unload divert point (EnRoutePickup.cs:159,466-503) — exactly the chain WYU guards. HD has independently implemented all three guard classes; verification ask: HD's en-route<->unload mutex truly prevents the chain when BOTH HD features are on.
- ⚠️ applies to HD — **'fixed Common Sense compatibility' (WYU 6aba765) and 'define filter in local namespace to avoid InvalidCastException from other library mods' (cef7fdb).** _(WhileYoureUp git log)_ — WYU itself needed a CS compat fix (it reflection-reads CommonSense.Settings.hauling_over_bills, Mod.cs:66-70) AND a fix for an InvalidCastException from a shared library filter type across mods. Lessons for HD: (1) HD's CommonSenseCompat reflection-reads CS toggles — ensure it reads the RIGHT toggle for the behavior it cedes. Issue #243 (2026-08-09) sharpened this lesson on HD itself: HD read the right FIELDS but keyed the cede on the wrong FACT (driver ownership, adv_cleaning||adv_haul_all_ings) — CS's cleaning option owns the driver without gathering, so the cede starved the gather; it now keys on adv_haul_all_ings alone (= CS actually gathers). WYU keys its bill detour on hauling_over_bills = a different CS feature again, so neither mod over-cedes against the other. (2) The shared-library-InvalidCastException lesson: keep any shared type local — HD currently has no shared library types, so low risk.
- ⚠️ applies to HD — **Keyz 'keep items in ListerHaulables after vanilla Cancel' (#24, v1.2.4); 'do not haul' / 'allow canceling' (v1.2.2); 'trying to mark already marked pawn on finish off' (v1.3.2).** _(KeyzAllowUtils CHANGELOG)_ — Keyz #24 keeps urgently-designated items in ListerHaulables (and explicitly does NOT add vanilla DesignationDefOf.Haul). HD reads map.listerHaulables.ThingsPotentiallyNeedingHauling everywhere — so a Keyz urgent item correctly appears in HD's sweep pool and HD's urgent-haul postfix upgrades it. appliesToHD true = the fix makes Keyz items VISIBLE to HD's lister-based sweeps (desired). The 'mark already marked pawn' bug is internal to Keyz's finish-off designation, not an HD interaction.
- n/a to HD — **While You Are Nearby: no public changelog (decompiled-only, no About/changelog).** _(WhileYouAreNearby decompiled)_ — No source changelog. The decompiled JobGiver_Work postfix is straightforward (re-scan + overwrite __result). The implicit lesson HD must respect: a postfix that OVERWRITES __result on JobGiver_Work.TryIssueJobPackage will fight any other postfix on the same method unless priorities are pinned. HD should run its TryIssueJobPackage postfixes LAST so it observes the final job.

### Top risks
- Common Sense 'haul all ingredients' DoBill driver replacement (adv_haul_all_ings/adv_cleaning, default ON) vs HD's bill-ingredient routing -> infinite gather->bench->unload loop. ALREADY mitigated by CommonSenseCompat ceding the ingredient gather exactly when CS will gather (adv_haul_all_ings; fail-closed) — narrowed 2026-08-09 from "cede whenever CS owns the driver", which issue #243 proved starves the gather under cleaning-only (CS then collects via vanilla's own toils and HD's stand-down left nobody gathering). Highest-severity overlap; keep the cede and verify it still triggers on current CS field names.
- Mark-for-unload cycle: CS's CompUnloadChecker + JobGiver_UnloadYourInventory PREFIX (UnloadMarkedItems) vs HD's JobGiver_UnloadYourInventory POSTFIX + HD's own tagged-unload. Risk of HD overriding CS's custom unload job and/or a pick-up-then-drop cycle (CS changelog records both the '10 jobs/tick' and the unmarked-item cycle bugs). Mitigation: HD should only substitute when __result is the VANILLA UnloadInventory job, never a foreign custom unload job.
- While You Are Nearby OVERWRITES __result on JobGiver_Work.TryIssueJobPackage — the exact method HD postfixes twice for opportunistic unload/load. Postfix ordering is unspecified across mods; HD may make its run-over/divert decision against the wrong (pre-substitution) job. Mitigation: pin HD's two TryIssueJobPackage postfixes to run LAST so they observe the final substituted job.
- WYU and HD both attach to Pawn_JobTracker.TryOpportunisticJob with DIFFERENT mechanisms (WYU transpiles to early-return; HD postfixes). They don't double-inject (WYU's return pre-empts HD's __result==null path), but running both en-route features is redundant and can surprise the player with two competing 'haul on the way' behaviors. Mitigation: doc + optional startup notice; no forced suppress.
- WYU and HD both modify WorkGiver_ConstructDeliverResources.ResourceDeliverJobFor (WYU transpiler vs HD's Patch_ResourceDeliverJobFor_Routing). Confirm HD's patch is a postfix (or non-colliding transpiler) and tolerates a WYU-substituted job; pin priority for determinism.

### General-pattern recommendations
- HD's haul-redirect postfixes already follow the load-bearing invariant: ONLY UPGRADE a non-null vanilla HaulToCell job (never null it, never claim-then-not-produce). Confirmed in BulkHaul postfix (Patch_WorkGiver_HaulGeneral_BulkHaul) and the urgent-haul postfix. This single rule makes HD compose with ANY foreign mod that produces or re-selects a haul job. Keep it as an explicit, tested contract.
- TryOpportunisticJob postfix self-discipline: HD acts only when __result==null AND job.def.allowOpportunisticPrefix (and its own emitted job sets allowOpportunisticPrefix=false). This makes HD respect ANY foreign opportunistic-job producer (WYU, vanilla) and prevents recursion. General rule: an opportunistic-prefix postfix must never override a non-null result and must self-exclude its own job def.
- Honor foreign suppression seams by routing every intake candidate through HaulAIUtility.PawnCanAutomaticallyHaulFast — this is why Keyz/Allow Tool 'Do Not Haul' is honored for free. General rule: gate ALL HD sweep/scoop/en-route candidates through the vanilla legality funnel that other mods postfix, so any 'don't haul this' mod is respected without a reference.
- Don't override a FOREIGN custom unload job. HD's JobGiver_UnloadYourInventory postfix should substitute ONLY when __result is the vanilla JobDefOf.UnloadInventory job — never when another mod (Common Sense UnloadMarkedItems, PUAH UnloadYourHauledInventory) already produced a custom unload result. A general guard that protects against the family's documented unload-cycle bugs without naming any mod.
- Run-last on shared work-selection seams. When multiple mods postfix JobGiver_Work.TryIssueJobPackage and may rewrite __result (While You Are Nearby does), HD's reactive postfixes (opportunistic unload/load) should pin a LATER Harmony priority so they observe the FINAL job. General rule: a postfix that REACTS to the chosen job should run after postfixes that REPLACE the chosen job.
- Cede an owned subsystem to a mod that will actually DO that subsystem's work, detected by reflection on its live toggles, fail-CLOSED — and key the cede on the WORK, not on driver ownership. RETRACTED as originally written (2026-08-09, issue #243): this rule used to say "cede when CS owns JobDriver_DoBill.MakeNewToils" and generalize to "if a foreign mod replaces the driver I would convert, do not convert" — but CS's cleaning option replaces the driver while delegating the collecting straight back to vanilla, so ceding on the replacement left nobody gathering. HD's CommonSenseCompat now cedes exactly when CS's haul-all option makes it the gatherer (CommonSenseCedePolicy.CommonSenseGathersIngredients). The template question for any future driver-replacing mod is "will it do the work I would stop doing?", which generally requires reading that mod's own settings — a driver rewrite alone proves ownership, not work.
- Independently-evolved guards match the family's hard-won fixes: per-tick plan memo + AlreadyHauling guard (vs WYU 'repeating hauls in one tick'), en-route<->unload mutex (vs WYU 'opportunity->unload->opportunity chain'), bleeding intake gate (vs WYU 'don't haul extra when bleeding'), transient in-flight state cleared on load (vs WYU 'NREs at load from in-progress hauls'). Keep these; they are the proven mitigations for this family's bug classes.

### Targeted patches (only where a general pattern can't cover it)
- Patch_HaulUrgently_BulkHaul (existing, KEEP): postfix on KeyzAllowUtilities.WorkGiver_HaulUrgently.JobOnThing AND AllowTool.WorkGiver_HaulUrgently.JobOnThing, resolved by string with a Prepare()-gate, running BulkHaul.TryBuildBulkJob. Genuinely required because the urgent giver bypasses HD's WorkGiver_HaulGeneral.JobOnThing funnel. Inherits all HD gating and declines non-HaulToCell jobs (e.g. CarryGenepackToContainer) automatically.
- CommonSenseCompat (existing, KEEP; re-keyed 2026-08-09 for issue #243): reflection-read CommonSense.Settings.adv_haul_all_ings and cede HD's DoBill ingredient-gather conversions exactly when CS will gather (CommonSenseCedePolicy.CommonSenseGathersIngredients), fail-closed if the field doesn't resolve; adv_cleaning is still read, but only to feed the driver-ownership fact (CommonSenseOwnsDoBillDriver) behind the "Batch even with Common Sense active" opt-in. Genuinely required because CS's haul-all toils re-deposit HD's in-inventory ingredients — without the cede that is an infinite loop. Ceding more broadly, on the driver replacement (either option), was the original shape of this patch and is the #243 starve: cleaning-only replaces the driver but collects via vanilla, so HD stood down and nobody gathered. Verify the field names against current CS 1.6 (adv_cleaning/adv_haul_all_ings still present per Settings.cs:18-19).

### Adversarial verification
**Confirmed real:**
- WYAN<->HD JobGiver_Work.TryIssueJobPackage postfix-ordering is genuinely UNPINNED. VERIFIED: WYAN's patch (JobGiver_Work_TryIssueJobPackagePatch.cs:16 `[HarmonyPostfix]`, no `[HarmonyPriority]`, applied via plain `PatchAll()` in ModMain.cs) runs at Priority.Normal(400) and OVERWRITES __result with a nearby job (line 165). HD's TWO postfixes on the SAME method (Patch_JobGiver_Work_OpportunisticUnload @ HarmonyPatches.cs:147-203 and Patch_OpportunisticLoadDeposit @ Patch_OpportunisticLoadDeposit.cs:45-48) ALSO carry NO `[HarmonyPriority]` (grep-confirmed: the only HarmonyPriority uses in HD are on the two TryTakeOrderedJob prefixes, not these). All three at equal priority 400 => cross-mod order is unspecified, so HD's react-to-the-chosen-job postfixes may decide against the pre-substitution (vanilla, far) job instead of WYAN's substituted near job. The investigator's 'pin HD's two TryIssueJobPackage postfixes to Priority.Last (run-last)' mitigation is a real, currently-absent hardening. SEVERITY is LOWER than 'medium', though: HD's unload-divert only diverts a carrying pawn to shed its load and re-picks work after (so a stale far-vs-near read yields a suboptimal divert decision, never a loop or crash) — but the fix is cheap and correct.
- HD does NOT defer to a FOREIGN custom unload job — the 'don't override CS UnloadMarkedItems' gap is REAL. VERIFIED against source: CS's JobGiver_UnloadYourInventory.TryGiveJob is a PREFIX returning false with __result=`new Job(CommonSenseJobDefOf.UnloadMarkedItems)` whenever a CS-marked item exists (UnloadChecker.cs:129-142). Harmony runs prefixes before postfixes, so HD's Patch_JobGiver_UnloadYourInventory POSTFIX (HarmonyPatches.cs:87-131) sees __result = CS's UnloadMarkedItems job (non-null, def != JobDefOf.UnloadInventory). HD's postfix has NO `__result.def` guard (grep-confirmed: it only checks __result==null, pawn==null, CurJobDef==HD_UnloadInventory, then anyUnloadable) — so if the pawn ALSO carries an HD-tagged in-inventory surplus stack, HD SUBSTITUTES its own HaulersDream_UnloadInventory job OVER CS's UnloadMarkedItems. HD's driver only services inventory innerContainer items, so a CS-marked EQUIPMENT/APPAREL item (CS's driver handles unequip; HD's does not) could be left unserviced that scan. The investigator's general guard ('substitute ONLY when __result.def == JobDefOf.UnloadInventory') is real and currently missing. SEVERITY: low — narrow correctness (a CS-marked item's unload is delayed/skipped that scan), NOT the '10 jobs/tick' cycle (HD's anyUnloadable gate prevents the no-progress re-issue loop, and the two mods' tagged sets are normally disjoint since CS's TryAdd postfix sets only WasInInventory, never ShouldUnload, on HD-scooped items).
- While You're Up and While You Are Nearby are entirely UNDOCUMENTED in HD's COMPATIBILITY.md. VERIFIED: COMPATIBILITY.md documents Common Sense (line 34) and Haul Urgently/Allow Tool/Keyz (line 45) in detail, but a full read shows NO mention of 'While You're Up', 'Jobs of Opportunity', or 'While You Are Nearby' anywhere. The investigator's repeated 'GENERAL / DOC' recommendations (document WYU-as-redundant-with-HD en-route, recommend one be turned off; note WYAN substitution) are valid — these two mods are not covered.

**Refuted (not real / already handled):**
- 'CS haul-all-ingredients DoBill loop' — the cede exists and the loop stays closed, but this entry's original verdict ("`ShouldCedeDoBillFlow` returns `advCleaning || advHaulAll` — byte-matching CS's own prefix condition — KEEP, no change") was WRONG and is retracted (2026-08-09, issue #243, field-confirmed by Lensrub after v1.22.0: with cleaning on and haul-all off, nothing gathered at all). The decompiled facts it rested on are correct and stand: CS's Prefix returns false — replacing the driver — exactly when `adv_cleaning || adv_haul_all_ings` (JobGiver_DoBIll_Rework.cs:452-460, the shipped-DLL decompile; both default true in Settings.cs:18-19); its gather chain runs only under `adv_haul_all_ings` (:380); and the cleaning-only else branch yields RimWorld's own `JobDriver_DoBill.CollectIngredientsToils(...)`, argument for argument vanilla's call (:426-432). What was wrong is the conclusion drawn from them: byte-matching CS's prefix condition verifies "does CS own the driver", but the cede's load-bearing question was "will anyone gather" — under cleaning-only CS held the driver, collected via vanilla, HD stood down, and NOBODY gathered. `ShouldCedeDoBillFlow` no longer exists. Current shape: `CommonSenseCedePolicy.CommonSenseGathersIngredients` (adv_haul_all_ings only) cedes the two gather paths (Patch_WorkGiver_DoBill_InventoryRoute.cs:71 and the carried-stock injection at SharedBillPatches.cs:71) and drives the per-bench "Gather ingredients" notice from the same fact, while `CommonSenseOwnsDoBillDriver` (either option) survives solely for the "Batch even with Common Sense active" opt-in (Patch_WorkGiver_DoBill_BatchRoute.cs:55). Fail-CLOSED on an unreadable field in both rules. HD's gather composes with the cleaning-only driver because HD converts at the work-giver level into a job def CS does not patch, and vanilla's `CollectIngredientsToils` takes inventory-held ingredients natively (`canTakeFromInventory: true`). BillGatherTruthTableTests pins all 48 rows.
- 'Haul Urgently bypasses HD's bulk sweep' is NOT an open risk — Patch_HaulUrgently_BulkHaul is verified correct. Keyz WorkGiver_HaulUrgently.JobOnThing(pawn,t,forced) returns `TryMakeGenebankJob(...) ?? JobOnThingDelegate(...)` where the default delegate is HaulAIUtility.HaulToStorageJob => a HaulToCell job (WorkGiver_HaulUrgently.cs:13,42-47). HD's postfix targets the exact 3-arg JobOnThing on both `KeyzAllowUtilities.WorkGiver_HaulUrgently` AND `AllowTool.WorkGiver_HaulUrgently` by string (Patch_HaulUrgently_BulkHaul.cs:42-71, Prepare()-gated) and runs BulkHaul.TryBuildBulkJob. The genebank CarryGenepackToContainer job is NOT HaulToCell, so TryBuildBulkJob declines it internally (BulkHaul.cs:300-301). Verified the type-name string matches the actual namespace (`KeyzAllowUtilities`).
- 'Keyz Do Not Haul not honored' is REFUTED (honored by construction). Keyz postfixes HaulAIUtility.PawnCanAutomaticallyHaulFast to return false for KAU_NoHaulDesignation things (HaulAIUtility_Patches.cs:26-57). HD routes EVERY intake candidate through PawnCanAutomaticallyHaulFast: BulkHaul.cs:671, EnRoutePickup.cs:289, plus the bulk pool gate. So a Do-Not-Haul item is excluded from every HD sweep with zero HD code. Investigator's 'risk:none' is correct.
- 'WYU + HD double-inject on TryOpportunisticJob' is REFUTED. WYU's transpiler (OpportunityDetour.cs:27-48) inserts a `Ret` of its own job/null right before the vanilla listerHaulables access — which is AFTER vanilla's allowOpportunisticPrefix check (WYU's own comment at line 52-53 confirms this). When WYU is enabled it returns first; HD's POSTFIX (EnRoutePickup.cs:66-92) then sees that result and bails on `__result != null` (line 77). When WYU declines (null) HD runs. No double-injection. HD also mirrors the allowOpportunisticPrefix gate (line 84). They are redundant-not-conflicting, exactly as the investigator said — no forced suppress needed. (Note: WYU's transpiler Ret means vanilla's own opportunistic search never runs while WYU is enabled, which is irrelevant to HD.)
- 'WYU + HD collide on WorkGiver_ConstructDeliverResources.ResourceDeliverJobFor (transpiler vs HD)' is REFUTED as a live risk. WYU's BeforeSupplyDetour is a TRANSPILER that inserts a `Ret` mid-method right after FindAvailableNearbyResources (BeforeCarryDetour.cs:29-72), returning its own HaulToCell relocation job. HD's Patch_ResourceDeliverJobFor_Routing is a POSTFIX with [HarmonyPriority(Priority.Last)] (Patch_ResourceDeliverJobFor_Routing.cs:30-31) that only acts when `job.def == JobDefOf.HaulToContainer` (line 46). When WYU returns its HaulToCell relocation, HD's def-mismatch guard bails (HaulToCell != HaulToContainer); when WYU declines, vanilla builds the HaulToContainer and HD can route it. Composes cleanly; HD is already a postfix at Priority.Last as the investigator asked.
- 'WYU PUAH+ features conflict with HD' is REFUTED. WYU's havePuah requires `PickUpAndHaul.*` types to resolve (Mod.cs:21-46). HD ships no PickUpAndHaul.* type (its assembly is `HaulersDream`), so havePuah is false with only HD present and every PUAH-gated WYU path (PuahMiscPatches, ListerHaulables/StorageSettings.Priority postfixes, UsePickUpAndHaulPlus branches) self-disables. Investigator's 'risk:low, self-disables' confirmed.
- 'CS hauling_over_bills enqueues a haul HD breaks' is REFUTED. CS's Hauling_Opportunity calls `((WorkGiver_Scanner)haulGeneral.Worker).JobOnThing(pawn, target.Thing)` (OpportunisticTasks.cs:140) where haulGeneral.Worker is WorkGiver_HaulGeneral (the 2-arg overload dispatches to the HD-patched 3-arg with forced=false). HD's BulkHaul postfix UPGRADES that valid HaulToCell to a bulk job (never nulls it). CS then EnqueueFirst's HD's HaulersDream_BulkHaul (StartJob -> TryOpportunisticJob sees allowOpportunisticPrefix==false => no recursion). Composes; investigator's 'no change, benign' confirmed.
- 'CS clean-before-work / cleaning jobs interact with HD' is REFUTED (orthogonal). CS's StartJob/EndCurrentJob prefixes EnqueueFirst Clean jobs (OpportunisticTasks.cs:157-262); Clean is not a haul/inventory job and never reaches HD's lister/comp/unload seams (HD's TryIssueJobPackage and TryOpportunisticJob postfixes early-out on non-yield/non-haul and empty tracked set). Investigator's 'risk:none' confirmed.
- 'CS-marked item ping-pong / pick-up-then-drop cycle with HD' is REFUTED as currently-open. CS's PutBackToBackpack (PutBackToBackpack.cs) only SneakyMove's a carried thing back to inventory when `cuc.WasInInventory` is true, and CS's GenDrop.TryDropSpawn_NewTmp postfix (UnloadChecker.cs:81-95) clears WasInInventory+ShouldUnload on any drop/spawn. An interrupted HD haul that lands back in inventory stays HD-tagged and HD re-services it — exactly what COMPATIBILITY.md:38-43 already documents and tests. HD sweeps GROUND items (BulkHaul pool/EnRoutePickup both scan map.listerHaulables, never inventory). No ping-pong; the WasInInventory flag CS sets on HD-scooped items is benign (it only affects CS's own SneakyMove-on-interrupt, which is the documented re-tag path).

**Missed by the first pass (folded into the plan):**
- No NEW crash/cycle risk found beyond the map — but one nuance the investigator UNDERSTATED: the WYAN postfix-ordering risk was framed as a 'react-after-replace' decision problem, yet it ALSO applies to HD's Patch_JobGiver_Haul_AnimalBulk (#4 animal feature) NOT being in scope is CORRECT — verified none of the four mods patch JobGiver_Haul (WYAN patches ONLY JobGiver_Work; grep across the WYAN decompiled tree shows JobGiver_Work as the sole vanilla AI target). So the animal bulk path has no co-patch from this family. This is a confirming negative, not a new risk — worth recording so the run-last priority fix is scoped to the two JobGiver_Work postfixes only.
- Process note (not a code risk): the investigator's claim that HD's CommonSenseCompat 'reads adv_cleaning/adv_haul_all_ings but NOT hauling_over_bills, and that is correct because hauling_over_bills does not replace the DoBill driver' is VERIFIED EXACTLY — CS's hauling_over_bills path is a StartJob-prefix EnqueueFirst of a HaulGeneral job (OpportunisticTasks.cs:182-188), NOT a driver replacement, so HD's cede (since #243 keyed on adv_haul_all_ings alone — the gather, an even narrower fact than the driver keying this note was written against) does not over-cede vs the bills detour. WYU separately reflection-reads CS's hauling_over_bills (Mod.cs:68) for its own gate; this does not involve HD. No gap — included to close the loop on the investigator's cross-mod field-name reasoning.

**Verdict:** The investigator's risk map is largely ACCURATE and well-grounded. Of its claims I could verify against actual source: the two existing targeted patches (Patch_HaulUrgently_BulkHaul, CommonSenseCompat) are genuinely correct and load-bearing (CS field names + cede condition byte-match the live CommonSense11/1.6 source; Keyz urgent-haul shape + Do-Not-Haul honoring confirmed). The WYU en-route 'redundant-not-conflicting', the ResourceDeliverJobFor postfix-at-Priority.Last, and the PUAH self-disable claims are all confirmed REFUTED-as-open (i.e. already handled). TWO of the investigator's general-hardening recommendations correspond to REAL, currently-absent guards: (1) HD's two JobGiver_Work.TryIssueJobPackage postfixes carry NO [HarmonyPriority], so cross-mod ordering with WYAN's equal-priority postfix is unpinned — the 'run-last' pin is a real fix (low harm: suboptimal divert decision, never a loop); (2) HD's JobGiver_UnloadYourInventory postfix has NO foreign-unload-def guard, so it overrides CS's UnloadMarkedItems job — the 'substitute only when __result.def == vanilla UnloadInventory' guard is a real fix (low severity: a CS-marked equipment/apparel item's unload can be delayed/skipped a scan, NOT a cycle). Both are correctly characterized in the map as GENERAL (mod-agnostic) hardening, not new mod-specific patches. WYU and WYAN are entirely undocumented in COMPATIBILITY.md (the investigator's doc recommendations are valid). I found NO high-confidence crash risk and NO additional cross-mod hazard the investigator missed; the unverifiable items (the WYU/WYAN/CS git changelog prior-bug entries) are plausibility claims I could not falsify but whose HD-side mitigations I did verify exist (en-route<->unload mutex, AlreadyHaulingIntoInventory, bleeding gate, anyUnloadable gate). Net: the map's 'topRisks' #2 (don't-override-foreign-unload) and #3 (run-last on JobGiver_Work) are the two genuine code gaps; everything else is already handled or impossible by construction. → GOTCHA (2026-08-09): "cede condition byte-match the live CommonSense source" aged badly — the byte-matched condition was itself the #243 bug. Matching CS's prefix gate verified driver OWNERSHIP; the load-bearing question was "will anyone gather", and no pass here asked what happens when CS owns the driver without gathering (cleaning-only). The cede has since been narrowed to adv_haul_all_ings alone; the half of this verdict that verified the cede FIRES under CS defaults, and that the loop stays closed, remains true.

---

## Family: `crafting-bills`

HD's batch-craft / bill-routing subsystems overlap heavily with this family but are, with one real exception, already well-guarded. The single high-risk item is GENERAL, not mod-specific: HD's full-replace prefix on BillRepeatModeUtility.MakeConfigFloatMenu (returns false) hides the repeat-mode entries of ANY mod that transpiles that method. HD fixed this for Everybody Gets One via EverybodyGetsOneCompat.TryInsertModes, but the bridge is EGO-only — any other mod that injects modes the same way is still hidden. Recommended general fix: instead of returning false, let vanilla (and thus every foreign transpiler) build the base list, then ADD HD's batch entries via a postfix (or call all foreign inserters generically). Two medium items: (a) Cook Carefully's poison-discard transpiles Toils_Recipe.FinishRecipeAndStartStoringProduct, which HD's JobDriver_BatchCraft REPLICATES (does not call) — so a batched CookMealCarefully (it is allowMixingIngredients=true and now batchable) banks poisoned meals instead of discarding them; CanBatch should exclude recipes another mod special-cases at the finish step (targeted exclusion of CookMealCarefully is the practical fix). (b) HD and Common Sense BOTH transpile WorkGiver_DoBill.TryFindBestBillIngredientsInSet_AllowMix's single SortBy call with no Harmony priority, so which spoiling-sort wins was load-order dependent (no crash; both do spoiling-first) — since resolved: HD's AllowMix transpiler now skips entirely when CS is installed (Prepare() => !CommonSenseCompat.IsActive, SharedBillPatches.cs:133), so CS's sort always applies on that path. The DoBill ingredient-gather loop with Common Sense is already correctly ceded via CommonSenseCedePolicy.CommonSenseGathersIngredients (keyed, since issue #243, on CS's adv_haul_all_ings alone — the only configuration in which CS actually gathers; it ships ON, so the cede fires at CS defaults). HD's banked-inventory CountProducts postfix is verified correct against decompiled Verse.RecipeWorkerCounter (vanilla counts inventory only when includeEquipped) and is deliberately mode-agnostic so it also fixes EGO's count-based modes. CanBatch correctly excludes EGO's dynamic person-count modes. The mech "10 jobs/tick" loop (a bug Common Sense itself fixed in 1.3.4) is guarded by BillRouteGate.WorkerMayShareCraft.

### Feature risk map

- `LOW` **Everybody Gets One - Continued — Three custom repeat modes (TD_PersonCount "one per person", TD_XPerPerson "X per person", TD_WithSurplusIng "with surplus") injected into the bill repeat-mode dropdown**
  - Mechanism: Transpiler on BillRepeatModeUtility.MakeConfigFloatMenu hooks the `new List<FloatMenuOption>()` Newobj and calls MakeConfigFloatMenu_Patch.InsertMode(list, bill) to add its three options (BillRepeatTypes.cs:203-265). Verified vanilla method at RimWorld.BillRepeatModeUtility.MakeConfigFloatMenu builds exactly the 3-entry list HD's prefix replaces.
  - HD overlap: Patch_BillRepeatModeUtility_MakeConfigFloatMenu (Patch_BillRepeatMode_Batch.cs:19-91): a Prefix that returns false, fully replacing the method body — which SKIPS EGO's transpiler. EGO modes would vanish from the dropdown.
  - Fix (targeted patch): Already fixed for EGO via EverybodyGetsOneCompat.TryInsertModes (EverybodyGetsOneCompat.cs), invoked in Patch_BillRepeatMode_Batch.cs. Reflection-only, fail-open. Residual risk is the GENERAL gap below, not EGO itself.
- `LOW` **Compositable Loadouts (Wiri, Steam 2679126859, assembly Inventory.dll) — one custom repeat mode "X per Tag" (W_PerTag: make one product per colonist assigned a loadout tag) injected into the bill repeat-mode dropdown** _(issue #92 — the predicted "second such mod")_
  - Mechanism: Transpiler on BillRepeatModeUtility.MakeConfigFloatMenu (Inventory.MakeConfigFloatMenu_Patch) splices a call to its own static `GetOptions(List<FloatMenuOption>, Bill_Production)` in front of the vanilla RepeatCount entry. GetOptions adds one "X per Tag" option whose action sets repeatMode=W_PerTag/targetCount=1/repeatCount=0/includeEquipped=true; the per-tag association lives in CL's LoadoutManager, not the bill. Verified against github.com/simplyWiri/Loadout-Compositing.
  - HD overlap: same as EGO — Patch_BillRepeatModeUtility_MakeConfigFloatMenu's full-replace prefix skips CL's transpiler, so "X per Tag" vanished from the dropdown when HD was installed (the reported #92 bug).
  - Fix (targeted patch): CompositableLoadoutsCompat.TryInsertModes (CompositableLoadoutsCompat.cs), invoked in Patch_BillRepeatMode_Batch.cs right after the EGO bridge. Reflection-only, fail-open; resolves Inventory.MakeConfigFloatMenu_Patch.GetOptions and invokes it so CL's option reappears with CL's own label/guard/bill-setup. CanBatch already refuses W_PerTag (non-vanilla mode), so HD never batches a CL-mode bill. GetOptions touches no LoadoutManager state, so it is UI-safe.
- `HIGH` **ANY mod (EGO + Compositable Loadouts are the known instances) — GENERAL PATTERN: any mod that adds repeat-mode dropdown entries by transpiling BillRepeatModeUtility.MakeConfigFloatMenu**
  - Mechanism: Same hook EGO/CL use (transpile the List<FloatMenuOption> ctor / append options). HD's full-replace Prefix (return false) skips the original method and EVERY transpiler on it, not just the two known ones.
  - HD overlap: Patch_BillRepeatModeUtility_MakeConfigFloatMenu full-replace prefix (Patch_BillRepeatMode_Batch.cs `return false`). EGO and Compositable Loadouts are now each re-surfaced via their own targeted bridge (TryInsertModes); any OTHER, not-yet-known such mod's modes still stay hidden.
  - Fix (general / no-op): GENERAL: stop returning false. Let vanilla's MakeConfigFloatMenu run (so every foreign transpiler executes and builds its full option list), then APPEND HD's three batch entries + the size option via a Postfix that re-opens/augments the FloatMenu — or transpile the same Newobj insertion point HD-side so HD composes with all other inserters instead of replacing them. This makes HD a peer of EGO et al. rather than a menu monopolist, and lets EverybodyGetsOneCompat be retired (its modes appear via EGO's own transpiler). The hint's 'invoke ALL foreign menu inserters generically' is hard to do safely (no registry of foreign InsertMode methods); the postfix-augment approach is the robust general form.
- `NONE` **Everybody Gets One - Continued — ShouldDoNow / CanUnpause / RepeatInfoText overrides making vanilla counting honor the dynamic TargetCount() (= person count + targetCount, etc.)**
  - Mechanism: Prefix patches on Bill_Production.ShouldDoNow, CanUnpause, RepeatInfoText (BillRepeatTypes.cs:57-200); CurrentPersonCount() reads a TD_Find QuerySearch over the colony (ColonistCounter.cs:220).
  - HD overlap: CraftBatchPlanner.CanBatch (CraftBatchPlanner.cs:81-84) explicitly EXCLUDES any repeatMode that is not the 3 vanilla modes — so EGO modes are never batched. Patch_CountProducts_BankedInventory is mode-agnostic and HELPS EGO's count-based modes (it adds banked inventory to CountProducts which EGO's ShouldDoNow reads).
  - Fix (general / no-op): No change. CanBatch correctly refuses to batch a dynamic-target EGO mode (HD doesn't model that target), and HD's count postfix is a net benefit to EGO (its ShouldDoNow/CanUnpause now observe in-flight banked products too).
- `NONE` **Everybody Gets One - Continued — DoConfigInterface / Dialog_BillConfig.DoWindowContents / JobDriver_DoBill recount-toil transpilers (OrPersonCount_Transpiler) extending vanilla TargetCount handling to EGO modes**
  - Mechanism: Transpilers on Bill_Production.DoConfigInterface, Dialog_BillConfig.DoWindowContents, and a compiler-generated JobDriver_DoBill recount toil via PatchCompilerGenerated.PatchGeneratedMethod (BillRepeatTypes.cs:132-440).
  - HD overlap: None — HD does not patch any of these methods. HD's MakeConfigFloatMenu prefix does not touch them (different methods), so these transpilers run normally.
  - Fix (general / no-op): No change.
- `MEDIUM` **Cook Carefully — CookMealCarefully bill: discards a poisoned meal at finish (ends job, destroys the product) so low-skill cooks learn without poisoning the colony**
  - Mechanism: Transpiler on the compiler-generated initAction of Toils_Recipe.FinishRecipeAndStartStoringProduct, injected after RecordsUtility.Notify_BillDone: RemoveIfPoisoned checks each product's CompFoodPoisonable and, for recipe defName 'CookMealCarefully', Destroys it + EndCurrentJob(Succeeded) (CookCarefully.cs:27-90). Recipe is allowMixingIngredients=true with cooking products (Recipes_CookCarefully.xml).
  - HD overlap: JobDriver_BatchCraft.FinishOneRepIntoInventory (JobDriver_BatchCraft.cs:529-625) REPLICATES FinishRecipeAndStartStoringProduct rather than calling it, so Cook Carefully's transpiler never runs for a batched bill. Since CookMealCarefully is a mixing recipe with products, CanBatch returns true (CraftBatchPlanner.cs:102), so a batched CookMealCarefully would bank poisoned meals into inventory instead of discarding them — defeating the recipe's whole purpose.
  - Fix (targeted patch): GENERAL where possible: CanBatch should not batch recipes whose finish step is special-cased by another mod. There is no generic way to detect 'a mod transpiled FinishRecipeAndStartStoringProduct for this recipe', so the practical fix is a small soft-dependency exclusion: in CanBatch, refuse recipes whose defName is the Cook Carefully recipe (resolve 'CookMealCarefully' by name, like the other reflection compat shims) — falling back to vanilla single-meal crafting so the poison-discard runs. (A truly general alternative — have the batch finish route through the real Toils_Recipe.FinishRecipeAndStartStoringProduct so any finish-transpiler applies — is a large rewrite of the batch driver and risks the duplication traps the driver was built to avoid.)
- `LOW` **Common Sense — Advanced ingredient hauling: replaces JobDriver_DoBill's toil chain to gather ALL ingredients into inventory in one sweep, set them on the bench, then craft (adv_haul_all_ings, default ON)**
  - Mechanism: Prefix on JobDriver_DoBill.MakeNewToils that returns its own DoMakeToils when adv_cleaning||adv_haul_all_ings; Prepare() = !optimal_patching_in_use||adv_cleaning||adv_haul_all_ings (JobGiver_DoBIll_Rework.cs:18-419). Defaults verified: adv_haul_all_ings=true, adv_cleaning=true, optimal_patching_in_use=false (Settings.cs).
  - HD overlap: HD's three DoBill conversions — Patch_WorkGiver_DoBill_BatchRoute, _InventoryRoute, and the ingredient-share injection in SharedBillPatches — would re-deposit HD's in-inventory ingredients to the bench floor that CS re-gathers, creating an infinite gather→bench→unload loop.
  - Fix (targeted patch): Already fully handled, re-keyed 2026-08-09 for issue #243: CommonSenseCompat.GathersIngredients (CommonSenseCompat.cs:138-160) live-reads CS's adv_haul_all_ings and HD cedes (returns early) at the two gather paths (Patch_WorkGiver_DoBill_InventoryRoute.cs:71 and the carried-stock injection at SharedBillPatches.cs:71). The batch route is governed separately — by driver ownership plus the "Batch even with Common Sense active" opt-in (BatchSuppressedByCommonSense, Patch_WorkGiver_DoBill_BatchRoute.cs:55) — because HaulersDream_BatchCraft overrides MakeNewToils in a subclass CS never patches (the by-construction fact recorded under 'Missed by the first pass' below), so batching runs safely under CS by default. Fail-closed when the field doesn't resolve. Deliberately NOT keyed on the driver replacement: that broader keying was the #243 starve (cleaning-only owns the driver but collects via vanilla's CollectIngredientsToils, JobGiver_DoBIll_Rework.cs:426-432, so a cede there leaves nobody gathering) — and for the same reason, do NOT "generalize the cede to any non-HD JobDriver_DoBill.MakeNewToils prefix" as this entry once suggested, since a foreign prefix proves ownership, not gathering.
- `LOW` **Common Sense — Prefer spoiling ingredients: sorts mixing-recipe ingredient candidates by closeness-to-rot (prefer_spoiling_ingredients, default ON)**
  - Mechanism: Transpiler on WorkGiver_DoBill.TryFindBestBillIngredientsInSet_AllowMix appending DoSort(availableThings, bill) AFTER the single SortBy call (IngredientPriority.cs:110-167). Matches by operand.Name=="SortBy". Also a transpiler on TryFindBestIngredientsHelper to add same-stockpile spoiling candidates (PreProcess).
  - HD overlap: HD ALSO transpiles the same SortBy call in TryFindBestBillIngredientsInSet_AllowMix, replacing it with SpoilingFirst.SortAllowMix (structural match on GenCollection.SortBy<3>). Neither HD nor CS set a Harmony priority, so which sort won was load-order dependent: HD first → CS's matcher found no 'SortBy' Call, logged '[Common Sense] ...patch 0 didn't work' and its sort went silently inactive; CS first → both ran and CS's DoSort (last) won.
  - Fix (general / no-op): RESOLVED — the "CS-present skip" once listed here as optional hardening has shipped: HD's AllowMix transpiler now declines to apply at all when Common Sense is installed (Prepare() => !CommonSenseCompat.IsActive, SharedBillPatches.cs:133), so CS's spoiling sort always lands on the vanilla-chooser cook path, the benign CS warning is gone, and the load-order dependence is gone. HD's batch-cook ingredient picker still honors HD's cook keys (it is CS-immune by design), and with CS absent HD's transpiler applies as before.
- `NONE` **Common Sense — Advanced cleaning during bills (clean filth around the bench as part of the DoBill toil chain; adv_cleaning default ON)**
  - Mechanism: Same JobDriver_DoBill.MakeNewToils Prefix as the ingredient-haul feature; appends ListFilthToil/CleanFilthToil sequence (JobGiver_DoBIll_Rework.cs:386-399). With cleaning ON and haul-all OFF, the replacement chain's else branch yields RimWorld's own CollectIngredientsToils — argument for argument vanilla's call (:426-432) — so CS holds the driver but gathers nothing.
  - HD overlap: None since the #243 re-keying (2026-08-09). This entry originally read "adv_cleaning alone makes CS own the flow, and HD steps aside" — and HD stepping aside there WAS issue #243: nothing gathered into inventory under cleaning-only, so the cede starved the gather and the bench button could stop it but not start it. HD now deliberately keeps gathering in that configuration, and the two compose: HD converts at the work-giver level into a job def CS never patches, vanilla's CollectIngredientsToils (which CS's cleaning-only driver delegates to) takes inventory-held ingredients natively, and CS's filth detour is appended AFTER the collecting, driven off the job's queue A, which the ingredient flow never touches.
  - Fix (general / no-op): No change needed now — but by composition, not by ceding. The prior verdict ("subsumed by the cede") is retracted; only the batch conversion still consults the driver fact, via the "Batch even with Common Sense active" opt-in.
- `LOW` **Common Sense — Parallel auto-unload: items the player marks via the gear-tab button are unloaded via the SAME vanilla JobGiver_UnloadYourInventory node; cross-tags everything entering inventory with WasInInventory**
  - Mechanism: CompUnloadChecker on things; ThingOwner<Thing>.TryAdd postfix tags inventory adds; JobGiver_UnloadYourInventory.TryGiveJob Prefix re-routes to UnloadMarkedItems when something is marked (UnloadChecker.cs:99-127). GenDrop.TryDropSpawn postfix clears the flags on drop.
  - HD overlap: HD has its own tagging (CompHauledToInventory) and its own unload pass. Both ride JobGiver_UnloadYourInventory. CS only unloads items YOU marked via its gear tab (HD never sets that flag), and HD re-finds its own work from its tags.
  - Fix (general / no-op): Already composes (documented in COMPATIBILITY.md). CS's ThingOwner.TryAdd postfix will set WasInInventory=true on HD-loaded stock too, but that only matters for CS's 'put back to backpack' (returns an interrupted carry to inventory) which is self-healing for HD (the returned stack is still HD-tagged → HD re-issues). No HD change.
- `LOW` **Common Sense — Put interrupted carry back to inventory (CleanupCurrentJob transpiler; put_back_to_inv default ON)**
  - Mechanism: Transpiler on Pawn_JobTracker.CleanupCurrentJob redirecting TryDropCarriedThing to SneakyMoveToInventory, which transfers a WasInInventory carry back to inventory instead of dropping (PutBackToBackpack.cs).
  - HD overlap: An interrupted HD haul/unload mid-carry would be returned to inventory rather than dropped. HD's stock is still tagged, so HD re-issues the unload — self-healing.
  - Fix (general / no-op): No change. Self-healing via HD's tag-and-re-find. (Note CS changelog 1.3.9 fixed a pick-up-and-drop cycle when its own unload-marking is OFF — a mod-order issue; HD's tagging is independent so it is not the cause.)
- `LOW` **Common Sense — Opportunistic hauling-over-bills: when a pawn starts a DoBill, divert to haul a bill ingredient stack first if it is closer to storage (hauling_over_bills default ON)**
  - Mechanism: Prefix on Pawn_JobTracker.StartJob; for a DoBill job, Hauling_Opportunity builds a HaulToInventory/HaulGeneral job and enqueues it before the bill (OpportunisticTasks.cs:112-228).
  - HD overlap: Overlaps conceptually with HD's storage-routing 'haul before carry' (Patch_WorkGiver_DoBill_Routing). Both can re-store an ingredient stack closer before crafting. They act at different layers (CS at StartJob enqueue; HD at JobOnThing result) and CS only enqueues when its distance heuristic fires.
  - Fix (general / no-op): No crash; worst case both want to relocate the same stack and one wins (the other no-ops on the next scan). HD's routing already runs at Priority.Last and bails when the job is no longer a plain DoBill. Optionally HD could cede routeIngredients to CS when CommonSenseGathersIngredients (CS already rewrote the gather), but the current independent behavior is safe. Low priority.
- `NONE` **Common Sense — Optimize ingredient pickup path (reorder chosen ingredient stacks for a shorter route; adv_haul_all_ings)**
  - Mechanism: Postfix on WorkGiver_DoBill.TryFindBestBillIngredients calling Utility.OptimizePath(chosen) (IngredientPriority.cs:15-30).
  - HD overlap: HD also Prefixes TryFindBestBillIngredients (to stash CurrentWorker) and Prefixes TryFindBestBillIngredientsInSet (to inject carried stock + reorder). HD's injection is ceded when CommonSenseGathersIngredients — the same adv_haul_all_ings this CS postfix is gated on — so whenever CS's path-optimize is live, HD's injection is off and it runs on the vanilla-chosen set.
  - Fix (general / no-op): No change — HD's injection stands down under CS, so there is no double-reorder fight on the chosen list.
- `NONE` **Common Sense — Separate meals by ingredient (don't stack meals with different ingredient sets; separate_meals), add meal ingredients to crafted ingestibles (add_meal_ingredients), prefer spoiling meals when eating (prefer_spoiling_meals)**
  - Mechanism: Thing.CanStackWith prefix (MealStacking.cs), ThingMaker.MakeThing postfix (RandomIngredients.cs), FoodUtility.FoodOptimality postfix (IngredientPriority.cs:170-203).
  - HD overlap: None — HD does not touch CanStackWith, ThingMaker, or FoodOptimality. HD's batch driver makes products via GenRecipe.MakeRecipeProducts (the public maker), so CS's ThingMaker postfix still fires for batched products.
  - Fix (general / no-op): No change.
- `NONE` **Common Sense — Clean before work / clean after tending / fun police (opportunistic non-bill behaviors)**
  - Mechanism: Pawn_JobTracker.StartJob and EndCurrentJob prefixes (OpportunisticTasks.cs:154-278).
  - HD overlap: None relevant to crafting-bills. Can interrupt/enqueue ahead of an HD job, but HD jobs are tag-and-re-find self-healing.
  - Fix (general / no-op): No change.

### Prior bugs learned from (their bug-fix history)

- ⚠️ applies to HD — **Common Sense 1.3.4: 'allegedly fixed a problem that caused 10 jobs per tick issue'** _(CommonSense change.log)_ — Same bug CLASS HD guards against with BillRouteGate.WorkerMayShareCraft (BillRouteGate.cs:54): a DoBill issued for a worker (esp. a mech that ignores forbidden/allowed-area) that cannot complete and re-issues every tick. HD explicitly cites 'started 10 jobs in one tick stonecutter loop' in its mech-exclusion comments. HD is already protected — the share-injection, BatchRoute, and InventoryRoute all gate on WorkerMayShareCraft. Confirms the guard is correctly motivated.
- ⚠️ applies to HD — **Common Sense 1.3.5: 'fixed pawns sometimes not using full cost to make an item; fixed pawns using wrong primary material to make an item'** _(CommonSense change.log)_ — Wrong-dominant-ingredient / under-consume class. HD's batch driver consumes via job.placedThings → CalculateIngredientsFromPlacedThings and selects dominant via CalculateDominantIngredient mirroring vanilla (JobDriver_BatchCraft.cs:558-582), and its ingredient injection only ADDS candidates to vanilla's chooser (vanilla still picks/counts). The risk is reintroducing under-cost if HD's per-rep value-fill (BuildMixForRep, mixing path) miscomputes counts — HD has unit tests (CraftBatchMath) for the cap math but the dominant/consume is RimWorld-side. Recommend in-game verification that batched mixing recipes consume the full per-rep value and pick the correct dominant ingredient.
- ⚠️ applies to HD — **Common Sense 1.3.9: pick-up-then-drop cycle when its unload-marking option is off ('mod order issue with a mod that I don't even know')** _(CommonSense change.log)_ — Same gather/deposit/re-gather loop class as HD's CommonSenseGathersIngredients concern. HD's cede (CommonSenseCedePolicy.CommonSenseGathersIngredients) plus tag-and-re-find unload prevents HD from being the unnamed mod in this cycle. The interrupted-carry return (PutBackToBackpack) lands HD stock back in inventory still tagged, so no perpetual cycle. Already mitigated.
- ⚠️ applies to HD — **Common Sense 1.2.6: 'standing still when prefer spoiling ingredients is enabled'; 1.2.11: made prefer-spoiling-meals more aggressive** _(CommonSense change.log)_ — The spoiling-sort itself caused a pawn-freeze bug for CS. HD implements its OWN spoiling-first reorder (SpoilingFirst / Patch_WorkGiver_DoBill_TryFindBestBillIngredientsInSet). HD must ensure its reorder never produces an unsatisfiable/empty candidate set that stalls the chooser. HD sets alreadySorted=true only after a successful in-place reorder (SharedBillPatches.cs:90-91) and never removes candidates (only reorders), so it should not reintroduce the freeze — and since the CS-present skip shipped, the two never sort AllowMix at once (with CS installed only CS's sort runs there), which retires the both-on verification ask.
- ⚠️ applies to HD — **Common Sense 1.3.9 / 1.3.10: gestator/autonomous-building compat + an unequip/equip stacking loop with an unknown mod** _(CommonSense change.log)_ — Autonomous-worktable hauling is fragile (deposit-into-container vs inventory-relay). HD already EXCLUDES Building_WorkTableAutonomous from its inventory-gather routing (BillRouteGate.MayRouteToInventory, BillRouteGate.cs:31) for exactly this reason. Same class of bug correctly pre-empted.
- ⚠️ applies to HD — **Cook Carefully has no changelog/git history beyond a 'bump' commit; recipe is a thin add (CookMealCarefully)** _(CookCarefully git log + Recipes_CookCarefully.xml)_ — No prior-bug data, but the design itself (discard at finish) is the bug surface: any mod that bypasses Toils_Recipe.FinishRecipeAndStartStoringProduct (as HD's batch driver does) silently defeats it. This is the medium-risk feature item above.
- n/a to HD — **Everybody Gets One: only a translation-add commit in git; mechanism is mature (multi-version transpilers + dialog patches)** _(EverybodyGetsOne git log + About.xml)_ — No compat bug to learn from in history. The relevant 'bug' (EGO modes vanishing under HD) is HD-introduced and already fixed via TryInsertModes; the residual is the general MakeConfigFloatMenu-monopoly gap (high item above).

### Top risks
- GENERAL menu-monopoly: HD's full-replace prefix on BillRepeatModeUtility.MakeConfigFloatMenu (return false, Patch_BillRepeatMode_Batch.cs) hides the repeat-mode entries of ANY mod that transpiles that method. EGO and Compositable Loadouts (the predicted "second such mod", issue #92) are each re-surfaced via their own targeted bridge; a THIRD, not-yet-known such mod would still silently lose its dropdown modes. A general compose-don't-replace fix would retire the per-mod bridges, but is deferred (the vanilla method opens the FloatMenu inline, so a postfix can't cleanly augment it — see the deferred item below).
- Cook Carefully batched: CookMealCarefully (allowMixingIngredients=true, has products → CanBatch true) is batched by HD, whose JobDriver_BatchCraft.FinishOneRepIntoInventory REPLICATES rather than calls Toils_Recipe.FinishRecipeAndStartStoringProduct, so Cook Carefully's poison-discard transpiler never fires — poisoned meals get banked into inventory, defeating the recipe.
- HD + Common Sense both transpile TryFindBestBillIngredientsInSet_AllowMix's SortBy with no Harmony priority — load-order decided which spoiling-sort won (no crash, cosmetic CS warning when HD won the IL), and re-verifies the same-class freeze bug CS itself had (1.2.6). Since resolved by the shipped CS-present skip (HD's transpiler Prepare() gate) — see the feature entry.
- Under-consume / wrong-dominant-ingredient regression risk in HD's batch mixing path (the class CS fixed in 1.3.5): HD's per-rep value-fill + dominant selection is RimWorld-side and not fully unit-covered; needs in-game verification.
- If a SECOND mod also rewrites JobDriver_DoBill.MakeNewToils like Common Sense, HD's CommonSenseGathersIngredients cede is keyed on CS's specific settings fields and would not detect it — HD could re-open a gather loop with that other mod. (Per #243, the right key for any such mod is whether it will GATHER, not whether it owns the driver — a rewrite that delegates collecting back to vanilla needs no cede at all.)

### General-pattern recommendations
- Menu/option-list patches must COMPOSE, not REPLACE: when HD augments a vanilla UI list-builder (MakeConfigFloatMenu), prefer a Postfix that appends to / re-opens the menu, OR transpile the same insertion point, so every other mod's transpiler on that method still runs. A full-replace prefix that returns false is a monopoly that hides all peers and forces a per-mod bridge (the EGO TryInsertModes is exactly that debt). The general rule: never `return false` from a prefix on a method other mods are known to transpile for additive UI.
- CanBatch (and any HD path that re-implements a vanilla recipe finish) must exclude recipes another mod special-cases at the finish/product step. HD already excludes UFT, take-entire-stacks, non-Bill_Production, and non-vanilla repeat modes; add 'recipes whose finish is transpiled by another mod' (detected by soft-dep recipe-defName match where a generic detector is impossible). General principle: if HD replaces a vanilla method instead of calling it, HD inherits responsibility for every mod that patched the original — either route through the real method or exclude the special-cased inputs.
- Cede to a mod that will actually DO the shared work, keyed on that mod's own live toggles — RETRACTED as originally written (2026-08-09, issue #243). This recommendation used to read "cede to any mod that OWNS a shared driver flow" and suggested generalizing detection to "a non-HD Prefix is registered on JobDriver_DoBill.MakeNewToils that returns false". Ownership is the wrong key: CS's cleaning option owns the driver yet hands collecting back to vanilla's CollectIngredientsToils, so an ownership-keyed cede — including that prefix-detection generalization, which can only ever prove ownership — leaves nobody gathering. CommonSenseCedePolicy.CommonSenseGathersIngredients now keys on the gather fact (adv_haul_all_ings); a future driver-rewriting mod needs a cede only if it gathers, and telling that apart requires reading its settings/behaviour, not its patch table.
- When two mods transpile the SAME single instruction (here the AllowMix SortBy), set an explicit Harmony priority or make the matcher robust to a prior wrapper, so behavior is load-order-independent. At minimum, match structurally (HD does) and tolerate the case where the target call was already replaced (degrade to no-op, log once).
- Count-correction at the SOURCE (RecipeWorkerCounter.CountProducts postfix) is the right general place to make HD-banked inventory visible to ANY count-based repeat mode (vanilla TargetCount AND EGO's modes), gated on !includeEquipped to avoid double-count — verified against decompiled Verse.RecipeWorkerCounter. This is a model for 'fix it once where every consumer reads it' rather than per-mode patches.
- Mech/non-colonist worker exclusion (BillRouteGate.WorkerMayShareCraft) is the general guard against the '10 jobs/tick' loop class that affects ANY DoBill-rerouting feature; keep it the single source of truth across all bill-conversion entry points (HD already does).

### Targeted patches (only where a general pattern can't cover it)
- Cook Carefully: add a soft-dependency exclusion in CraftBatchPlanner.CanBatch that refuses recipes whose defName is 'CookMealCarefully' (resolve by name, reflection-style, so no hard ref) — so batched cooking falls back to vanilla single-meal crafting and the poison-discard transpiler runs. Targeted because there is no generic way to detect 'a mod transpiled FinishRecipeAndStartStoringProduct for THIS recipe'. (If feasible later, prefer the general route-through-the-real-finish approach to also cover other finish-step mods.)
- Everybody Gets One: the existing EverybodyGetsOneCompat.TryInsertModes bridge is a targeted patch that should be RETIRED in favor of the general menu-compose fix (top risk #1). Keeping it is acceptable short-term but it only covers EGO; the general fix makes it redundant.
- Common Sense: the CommonSenseGathersIngredients cede is keyed on CS's exact settings field (adv_haul_all_ings — the gather fact; adv_cleaning is read only for the driver fact behind the "Batch even with Common Sense active" opt-in). This is appropriately targeted (CS is the known DoBill-rewriter); if a second such mod appears, key any new cede on whether THAT mod gathers — the ownership generalization once pointed to here is retracted (#243; see generalPatterns #3).

### Adversarial verification
**Confirmed real:**
- GENERAL MENU-MONOPOLY (top risk, mechanism real): HD's Prefix on BillRepeatModeUtility.MakeConfigFloatMenu returns false (Patch_BillRepeatMode_Batch.cs:90), fully replacing the method. A Harmony Prefix returning false skips the original body AND every transpiler attached to it. Only EGO is re-surfaced, via the EGO-specific bridge EverybodyGetsOneCompat.TryInsertModes (Patch_BillRepeatMode_Batch.cs:53). Verified vanilla BillRepeatModeUtility.MakeConfigFloatMenu builds exactly the 3 entries HD copies, and EGO's MakeConfigFloatMenu_Patch.Transpiler (EverybodyGetsOne/Source/BillRepeatTypes.cs:203-265) hooks the List<FloatMenuOption> ctor to InsertMode. So ANY OTHER mod adding repeat-mode entries the same way would lose them. CAVEAT (skeptic): within THIS 3-mod family, EGO is the ONLY mod doing this and it IS handled — there is no concrete second instance, so the realized impact today is zero; the risk is a latent pattern, not an active bug.
- SORTBY LOAD-ORDER (real, load-order-dependent, no crash): Both HD (SharedBillPatches.cs:111-171) and CS (CommonSense16/IngredientPriority.cs:110-167) transpile the single SortBy call in vanilla WorkGiver_DoBill.TryFindBestBillIngredientsInSet_AllowMix (verified exactly one SortBy at WorkGiver_DoBill.cs:695, neither sets Harmony priority). HD REPLACES the `Call SortBy` instruction (emits Ldarg_1; Call GetSettings; Call SortAllowMix and `continue`, never re-emitting SortBy). CS matches `opcode==Call && operand.Name=="SortBy"`. If HD wins the IL race, CS's matcher finds no SortBy call -> b0 stays false -> CS logs '[Common Sense] ...patch 0 didn't work' and its DoSort is never inserted. If CS wins, both run and CS's DoSort (appended last) wins. Confirmed: no crash, benign CS warning when HD wins. REFINEMENT to the map's 'cosmetic' label: it is NOT purely cosmetic when HD's cookSpoilingFirst toggle is OFF — in that case HD's SortAllowMix falls back to plain vanilla SortBy (SpoilingFirst.cs:73), so if HD wins the IL, CS's prefer_spoiling_ingredients for COOKING is silently lost entirely (not merely 'a different spoiling-first wins'). So under (HD cook toggle off + CS spoiling on + HD wins load order) the user lost CS's cook spoiling-sort. (Moot since the CS-present skip shipped: with CS installed HD's AllowMix transpiler never applies.)
- CookCarefully poison-discard bypass MECHANISM is real (only the version reality refutes it): vanilla Toils_Recipe.FinishRecipeAndStartStoringProduct (Toils_Recipe.cs:171-225) was decompiled and HD's JobDriver_BatchCraft.FinishOneRepIntoInventory (JobDriver_BatchCraft.cs:529-625) is confirmed a faithful REPLICA that never calls the real method, so CookCarefully's transpiler (inserted right after RecordsUtility.Notify_BillDone, CookCarefully.cs:49-68) would indeed never fire for a batched bill. CanBatch DOES return true for CookMealCarefully (allowMixingIngredients=true with a product; CraftBatchPlanner.cs:102). So IF CookCarefully were active, batched CookMealCarefully would bank poisoned meals. (See refuted list for why it is not active on the 1.6 target.)

**Refuted (not real / already handled):**
- CookCarefully risk is NOT REAL on the actual target version (RimWorld 1.6): CookCarefully's About/About.xml supportedVersions lists ONLY 1.3 and 1.4; the only version folders are 1.3/ and 1.4/; csproj defines only v13/v14 (no v15/v16); the newest assembly is 1.4/Assemblies/CookCarefully.dll; there is NO root Assemblies/ folder and NO 1.5/1.6 folder. On a 1.6 game RimWorld loads no CookCarefully assembly (only version-matched or root assemblies load), so the poison-discard transpiler NEVER applies for ANY cook -- batched or vanilla. The CookMealCarefully recipe def lives in root Defs/ so it loads and is batchable, but there is no poison-discard behavior on 1.6 for HD to defeat. The map's premise that the discard is active and HD silently defeats it does not hold on the target version. (Conditional: the risk would re-activate ONLY if CookCarefully ships a 1.6 assembly. The map also did not flag a secondary issue: even the 1.4 DLL's `Ldloc_S, IngredientsIndex=6` is keyed to 1.4 IL and would likely mis-target on 1.6 local layout, a further reason it is not a live 1.6 concern.)
- EGO modes vanishing under HD -- already fully handled: EverybodyGetsOneCompat.TryInsertModes (EverybodyGetsOneCompat.cs:46-54) resolves Everybody_Gets_One.MakeConfigFloatMenu_Patch.InsertMode(List<FloatMenuOption>, Bill_Production) by reflection and invokes it inside HD's rebuilt menu (Patch_BillRepeatMode_Batch.cs:53). Signature verified against EGO 1.6 source (BillRepeatTypes.cs:222 -- exact match, and EGO's /Source builds to 1.6/Assemblies). Fail-open when EGO absent. EGO's 3 modes reappear with EGO's own labels/guards.
- CountProducts double-count concern -- not real: HD's Patch_CountProducts_BankedInventory (postfix on RecipeWorkerCounter.CountProducts) guards on !bill.includeEquipped (line 44). Verified against decompiled vanilla RecipeWorkerCounter.CountProducts: vanilla counts pawn INVENTORY only inside the `if (bill.includeEquipped)` block, and GetCarriedCount counts only carryTracker.CarriedThing (the HANDS). HD's BankedInFlightProductCount filters ParentHolder is Pawn_InventoryTracker (inventory only, never hands), applies CountValidThing, and counts only InventorySurplus.SurplusOf. So it adds inventory exactly when vanilla doesn't -> no double-count with storage, hands, or includeEquipped. Mode-agnostic design correctly HELPS EGO's count-based modes (TD_PersonCount/TD_XPerPerson ShouldDoNow reads CountProducts) and is irrelevant to TD_WithSurplusIng (which reads IngredientCount, not CountProducts).
- CanBatch batching EGO dynamic-target modes -- not real: CraftBatchPlanner.CanBatch (CraftBatchPlanner.cs:81-84) returns false for any repeatMode not in {RepeatCount, TargetCount, Forever}, so EGO's TD_PersonCount/TD_XPerPerson/TD_WithSurplusIng are never batched. Confirmed.
- Common Sense DoBill gather-loop -- the loop stays closed, but this entry's original "Verified safe" (a cede matching CS's own prefix condition, adv_cleaning||adv_haul_all_ings, then named ShouldCedeDoBillFlow) is RETRACTED (2026-08-09, issue #243). The decompiled half stands: CS's Prefix returns false — replacing the driver — exactly when adv_cleaning||adv_haul_all_ings (JobGiver_DoBIll_Rework.cs:452-460 in the shipped-DLL decompile; the older CommonSense16 source snapshot cited :412-419), its gather chain is gated on adv_haul_all_ings alone (:380), and the cleaning-only else branch yields vanilla's own CollectIngredientsToils (:426-432). The verdict half was wrong: matching the prefix condition verified driver OWNERSHIP when the load-bearing question was whether anyone GATHERS — under cleaning-only CS owned the driver, collected via vanilla, and the matched cede left NOBODY gathering. ShouldCedeDoBillFlow no longer exists. Current shape: CommonSenseCedePolicy.CommonSenseGathersIngredients (adv_haul_all_ings only) cedes the two gather paths (InventoryRoute.cs:71, SharedBillPatches.cs:71 ingredient-share injection); CommonSenseOwnsDoBillDriver (either option) survives for the batch opt-in only (BatchRoute.cs:55, "Batch even with Common Sense active"). Fail-closed when CS fields don't resolve.
- Mech '10 jobs/tick' loop -- already guarded: BillRouteGate.WorkerMayShareCraft (BillRouteGate.cs:54) excludes mechanoids from the share-injection, meet-in-the-middle nudge, and conversions; CS's own adv_haul gate also requires RaceProps.Humanlike (JobGiver_DoBIll_Rework.cs:336). Both mods independently exclude mechs. Verified.
- CS prefer_spoiling_meals / separate_meals / FoodOptimality / ThingMaker -- not real conflicts: HD does not patch FoodUtility.FoodOptimality, ThingMaker.MakeThing, or Thing.CanStackWith; HD's batch driver makes products via GenRecipe.MakeRecipeProducts so CS's ThingMaker postfix still fires. Confirmed against CommonSense16 sources.

**Missed by the first pass (folded into the plan):**
- BENEFICIAL interaction the map UNDER-rated (separate_meals): HD's JobDriver_BulkHaul inventory consolidation gates its stack merge on target.CanStackWith(split) (JobDriver_BulkHaul.cs:227), which routes through vanilla Thing.CanStackWith -- the exact method CS's CompIngredients_CanStackWith_CommonSensePatch postfixes (CommonSense16/MealStacking.cs). So HD's bulk-sweep merge ACTIVELY HONORS CS's separate_meals (differing-ingredient meals return CanStackWith=false and HD won't merge them). The map marked MealStacking 'none / HD does not touch CanStackWith', which understates it: HD CONSUMES CanStackWith, so it composes correctly with separate_meals by construction. Verified non-risk, but the map's reasoning was incomplete.
- VERIFIED non-risk the map did not explicitly trace: HD's JobDriver_BatchCraft : JobDriver_DoBill (JobDriver_BatchCraft.cs:76) cleanly OVERRIDES MakeNewToils (line 293, does not call base). CS's Prefix is on the base JobDriver_DoBill.MakeNewToils MethodInfo; Harmony patches that specific declaration, and virtual dispatch on a JobDriver_BatchCraft instance goes to the override, which CS has NOT patched. So CS's gather-rewrite never fires INSIDE HD's batch driver -- the batch driver's self-contained toil list runs unmolested even with CS active. Safe by construction, worth documenting since a future refactor that made BatchCraft call base.MakeNewToils() would silently break this.
- PARTIAL silent-loss the map mislabeled (see SortBy item in confirmed): the map calls the HD/CS SortBy collision 'cosmetic, both do spoiling-first.' That is only true when HD's cookSpoilingFirst toggle is ON. When HD's cook toggle is OFF and HD wins the IL race, CS's prefer_spoiling_ingredients for cooking is fully suppressed (HD's SortAllowMix degrades to plain vanilla SortBy and CS's DoSort was never inserted). This is a genuine (if minor, load-order-and-toggle-gated) functional loss of a CS feature, not a cosmetic log line. A Harmony priority on HD's AllowMix transpiler, or HD skipping its own AllowMix transpiler when CS is present, would make it order-independent — the CS-present skip has since shipped (SharedBillPatches.cs:133).
- EGO RepeatInfoText interaction -- verified benign but not stated in map: EGO's RepeatInfoText_Patch is a Prefix returning false for its 3 modes (BillRepeatTypes.cs:57-80); HD's Patch_Bill_Production_RepeatInfoText is a Postfix that runs afterward but is gated on CraftBatchPlanner.CanBatch (Patch_BillRepeatMode_Batch.cs:117), which is false for EGO modes -- so HD never prepends its 'xN' marker onto an EGO-mode bill's repeat text. No double/garbled UI. Confirmed safe.

**Verdict:** The investigator's map is largely accurate on mechanism but mis-calibrated on REALITY for its two highest-profile items. (1) The general MENU-MONOPOLY risk is a real latent pattern but has ZERO active instance in this 3-mod family (EGO, the only mod doing it, is fully bridged) -- so it is 'high as a principle, none as a current bug.' (2) The Cook Carefully risk is REFUTED on the actual target version: CookCarefully ships only 1.3/1.4 assemblies (no 1.5/1.6, no root Assemblies/), so on RimWorld 1.6 its poison-discard transpiler never loads and there is nothing for HD to defeat -- the map treated a 1.4-only behavior as active. HD's replication of the finish step IS faithful (decompile-confirmed), so the risk would re-activate only if CookCarefully gets a 1.6 build. (3) The SortBy load-order item is real and slightly WORSE than 'cosmetic': under HD-cook-off + HD-wins-IL it silently disables CS's cook spoiling-sort. All other claimed risks (EGO bridge, CountProducts double-count, CanBatch excluding EGO modes, CS DoBill cede, mech loop, meal/ThingMaker) are correctly assessed as already-handled / not-real, and I confirmed each against decompiled vanilla 1.6 and the live mod sources. Two missed-but-fine interactions: HD's bulk merge actively honors CS separate_meals via CanStackWith, and HD's batch driver override is immune to CS's MakeNewToils Prefix by virtual-dispatch construction. Net: the only items warranting a code change are the menu-compose generalization (defensive/forward-looking) and an explicit Harmony priority (or CS-present skip) on HD's AllowMix SortBy transpiler — the CS-present skip has since shipped; the Cook Carefully targeted exclusion is unnecessary on 1.6 (optional future-proofing only). → GOTCHA (2026-08-09): "CS DoBill cede — correctly assessed as already-handled" did not survive the field. The cede stopped the LOOP exactly as verified, but neither the investigator nor this pass asked the opposite question — does the cede fire when CS will NOT gather? Issue #243 (cleaning-only → CS owns the driver, collects via vanilla → HD stands down → nobody gathers) is precisely that blind spot; the cede is now keyed on adv_haul_all_ings alone. See the corrected gather-loop entry above.

---

## Family: `pathfinding`

Perfect Pathfinding (PP; real assembly/namespace "PerfectPathing", Mod class PerfectPathing.Mod, About name "Perfect Pathfinding (1.5 Update)", packageId jp.perfectpathing.updated) is a tiny mod: a [StaticConstructorOnStartup] HarmonyPatches that applies up to four patches to the vanilla pathfinder — a postfix on PathFinder.DetermineHeuristicStrength (force heuristic strength 1 for non-animals = "perfect"/no-zigzag long paths), and FindPath transpilers for a per-cell light factor, a per-cell weather factor, and optional dirt avoidance. CRITICAL ENGINE FACT (decompile-verified against this machine's RW1.6 Assembly-CSharp.dll): RW1.6 REWROTE the pathfinder into a Unity-Jobs system. Verse.AI.PathFinder NO LONGER EXISTS (moved to Verse.PathFinder); there is NO FindPath(IntVec3,LocalTargetInfo,TraverseParms,PathEndMode,PathFinderCostTuning) method and NO InitStatusesAndPushStartNode anchor anymore (PP's transpilers target dead code); DetermineHeuristicStrength still exists but moved namespace (Verse.AI->Verse) and is now static. PP ships ONLY 1.2-1.5 folders (no 1.6, no LoadFolders), supportedVersions lists only up to 1.5. CONSEQUENCE: on RW1.6 every PP AccessTools.Method("Verse.AI.PathFinder:...") returns null, so harmony.Patch(null,...) throws at PP's static ctor — PP applies NONE of its patches (it is effectively inert / fails to initialize). Therefore HD's pathfinding behavior under PP-on-1.6 is identical to pure vanilla. HD interacts with the pathfinder ONLY through the public Verse.PathFinder.FindPathNow entry point (decompile-verified signature matches HD's call sites exactly), never by patching it, so there is no signature/behavior-drift surface: if PP ever did apply (a future working 1.6 build), HD would simply inherit the more-accurate per-cell costs automatically — strictly fine. HD's PerfectPathfindingCompat is detection-ONLY (one informational log line; nothing branches on it on a gameplay path). Net risk to HD: NONE / LOW across the board. One inaccuracy worth noting: HD's type-name probe list does not include the real type (PerfectPathing.Mod), so detection relies entirely on the ModLister name fallback (which does match). All HD focus subsystems (EnRoutePickup.WithinPathCost/PathCost, TransportLoad NearestEligibleHybrid/PathCostTo, RoutePlanner.TryLeg) use the correct 1.6 FindPathNow signature, pool every PawnPath in a finally, and gate the expensive accurate/hybrid paths behind OFF-by-default settings (enRoutePathChecker=Vanilla, loadHybridPathing=false).

### Feature risk map

- `NONE` **Perfect Pathfinding — Heuristic Patch — forces the A* heuristic strength to 1 (full accuracy) for all non-animal pawns, removing vanilla's 'rushed' long-distance heuristic so distant paths don't zig-zag/beeline.**
  - Mechanism: [StaticConstructorOnStartup] HarmonyPatches.cs:22-23 — Postfix on AccessTools.Method("Verse.AI.PathFinder:DetermineHeuristicStrength"); HeuristicStrengthPatch.cs:15-19 sets ref __result=1f when Settings.heuristicPatch && pawn!=null && !pawn.RaceProps.Animal. Gated by Settings.heuristicPatch (default true).
  - HD overlap: HD's accurate path stages (EnRoutePickup.WithinPathCost, TransportLoad hybrid re-rank, RoutePlanner.TryLeg) all call map.pathFinder.FindPathNow, which internally calls DetermineHeuristicStrength — so HD would inherit any heuristic change automatically. No HD code reads or duplicates the heuristic.
  - Fix (general / no-op): No fix needed. (a) On RW1.6 PP cannot apply this patch at all: the target moved Verse.AI.PathFinder->Verse.PathFinder, so AccessTools.Method("Verse.AI.PathFinder:DetermineHeuristicStrength") returns null. (b) Even if it applied, HD inherits the result transparently via FindPathNow — a MORE accurate heuristic only makes HD's path-cost gates more accurate, never wrong. (c) The concern is moot on 1.6 regardless: decompiled Verse.PathFinder.DetermineHeuristicStrength ALREADY returns 1f for colonists (1.75f animals, scaled only for non-colonist humanlikes), so vanilla 1.6 already does the 'perfect' heuristic for player pawns. General pattern already in place: HD only ever consumes the public FindPathNow entry point, never patches/branches on the pathfinder internals — the correct way to be transparent to ANY pathfinder-modifying mod.
- `NONE` **Perfect Pathfinding — Lighting Patch — recomputes a pawn's movement cost PER CELL using that cell's glow (instead of vanilla caching speed once at the start), so dark tiles cost more during A*.**
  - Mechanism: HarmonyPatches.cs:25-32 — Transpiler EnvironmentPatch.Patch_FindPath on AccessTools.Method("Verse.AI.PathFinder:FindPath", [IntVec3,LocalTargetInfo,TraverseParms,PathEndMode,PathFinderCostTuning]); anchors on PathFinder.InitStatusesAndPushStartNode and rewrites the cardinal/diagonal TicksPerMove getters + the per-iteration cost local (#48) to call EnvironmentPatch.TicksPerMove_tile, which multiplies cost by a glow curve (LightEvalPoints 0->0.8,0.3->1.0) for friendly humans only. Gated by Settings.lightPatch (default true).
  - HD overlap: Same transparent inheritance via FindPathNow as the heuristic. HD never touches per-cell cost.
  - Fix (general / no-op): No fix needed and NOTHING to do on RW1.6. The transpiler's target FindPath(IntVec3,...) and its anchor InitStatusesAndPushStartNode were DELETED in the 1.6 Jobs-based pathfinder rewrite — decompiled Verse.PathFinder has only FindPathNow + a private PathFinderJob pipeline, no FindPath and no InitStatusesAndPushStartNode. PP's AccessTools.Method returns null -> harmony.Patch(null, transpiler) throws at PP's static ctor, so the patch never applies. If a future PP 1.6 ever re-implements this on the new Jobs pipeline, HD still inherits it cleanly through FindPathNow. (Historical note: PP already had to fix this exact stage once — see priorBugs, the GameGlowAt->GroundGlowAt 1.5 rename crash.)
- `NONE` **Perfect Pathfinding — Weather Patch — recomputes movement cost PER CELL by the current weather move-speed multiplier when the cell is unroofed (vanilla caches it once at the start).**
  - Mechanism: Shares the SAME transpiler/registration as the Lighting Patch (HarmonyPatches.cs:28-32 — the findPath transpiler is added when Settings.lightPatch||Settings.weatherPatch). EnvironmentPatch.TicksPerMove_tile (EnvironmentPatch.cs:150-153) multiplies cost by map.weatherManager.CurMoveSpeedMultiplier for spawned pawns on unroofed cells. Gated by Settings.weatherPatch (default true).
  - HD overlap: Transparent inheritance via FindPathNow; HD has no weather/per-cell cost logic.
  - Fix (general / no-op): No fix needed. Identical to the Lighting Patch: the FindPath transpiler target and InitStatusesAndPushStartNode anchor don't exist in RW1.6, so the patch cannot apply (PP static ctor would throw before it ever runs in 1.6). HD's transparent FindPathNow consumption is the correct general pattern.
- `NONE` **Perfect Pathfinding — Dirt Avoidance — adds a time penalty when stepping from a clean tile onto a filth-generating terrain tile, nudging pawns onto roads/floors (an OwlChemist 'Clean Pathfinding' style idea). Default OFF.**
  - Mechanism: HarmonyPatches.cs:34-38 — separate Transpiler DirtAvoid.Patch_FindPath on the SAME FindPath method; anchors on InitStatusesAndPushStartNode + TerrainDef.extraNonDraftedPerceivedPathCost field load, inserting DirtAvoid.AddCost (DirtAvoid.cs:75-94, adds Settings.dirtStrength*60 to cost on def.generatedFilth tiles). Gated by Settings.dirtAvoidance (default FALSE).
  - HD overlap: Transparent inheritance via FindPathNow; no HD overlap. Off by default in PP anyway.
  - Fix (general / no-op): No fix needed. Off by default in PP, and the FindPath transpiler target is gone on RW1.6 (cannot apply). If applied (hypothetical 1.6 build), HD's path-cost gates would just see slightly higher leg costs and remain correct (a few candidates near filth might fail the trip-ratio gate — a benign, intended effect).
- `LOW` **Perfect Pathfinding — Whole-mod application on RW1.6 — the [StaticConstructorOnStartup] static ctor runs at game start and patches the vanilla pathfinder.**
  - Mechanism: PerfectPathing.HarmonyPatches static ctor (HarmonyPatches.cs:16-44) calls AccessTools.Method on Verse.AI.PathFinder methods that were moved/deleted in 1.6, then harmony.Patch(...) on the (null) results. About.xml supportedVersions = 1.2..1.5 only; no 1.6 folder; no LoadFolders.xml; PP ships its 1.5 assembly as the highest-versioned build a forced-load would pick up.
  - HD overlap: Indirect: if PP errors at startup it could add red errors to the player's log alongside HD, but it does not affect HD's code paths. HD's behavior is identical to vanilla because PP applies no patches.
  - Fix (general / no-op): Nothing HD can or should do — this is PP's own version-support gap. Document in HD's COMPATIBILITY.md that Perfect Pathfinding has no RW1.6 build and is inert on 1.6 (its patches no-op / it may log a harmless startup error because the 1.6 pathfinder was rewritten). HD remains fully functional with vanilla pathfinding. This is informational, not a code change.
- `LOW` **Perfect Pathfinding (interaction surface) — PP detection by HD (informational only) — HD logs a line and gives the en-route feature a single place to reason about the dependency.**
  - Mechanism: HD PerfectPathfindingCompat.cs:40-72 Init() probes AccessTools.TypeByName for RimThreaded.PerfectPathfinding / PerfectPathfinding.PerfectPathfindingMod / PerfectPathfinding.Mod_PerfectPathfinding / PerfectPathfinding.PerfectPathfinding, then falls back to ModLister.AllInstalledMods name-contains "Perfect Pathfinding". IsActive is consumed only by a doc-comment in EnRoutePickup.cs:636 (no runtime branch).
  - HD overlap: PerfectPathfindingCompat itself; en-route accurate path stages (only via documentation, not behavior).
  - Fix (targeted patch): GENERAL/targeted minor accuracy fix (optional): HD's type-name list is WRONG for the real fork — the actual namespace/assembly is `PerfectPathing` and the Mod class is `PerfectPathing.Mod` (verified in PP source: RootNamespace/AssemblyName=PerfectPathing, Settings.cs:7 `class Mod : Verse.Mod`). None of the four probed type names match, so the type probe is dead code; detection currently works ONLY via the ModLister name fallback (About name 'Perfect Pathfinding (1.5 Update)' contains the substring). Since detection is purely informational and the fallback succeeds, this is harmless — but if HD wants the type probe to actually fire, add `PerfectPathing.Mod` (and keep the name fallback as the robust general path). No gameplay impact either way.
- `NONE` **Hauler's Dream (self, for-the-record) — HD's own FindPathNow consumers — EnRoutePickup accurate path stage (Default/Pathfinding), TransportLoad hybrid re-rank, RoutePlanner leg estimates.**
  - Mechanism: EnRoutePickup.cs:638-691 WithinPathCost/PathCost call map.pathFinder.FindPathNow(start,target,pawn,null,peMode); TransportLoad.cs:595-608 PathCostTo same idiom; RoutePlanner.cs:327 TryLeg same. All read PawnPath.Found/.TotalCost/.NodesReversed and ReleaseToPool() in a finally. Gated: enRoutePathChecker default Vanilla (region flood, no A*); loadHybridPathing default false.
  - HD overlap: This IS the HD subsystem under review.
  - Fix (general / no-op): No fix needed — verified correct for RW1.6. Verse.PathFinder.FindPathNow(IntVec3,LocalTargetInfo,Pawn,PathFinderCostTuning?,PathEndMode) signature exactly matches HD's calls (tuning is now nullable, so passing null/omitting is valid); the cell overload FindPathNow(IntVec3,LocalTargetInfo,...) also exists; Pawn_JobTracker.TryOpportunisticJob(Job,Job) (EnRoutePickup's postfix target) confirmed present in 1.6. The expensive A* paths are OFF by default (enRoutePathChecker=Vanilla / loadHybridPathing=false), so the perf-under-accurate-mode concern only arises when the user opts in — and even then it's bounded (top-N candidate budget 2..24, region-count short-circuits, all paths pooled). PP-active does not change any of this because PP can't patch the 1.6 pathfinder.

### Prior bugs learned from (their bug-fix history)

- ⚠️ applies to HD — **Lighting-patch crash on version bump: PP's EnvironmentPatch called map.glowGrid.GameGlowAt(...), a method that was renamed/removed in RW1.5 (replaced by GroundGlowAt). On 1.5 this threw while building/applying the assembly until fixed.** _(PP About.xml 'What was fixed' section (line 64): 'Fixed line 146 in EnvironmentPatch.cs ... it was calling a method that doesn't exist in game version 1.5, so I changed it to the correct method name.' Confirmed in current EnvironmentPatch.cs:146 which now uses GroundGlowAt with a comment noting the 1.5 rename.)_ — Same CLASS of bug HD must avoid: a HARDCODED vanilla API name that gets renamed/moved across a major version. This is exactly what bit PP again on 1.6 at a larger scale (Verse.AI.PathFinder -> Verse.PathFinder, FindPath/InitStatusesAndPushStartNode deleted). HD did NOT reintroduce it for pathfinding — all HD FindPathNow call sites use the correct 1.6 Verse.PathFinder signature (decompile-verified) and go through the stable PUBLIC entry point rather than transpiling private internals, which is the resilient pattern. The takeaway for HD's broader patch surface: prefer public-API calls + reflection probes with name fallbacks over hardcoded private-member transpilers, and re-verify every AccessTools.Method/Type target against the decompiled target version (HD already does this for FindPathNow).
- n/a to HD — **Hardcoded heuristic redundancy / behavioral overlap: PP's heuristic patch forces strength=1 for non-animals; vanilla RW1.6 already returns 1f for colonists, so the patch became largely a no-op for player pawns even where it could apply.** _(Decompiled Verse.PathFinder.DetermineHeuristicStrength (RW1.6 Assembly-CSharp.dll): returns 1f for colonists, 1.75f for animals, scaled only for non-colonist humanlikes. PP HeuristicStrengthPatch.cs:15-19.)_ — Not an HD-reintroducible bug — it's a PP feature that vanilla 1.6 partly absorbed. Relevant only as evidence that HD inheriting vanilla-1.6 pathfinding (with PP inert) already yields the accurate long-distance paths PP originally provided for colonists. Reinforces 'risk=none' for the heuristic concern.
- n/a to HD — **Hard incompatibility with Clean Pathfinding (Owlchemist.CleanPathfinding) — both transpile the same FindPath dirt-cost site, so they conflict.** _(PP About.xml <incompatibleWith>Owlchemist.CleanPathfinding</incompatibleWith> (line 23-25) + 'What was fixed' line 65; HarmonyPatches.cs:40-43 even probes for CleanPathfinding.Mod_CleanPathfinding.)_ — This is a PP<->CleanPathfinding transpiler clash, not involving HD. HD does not transpile FindPath (or any pathfinder method) at all — it only calls the public FindPathNow — so HD cannot participate in this class of transpiler conflict. Confirms HD's read-only consumption pattern keeps it out of pathfinder transpiler wars entirely.

### Top risks
- Perfect Pathfinding has NO RimWorld 1.6 build (ships only 1.2-1.5; About supportedVersions stops at 1.5). RW1.6 rewrote the pathfinder into a Unity-Jobs system: Verse.AI.PathFinder no longer exists (now Verse.PathFinder), and FindPath + InitStatusesAndPushStartNode (PP's transpiler anchors) are deleted. So on 1.6 EVERY PP patch targets a null method and harmony.Patch(null,...) throws at PP's static ctor — PP is effectively inert (and may log a startup error). Net effect on HD: HD's pathfinding behaves exactly like vanilla. Risk to HD = LOW/NONE.
- HD never patches the pathfinder; it only consumes the public Verse.PathFinder.FindPathNow entry point (signature decompile-verified to match HD's call sites in EnRoutePickup, TransportLoad, RoutePlanner). So there is NO signature/behavior-drift surface and NO transpiler-conflict surface with PP (or any pathfinder mod). If a working PP 1.6 ever shipped, HD would transparently inherit its more-accurate per-cell costs — strictly fine.
- HD's PerfectPathfindingCompat type-name probe list is inaccurate for this fork (real type is PerfectPathing.Mod, namespace/assembly 'PerfectPathing', not any of the four 'PerfectPathfinding.*' names HD probes). Detection therefore works ONLY via the ModLister name-contains fallback (which does succeed). Because detection is informational-only (no gameplay branch reads IsActive), impact is none — but the type probe is dead code.
- The only behavior-changing HD pathfinding paths (en-route accurate checker = Default/Pathfinding, and loadHybridPathing) are OFF by default (enRoutePathChecker=Vanilla, loadHybridPathing=false) and bounded (top-N candidate budget, region-count pre-gate, all PawnPaths pooled). The hypothesized 'perf of HD pathfinding under PP accurate mode' risk does not materialize: PP can't enable accurate mode on 1.6, and HD's own accurate modes are opt-in + bounded.

### General-pattern recommendations
- READ-ONLY pathfinder consumption (already implemented in HD): interact with the pathfinder ONLY through the public, stable Verse.PathFinder.FindPathNow entry point — never transpile or branch on its internals. This makes HD automatically transparent to ANY mod that modifies pathfinding accuracy/cost (Perfect Pathfinding, Clean Pathfinding, etc.): HD inherits their effect for free and can never participate in a pathfinder transpiler conflict. No change needed; keep this invariant.
- Reflection probe with a ModLister NAME fallback (already in HD's *Compat shims): when detecting an optional mod, don't rely solely on hardcoded type names (they drift across forks/renames — e.g. PerfectPathing vs PerfectPathfinding). Keep the ModLister.AllInstalledMods name-contains fallback as the robust path; treat the type probe as a best-effort fast path. HD already does this — the fallback is what makes PP detection succeed despite the wrong type names.
- Gate any HD feature that issues real A* pathfinding (FindPathNow) behind an OFF-by-default setting with a bounded candidate budget and always ReleaseToPool() the PawnPath in a finally (already done in EnRoutePickup/TransportLoad/RoutePlanner). This keeps the worst-case pathfinding cost opt-in and bounded regardless of which pathfinding mod is installed.
- Re-verify every hardcoded vanilla API target (AccessTools.Method/Type, FindPathNow signature) against the DECOMPILED target game version on each major bump — the PP GameGlowAt->GroundGlowAt (1.5) and Verse.AI.PathFinder->Verse.PathFinder + deleted FindPath (1.6) renames are the textbook failure mode. HD's FindPathNow calls were verified correct for 1.6 here.

### Targeted patches (only where a general pattern can't cover it)
- OPTIONAL accuracy-only tweak (not required for compat): in HD PerfectPathfindingCompat.cs Init(), add `?? AccessTools.TypeByName("PerfectPathing.Mod")` to the type-probe chain so the fast-path type detection actually matches this fork (current names PerfectPathfinding.* never match the real `PerfectPathing` namespace). Harmless to skip — the ModLister name fallback already detects PP — and IsActive is informational-only, so this changes no gameplay behavior; it only makes the (currently dead) type probe live. Set targeted=true because it is a PP-fork-specific string.

### Adversarial verification
**Confirmed real:**
- ENGINE FACT (decompile-verified, Krafs.Rimworld.Ref 1.6.4518 via Mono.Cecil): Verse.AI.PathFinder is MISSING; the type moved to Verse.PathFinder. So PP/HarmonyPatches.cs:22 AccessTools.Method("Verse.AI.PathFinder:DetermineHeuristicStrength") and :26 AccessTools.Method("Verse.AI.PathFinder:FindPath", arguments) both return null on RW1.6. Confirmed.
- ENGINE FACT (decompile-verified): No FindPath(IntVec3,LocalTargetInfo,TraverseParms,PathEndMode,PathFinderCostTuning) method exists anywhere in 1.6 (Verse.PathFinder has only FindPathNow + a Jobs pipeline: PathFinderTick/ScheduleGridJobs/EmitPath/etc.), and 'InitStatusesAndPushStartNode' appears in ZERO types. So PP's EnvironmentPatch (HarmonyPatches.cs:30) and DirtAvoid (line 36) transpilers target deleted code and their AccessTools.Method anchors (EnvironmentPatch.cs:26 typeof(PathFinder).InitStatusesAndPushStartNode) cannot resolve. Confirmed.
- ENGINE FACT (decompile-verified): DetermineHeuristicStrength EXISTS in 1.6, is now STATIC, and moved namespace Verse.AI->Verse (Verse.PathFinder.DetermineHeuristicStrength(Pawn,IntVec3,LocalTargetInfo)->float). Matches the map exactly.
- LOAD-BEARING CLAIM PROVEN by a LIVE Harmony 2.3.6 runtime test (not just reasoning): AccessTools.Method("Verse.AI.PathFinder:DetermineHeuristicStrength") returns null, and harmony.Patch(null, postfix:) THROWS System.NullReferenceException 'Null method for <id>' (same for transpiler:). Therefore PP's [StaticConstructorOnStartup] HarmonyPatches static ctor throws at the FIRST patch (line 23, the heuristic patch) BEFORE reaching any of the gated FindPath patches -> PP applies NONE of its patches -> PP is effectively inert on RW1.6. HD's pathfinding behavior under PP-on-1.6 is therefore identical to pure vanilla. Confirmed. (Nuance: a throwing StaticConstructorOnStartup is caught by RimWorld's startup loop and logged as a red error, so PP does not hard-crash the game; it just no-ops + logs.)
- PP ships ONLY 1.2/1.3/1.4/1.5 folders (each with Assemblies/PerfectPathing.dll), NO 1.6 folder, NO LoadFolders.xml; About.xml supportedVersions stops at 1.5; name='Perfect Pathfinding (1.5 Update)', packageId=jp.perfectpathing.updated. Confirmed by directory listing + About.xml read.
- PP real assembly/namespace is 'PerfectPathing' (Source/PerfectPathing/, AssemblyInfo AssemblyProduct="PerfectPathing") and the Mod class is PerfectPathing.Mod (Settings.cs:7 `public class Mod : Verse.Mod`). HD's PerfectPathfindingCompat.cs:47-50 type-probe list (RimThreaded.PerfectPathfinding, PerfectPathfinding.PerfectPathfindingMod, PerfectPathfinding.Mod_PerfectPathfinding, PerfectPathfinding.PerfectPathfinding) contains NONE of the real names -> the type probe is dead code; detection works ONLY via the ModLister.AllInstalledMods name-contains 'Perfect Pathfinding' fallback (PerfectPathfindingCompat.cs:53-64), which DOES match the About name. Confirmed. Impact is none because detection is informational-only (see next).
- HD's PerfectPathfindingCompat.IsActive is consumed on NO runtime gameplay path: a repo-wide grep finds it referenced only in a <see cref> doc-comment at EnRoutePickup.cs:636 (and the class's own declaration). No code branches on it. Confirmed dead/informational-only -> the wrong type-probe names cause zero gameplay impact.
- HD interacts with the pathfinder ONLY via the public Verse.PathFinder.FindPathNow entry point, never by patching it. A grep for HarmonyPatch/Transpiler targeting PathFinder/FindPath/InitStatusesAndPushStartNode/DetermineHeuristic in HD/Source returns EMPTY; HD's 60+ HarmonyPatch targets are all haul/job/bill/transport types, none touching the pathfinder. So HD has zero transpiler-conflict surface with PP or Clean Pathfinding. Confirmed.
- All three HD FindPathNow call sites match the real 1.6 signature exactly: EnRoutePickup.cs:666 & :682 FindPathNow(start,dest,pawn,null,peMode); TransportLoad.cs:599 FindPathNow(start,destThing,pawn,null,ClosestTouch); RoutePlanner.cs:327 FindPathNow(from,target,pawn,peMode:peMode). Decompile confirms FindPathNow(IntVec3,LocalTargetInfo,Pawn,Nullable<PathFinderCostTuning> tuning [optional,default=null],PathEndMode peMode [optional,default=Touch]) -> PawnPath; tuning/peMode being optional makes the named-arg RoutePlanner form resolve unambiguously (3rd arg Pawn disambiguates from the TraverseParms overload). Map.pathFinder is public Verse.PathFinder; PawnPath.Found/.TotalCost/.NodesReversed/.ReleaseToPool all exist. EMPIRICAL PROOF: HD builds GREEN (0 warnings/0 errors) against Krafs.Rimworld.Ref 1.6.4518. Confirmed.
- HD's expensive accurate-A* pathfinding stages are OFF by default: enRoutePathChecker = EnRoutePathChecker.Vanilla (HaulersDreamSettings.cs:283, Scribe default :512, ResetToDefaults :653) and loadHybridPathing = false (:200, :481, :622). All FindPathNow paths pool PawnPath in a finally (EnRoutePickup PathCost, TransportLoad PathCostTo, RoutePlanner TryLeg). Confirmed -> the hypothesized 'perf of HD pathfinding under PP accurate mode' risk is doubly moot (PP can't enable accurate mode on 1.6, and HD's accurate modes are opt-in).
- Pawn_JobTracker.TryOpportunisticJob(Job,Job)->Job (EnRoutePickup's postfix target, EnRoutePickup.cs:63) confirmed present in 1.6.4518. The prior-bug GameGlowAt->GroundGlowAt rename is also real (Verse.GlowGrid.GameGlowAt MISSING / GroundGlowAt present), matching PP's About.xml 'what was fixed' note and EnvironmentPatch.cs:146 — this is exactly the hardcoded-vanilla-API-name-drift bug class that bit PP again on 1.6 at the PathFinder level; HD did NOT reintroduce it (FindPathNow verified). Confirmed.
- PP is a code-only mod (only Assemblies + About.xml; NO Defs, NO XML Patches dirs). Its ENTIRE compat surface is the 4 Harmony patches already verified inert on 1.6. No hidden non-pathfinder overlap with HD exists. Confirmed.

**Refuted (not real / already handled):**
- REFUTED/NUANCED: the map calls PP's heuristic patch 'gated by Settings.heuristicPatch (default true)' and implies the heuristic patch is the one that would otherwise apply. Reading HarmonyPatches.cs, the heuristic patch at line 22-23 is NOT gated by any Settings check — it is applied UNCONDITIONALLY (the Settings.lightPatch/weatherPatch/dirtAvoidance gates are only on the LATER FindPath patches at lines 28-38). The HeuristicStrengthPatch BODY checks Settings.heuristicPatch (HeuristicStrengthPatch.cs:17), but the Patch() CALL itself is unconditional. This strengthens (not weakens) the 'PP is inert' conclusion: on 1.6 the very first, ungated harmony.Patch(null,...) at line 23 throws, so PP no-ops regardless of any setting. The map's per-feature 'gated by Settings (default true/false)' framing is about the body guard, not the patch application; minor mischaracterization, conclusion unchanged.
- REFUTED as a present-state claim: the map's recommended 'fix' for the whole-mod-application risk says to 'Document in HD's COMPATIBILITY.md that Perfect Pathfinding has no RW1.6 build and is inert.' Verified that HD's COMPATIBILITY.md currently contains NO mention of Perfect Pathfinding (grep returns empty). So this is an unaddressed (but purely informational) documentation gap, not an existing safeguard — and it is not a code risk to HD either way. The 'risk' itself (PP logging a startup error alongside HD) is real-but-low and outside HD's control, exactly as the map states.

**Missed by the first pass (folded into the plan):**
- MISSED (minor, low/none risk): HD's PerfectPathfindingCompat name-fallback matches ANY installed mod whose Name contains 'Perfect Pathfinding' case-insensitively (PerfectPathfindingCompat.cs:58-59), and the type-probe names are all wrong for the real fork. So if a DIFFERENT/renamed Perfect Pathfinding fork (or a future working 1.6 build with a different type) is installed, HD's IsActive could mis-detect or fail to fast-path — but since IsActive feeds nothing but one log line, the practical impact is zero. Worth noting only as confirmation the map's 'add PerfectPathing.Mod to the probe' is the right (optional) accuracy tweak; even that is cosmetic.
- MISSED (theoretical, does not materialize on 1.6): IF a future working PP-1.6 reimplements the lighting/weather/dirt patches on the new Jobs pipeline, those patches make per-cell movement cost HIGHER on dark/rainy/filthy cells, which directly raises PawnPath.TotalCost. HD's EnRoutePickup.WithinPathCost (EnRoutePickup.cs:638-658) and RoutePlanner gate on TotalCost RATIOS against MaxNewLegs/MaxTotalTrip thresholds. Higher absolute leg costs that scale roughly uniformly cancel in the ratios, but a localized cost spike on one leg (e.g. a dark thing->store leg) could push a candidate past the trip-ratio gate and REJECT an otherwise-valid en-route pickup. This is a benign/intended accuracy effect (the map flags the analogous dirt case), only reachable when the user opts into accurate mode (default Vanilla) AND a working PP-1.6 exists (none does). Not a real risk today; flagged for completeness so a future PP-1.6 retest re-checks ratio-gate sensitivity rather than assuming 'strictly fine'.
- MISSED (out of scope but worth recording): the map asserts vanilla 1.6 DetermineHeuristicStrength 'returns 1f for colonists, 1.75f animals, scaled for non-colonist humanlikes' as decompiled fact. I confirmed the method EXISTS/static/moved namespace but did NOT decompile its BODY (Cecil method-body read not performed), so the specific return-value claim is unverified here. It is not load-bearing for any HD risk (HD never reads the heuristic), so this does not change any verdict — noting only that this one sub-claim in the map rests on the prior investigator's body decompile, not re-verified in this pass.

**Verdict:** The investigator's risk map is SUBSTANTIALLY CORRECT and its central conclusion is fully verified by independent decompilation of the real RW1.6.4518 reference DLL (Mono.Cecil) and a LIVE Harmony 2.3.6 runtime test — not just reasoning. Net risk to HD from Perfect Pathfinding = NONE/LOW, exactly as claimed. Proven facts: (1) Verse.AI.PathFinder is gone (moved to Verse.PathFinder) and FindPath/InitStatusesAndPushStartNode are deleted, so all four PP AccessTools.Method targets resolve to null on 1.6; (2) harmony.Patch(null,...) THROWS NullReferenceException, so PP's [StaticConstructorOnStartup] ctor dies at its first (ungated) patch and applies NOTHING -> PP is inert and HD behaves as pure vanilla; (3) HD never patches the pathfinder (read-only FindPathNow), so there is zero signature-drift and zero transpiler-conflict surface; (4) all three HD FindPathNow call sites match the verified 1.6 signature and HD builds GREEN against 1.6.4518; (5) the accurate-A* stages are OFF by default; (6) HD's PerfectPathfindingCompat type-probe names are wrong for the real 'PerfectPathing.Mod' fork but detection is informational-only (consumed in a single doc-comment, no runtime branch), so zero gameplay impact. Two minor mischaracterizations corrected: PP's heuristic patch is applied UNCONDITIONALLY (not Settings-gated at the Patch() call), which only strengthens 'PP inert'; and HD's COMPATIBILITY.md currently has no PP entry (the map's documentation 'fix' is unaddressed but is not a code risk). No genuinely new HIGH/MEDIUM risk was missed; the only additions are a theoretical ratio-gate sensitivity that would matter ONLY if a working PP-1.6 ever ships (none exists) and the user opts into accurate mode, plus the note that the vanilla DetermineHeuristicStrength BODY return-value sub-claim was not re-decompiled (and is not load-bearing). The single concrete, optional code tweak the map suggests (add `PerfectPathing.Mod` to the type-probe chain) remains valid but cosmetic.

---

## Family: `vehicles`

HD's VF compat surface is reflection-only (8 AccessTools.TypeByName sites; no `using Vehicles`, no hard refs, no Vehicles.* type in any signature) and degrade-SAFE (VehicleFrameworkCompat.IsActive is true only when AddOrTransfer + GetStatValue(VehicleStatDef) + VehicleStatDefOf.CargoCapacity + cargoToLoad all bind; a partial drift reports inactive → HD behaves as without VF). Every reflection target in the shim was RE-VERIFIED against the cloned VF 1.6.2384 source and all still resolve unchanged: VehiclePawn:Pawn (no inventory/IThingHolder override → MassUtility.* and inventory.innerContainer need no reflection); VehiclePawn.AddOrTransfer(Thing,int) at VehiclePawn_Caravan.cs:68 (decrements cargoToLoad by the PASSED count via TransferableMatchingDesperate(thing,cargoToLoad,PodsOrCaravanPacking) at :88-91 — HD's MF1 RemainingDemandForThing clamp mirrors this EXACTLY); cargoToLoad public field at :19; Ext_Vehicles.InVehicle(Thing) at Ext_Vehicles.cs:1017; VehicleStatDefOf.CargoCapacity + GetStatValue(VehicleStatDef); VehicleReservationManager.Reserve<LocalTargetInfo,VehicleTargetReservation>(VehiclePawn,Pawn,Job,T1) at VehicleReservationManager.cs:32 + ReleaseAllClaimedBy(Pawn) at :95. The three headline risks are ALL real in VF and ALL already handled by HD guards: (1) the generic-base code-sharing — WorkGiver_PackVehicle:WorkGiver_CarryToVehicle<TransferableOneWay> shares JobOnThing's JIT-native code with siblings WorkGiver_BringUpgradeMaterial and WorkGiver_RefuelVehicleTurret (both <ThingDefCountClass>, both target a vehicle; RefuelVehicleTurret even calls base.JobOnThing) → HD's IsInstanceOfType(__instance) guard (Patch_WorkGiver_PackVehicle_Redirect.cs:73) is present, correct, and load-bearing (without it a refuel/upgrade job would be hijacked into a cargo bulk-load); (2) MassUtility.Capacity/CanEverCarryAnything are globally prefixed for VehiclePawn (Patch_CaravanHandling.cs:319/339) and GiveToPackAnimalUtility.UsablePackAnimalWithTheMostFreeSpace is prefixed to return a VehiclePawn whenever SpawnedPawnsInFaction(faction).HasVehicle() (Patch_CaravanFormation.cs:76-85) → HD's FindCarrier can receive a vehicle, but JobDriver_LoadPackAnimal's [SF4] branch deposits via AddOrTransfer when the carrier IsVehicle (event-correct), and HD never runs its mass/overload logic with a vehicle as the courier pawn; (3) cargoToLoad over-load — HD's explicit bulk-load (JobDriver_LoadVehicleInBulk) clamps to the SINGLE matched transferable (RemainingDemandForThing), never a def-sum, matching VF's decrement. No high-risk gaps found. VF's depth-1 clone blocks git prior-bug mining; the UpdateLog is current-version only.

### Feature risk map

- `LOW` **Vehicle Framework — VehiclePawn : Pawn (core type subclasses vanilla Pawn) — every VehiclePawn is a spawned player-faction Pawn with a normal inventory tracker, RaceProps, and CurJob, so it leaks into any HD per-pawn enumeration (SpawnedPawnsInFaction, holder loops)**
  - Mechanism: VehiclePawn.cs:16 `public partial class VehiclePawn : Pawn, ...`; does NOT override inventory / IThingHolder (confirmed) so ((Pawn)vehicle).inventory.innerContainer and MassUtility.* work polymorphically with no reflection
  - HD overlap: All per-pawn share/divert loops: CarriedHaulShare (SpawnedPawnsInFaction at CarriedHaulShare.cs:99,145), EatFromInventory holder loop, OrganicInventoryShare, InventoryShare.IsEligibleCarrier/IsLiveShareCarrier, PawnMassCache, OverloadGate
  - Fix (general / no-op): GENERAL pattern already in place and correct by construction: (a) EatFromInventory.cs:101 and OrganicInventoryShare.cs:62 skip a holder whose VehicleFrameworkCompat.InVehicle(holder)==true (embarked passenger/cargo — unreachable inventory); (b) a PARKED vehicle correctly PASSES IsLiveShareCarrier (spawned/alive/not drafted/not mental) and IsEligibleCarrier (CurJobDef not an HD preloaded-stock def) so eat-from / build-from its cargo is DESIRABLE and works unchanged; (c) CarriedHaulShare reads each pawn's StorageBoundCarried (needs an active storage-bound haul CurJob) — a parked vehicle never has one, so it is harmlessly skipped (no explicit IsVehicle needed). The general rule (skip embarked holders via InVehicle; rely on liveness+CurJob predicates that a vehicle naturally fails) covers ANY mod whose carrier type subclasses Pawn. No change needed.
- `MEDIUM` **Vehicle Framework — Autonomous cargo loading via WorkGiver_PackVehicle (single-stack 'one item in hands' pack job from a Hauling work-scan), built in the GENERIC base WorkGiver_CarryToVehicle<T>.JobOnThing**
  - Mechanism: WorkGiver_PackVehicle : WorkGiver_CarryToVehicle<TransferableOneWay> (WorkGiver_PackVehicle.cs:9); JobOnThing in the closed generic base (WorkGiver_CarryToVehicle.cs:34) → JobMaker.MakeJob(LoadVehicle, thing, vehicle); JobAvailable requires cargoToLoad.Count!=0 && !IsOverEncumbered(vehicle) (:11-17). For reference-type T the JIT shares ONE native code body across WorkGiver_PackVehicle<TransferableOneWay>, WorkGiver_BringUpgradeMaterial<ThingDefCountClass>, WorkGiver_RefuelVehicleTurret<ThingDefCountClass> — all targeting a vehicle; RefuelVehicleTurret.JobOnThing even calls base.JobOnThing (:24)
  - HD overlap: Patch_WorkGiver_PackVehicle_Redirect (postfix on the base JobOnThing); JobDriver_LoadVehicleInBulk; VehicleLoadTarget; TransportLoad.TryGiveBulkJob/HasPotentialBulkWork; the shared LoadLedger
  - Fix (targeted patch): GENERAL anti-conflict idiom already correct: the postfix lets VF do ALL eligibility and only UPGRADES VF's already-issued single-stack job to HD's one-trip bulk load (never starves it — fail-open at every step). The generic-base code-sharing trap is defended by the @__instance guard `if (PackVehicleType==null || !PackVehicleType.IsInstanceOfType(__instance)) return;` (Patch_WorkGiver_PackVehicle_Redirect.cs:73) — VERIFIED present, correct, and necessary against current VF (the two sibling <ThingDefCountClass> workgivers exist and target vehicles). This generic-base-IsInstanceOfType(__instance) guard is the reusable pattern for ANY Harmony patch on a method inherited from a generic base with reference-type instantiations. No change needed.
- `LOW` **Vehicle Framework — VF globally patches MassUtility.Capacity → returns CargoCapacity stat for a VehiclePawn, and MassUtility.CanEverCarryAnything → true for a VehiclePawn (so vehicles count as carriers without being PackAnimal/ToolUser)**
  - Mechanism: Patch_CaravanHandling.cs:48-54,76-78 patch MassUtility.Capacity (prefix CapacityOfVehicle :319-334 → vehicle.GetStatValue(VehicleStatDefOf.CargoCapacity)) and MassUtility.CanEverCarryAnything (prefix CanCarryIfVehicle :339-345 → __result = p is VehiclePawn)
  - HD overlap: HD calls MassUtility.Capacity / GearAndInventoryMass on the COURIER pawn: PackAnimalLoad.CeilingKg (:374), TryBuildBulkLoadJob (:269), JobDriver_LoadPackAnimal sweep (:85,127), PawnMassCache, OverloadGate.CountToPickUp (:73), VehicleLoadTarget.GetMassUsage (:112)
  - Fix (general / no-op): No change needed — HD only ever passes the COURIER (a colonist) to its Capacity/GearAndInventoryMass calls, never a vehicle. The ONE intentional vehicle read is VehicleLoadTarget.GetMassUsage = MassUtility.GearAndInventoryMass(vehicle) (:112), which CORRECTLY relies on this VF patch to match VF's own Dialog_LoadCargo.MassUsage. As a defensive backstop, OverloadGate.NoOverloadFor keys on RaceProps.Humanlike/IsMechanoid (:54-58) — a VehiclePawn is neither, so even if a vehicle reached the overload path it would stand down to the plain limit (no misfire). The general rule: don't feed a foreign carrier-pawn type into courier-mass math; HD doesn't.
- `LOW` **Vehicle Framework — VF prefixes GiveToPackAnimalUtility.UsablePackAnimalWithTheMostFreeSpace to return a VEHICLE (not a pack animal) whenever any player vehicle is spawned (or a vehicle caravan is forming)**
  - Mechanism: Patch_CaravanFormation.cs:28-32 patches UsablePackAnimalWithTheMostFreeSpace; prefix UsableVehicleWithMostFreeSpace (:76-85) returns CaravanHelper.UsableVehicleWithTheMostFreeSpace(pawn) (:585-607, returns a VehiclePawn) when IsFormingCaravanShipHelper(pawn) || SpawnedPawnsInFaction(faction).HasVehicle()
  - HD overlap: PackAnimalLoad.FindCarrier (:27) → MaybeAutoDivert, MaybeAutoDeliverYields, ShouldRedirectGiveToPackAnimal, BuildRedirectJob, TryBuildBulkLoadJob, QueueDepositOnly; JobDriver_LoadPackAnimal.findCarrier toil (:165)
  - Fix (general / no-op): No change needed — HD ALREADY handles a vehicle carrier in JobDriver_LoadPackAnimal: the [SF4] branch (:210-236) deposits via VehicleFrameworkCompat.AddOrTransfer (event-correct: fires CargoAdded + decrements cargoToLoad) when the carrier IsVehicle && enableVehicleFramework, with a raw-transfer fallback when VF is absent/unbound. Note the DELIBERATE [SF4] divergence: here `count` is mass/free-space-clamped (NOT demand-clamped like MF1), so AddOrTransfer may drive a cargoToLoad entry negative→removed — this is the intended auto-pack-onto-vehicle behavior and is documented. The fix is the general pattern 'when a foreign mod hands you its carrier type, deposit through that mod's event-correct API not a raw container move'.
- `LOW` **Vehicle Framework — cargoToLoad pending-load manifest (the at-base 'Load cargo' dialog list of TransferableOneWay) is decremented inside AddOrTransfer by the PASSED count, and the matching entry is found via TransferableMatchingDesperate(thing,cargoToLoad,PodsOrCaravanPacking)**
  - Mechanism: VehiclePawn_Caravan.cs:88-96 — TransferableMatchingDesperate(thing,cargoToLoad,PodsOrCaravanPacking) then CountToTransfer -= count, Remove when <=0. NOTE: decrement uses the passed `count`, not the moved `result` (:79 result=TryAddOrTransfer)
  - HD overlap: JobDriver_LoadVehicleInBulk.DepositOne (:88-144) + VehicleFrameworkCompat.RemainingDemandForThing (:233-248) + VehicleLoadPlanPolicy.DepositUnits; VehicleLoadTarget.GetTransferables/AnythingToLoad
  - Fix (general / no-op): No change needed — HD's MF1 clamp is CORRECT: RemainingDemandForThing uses the IDENTICAL matcher TransferableMatchingDesperate(...,PodsOrCaravanPacking) VF uses (VehicleFrameworkCompat.cs:246), so DepositUnits = min(surplus, that-single-transferable's-CountToTransfer) lines up exactly with VF's decrement and can never over-load a def. Residual VF-side caveat to flag (NOT an HD bug): VF decrements by passed `count` not moved `result`, so if TryAddOrTransfer ever moved fewer than count (vehicle inner container is uncapped, so in practice result==count) the manifest would over-decrement; HD's clamp is right given the uncapped container. The general pattern: clamp a foreign-manifest deposit to the SINGLE matched transferable using the foreign mod's own matcher, never a def-sum.
- `MEDIUM` **Vehicle Framework — Right-click 'Unload' / cargo ITab management of a vehicle's inventory (VF owns the vehicle's cargo; HD's bulk-unload-carrier must never claim it)**
  - Mechanism: ITab_Vehicle_Cargo + VF's own unload flow manage ((Pawn)vehicle).inventory; HD's WorkGiver_UnloadCarriers / bulk-unload float menu would otherwise treat any inventory-bearing Pawn (incl. a VehiclePawn) as an unloadable carrier
  - HD overlap: Patch_WorkGiver_UnloadCarriers ([UC1] guard, :69-72), FloatMenuOptionProvider_BulkUnloadCarrier ([UC2] guard, :45-48), JobDriver_UnloadCarrierInBulk
  - Fix (general / no-op): No change needed — HD's [UC1]/[UC2] guards (`if (VehicleFrameworkCompat.IsVehicle(carrier)) return false/continue;`) defense-in-depth skip a VehiclePawn in BOTH the WorkGiver JobOnThing path and the float-menu provider, leaving vehicle cargo entirely to VF. Both are gated on IsVehicle ONLY (IsVehicle returns false when VF absent) so they are pure safety fixes that cannot be toggled off by enableVehicleFramework. This is the general 'never bulk-unload a foreign carrier type the owning mod manages' pattern.
- `NONE` **Vehicle Framework — VF's own selected-vehicle float menu (Order vehicle / Carry pawn to vehicle / Load passengers, etc.) which BLOCKS all non-VF FloatMenuOptionProvider.Applies when a vehicle is the SELECTED actor**
  - Mechanism: FloatMenuOptionProvider_Vehicle (FloatMenuOptionProvider_Vehicle.cs:8) — Drafted=true/Undrafted=false/MechanoidCanDo=false, SelectedPawnValid(Pawn,FloatMenuContext) gates on the SELECTED pawn being a vehicle
  - HD overlap: FloatMenuOptionProvider_BulkLoadVehicle (HD's 'Prioritize bulk loading {vehicle}' option), FloatMenuOptionProvider_PickUpIntoInventory, FloatMenuOptionProvider_BulkLoadTransporter/PackAnimal
  - Fix (general / no-op): No change needed — HD's vehicle float menu fires when the SELECTED actor is a COLONIST clicking a vehicle (FloatMenuOptionProvider_BulkLoadVehicle.cs:34-101 reads context.FirstSelectedPawn = the colonist), so it never collides with VF's selected-VEHICLE menu. HD also avoids double-ordering (skips if pawn is already mid HaulersDream_LoadVehicleInBulk for the same vehicle, :68) and stands down under the LoadAndEnterTransporters boarding lord (:53). VF loads cargo via a Hauling work-SCAN (not a single-stack right-click), so there is no VF right-click option to suppress — the scan is upgraded by Patch_WorkGiver_PackVehicle_Redirect instead.
- `LOW` **Vehicle Framework — VF's belt-and-suspenders reservation system (VehicleReservationManager : MapComponent, separate from Map.reservationManager) used to claim a vehicle as a load target**
  - Mechanism: VehicleReservationManager.cs:32 Reserve<T1,T2>(VehiclePawn,Pawn,Job,T1) where T2:Reservation<T1>; :95 ReleaseAllClaimedBy(Pawn); VehicleTargetReservation : Reservation<LocalTargetInfo>; VerifyAndValidateClaimants self-heals stale claims
  - HD overlap: VehicleFrameworkCompat.ReserveVehicle/ReleaseVehicleClaims (:272-322); JobDriver_LoadVehicleInBulk.OnExtraClaim/OnReleaseExtraClaims (:68-79)
  - Fix (targeted patch): No change needed — VERIFIED the generic Reserve signature (2 type args, 4 params, constraint T2:Reservation<T1>) matches the shim's MakeGenericMethod(LocalTargetInfo, VehicleTargetReservation) + invoke {vehicle,pawn,job,(LocalTargetInfo)vehicle}. This VRM claim is explicitly NON-load-bearing (the authoritative stand-down is the WorkGiver JobOnThing redirect) and is the ONE permitted fail-open swallow in HD (ReserveVehicle catches + WarnOnce + returns true). It does NOT gate IsActive, so a VRM drift cannot disable the loader. Correct as is.
- `NONE` **Vehicle Framework — VehiclesShouldntUnloadEverything — VF forces Pawn_InventoryTracker.UnloadEverything=false for a VehiclePawn (a vehicle never auto-dumps its cargo)**
  - Mechanism: Patch_CaravanFormation.cs:43-47 prefixes Pawn_InventoryTracker.UnloadEverything setter (VehiclesShouldntUnloadEverything :168)
  - HD overlap: None directly — HD's unload paths target colonists/animals, not vehicle inventory (and [UC1]/[UC2] already exclude vehicles from bulk-unload)
  - Fix (general / no-op): No change needed. HD never sets UnloadEverything on a vehicle nor relies on a vehicle auto-dumping; the [UC1]/[UC2] guards already keep HD out of vehicle inventory management entirely.
- `LOW` **Vehicle Framework — Aerial / launchable vehicles — a vehicle can despawn or aerial-launch mid-trip while a colonist is loading it**
  - Mechanism: CompVehicleLauncher (Comps/Flyer/CompVehicleLauncher.cs) reads CargoCapacity; aerial vehicles use the SAME cargoToLoad/inventory/WorkGiver path; a launch DeSpawns the vehicle
  - HD overlap: JobDriver_LoadVehicleInBulk (deposit target may vanish), VehicleLoadTarget (caches Map because the vehicle may despawn)
  - Fix (general / no-op): No change needed — JobDriver_LoadVehicleInBulk uses FailOnDespawnedOrNull on the vehicle and FindTargetStillValid checks vehicle!=null && vehicle.Spawned (:81-85); on any non-Success end the finish action releases the VRM+ledger claim and SALVAGES the carried split back into inventory (re-tagged, rides HD's normal unload — never dropped on a temp map). VehicleLoadTarget caches the Map for exactly this despawn case. The general 'fail-on-despawn + salvage-not-drop' pattern covers any disappearing deposit target.
- `MEDIUM` **Vehicle Framework — VF's JobDriver_GiveItemToVehicle / WorkGiver_RefuelVehicleTurret (auto-load turret ammo into vehicle cargo) — a DIFFERENT load job competing for the same vehicle cargo space**
  - Mechanism: JobDriver_GiveItemToVehicle.cs:29 FindThingDefsToPack uses CargoCapacity - InventoryMass(vehicle) headroom; WorkGiver_RefuelVehicleTurret : WorkGiver_CarryToVehicle<ThingDefCountClass> shares the generic JobOnThing
  - HD overlap: Patch_WorkGiver_PackVehicle_Redirect (must NOT hijack the refuel/turret-ammo job)
  - Fix (targeted patch): No change needed — this is exactly the sibling-instantiation the @__instance IsInstanceOfType guard defends against (covered above). HD's postfix returns early unless __instance is genuinely a WorkGiver_PackVehicle, so the turret-ammo / upgrade-material jobs are left untouched. The general generic-base guard is the fix.
- `LOW` **Vehicle Framework — Load order / patch-application timing — HD reflection-resolves Vehicles.* types at static-field init and applies the redirect via [HarmonyPatch]+Prepare()**
  - Mechanism: Patch_WorkGiver_PackVehicle_Redirect.cs:41,46 AccessTools.TypeByName/Method at type init; Prepare() returns false (skips patch) when the target is null or VF inactive or the feature off
  - HD overlap: HD About.xml declares loadAfter brrainz.harmony only, NOT SmashPhil.VehicleFramework
  - Fix (general / no-op): Optional GENERAL hardening (not required): add <loadAfter>SmashPhil.VehicleFramework</loadAfter> to HD About.xml so the Vehicles assembly is guaranteed loaded before HD's static TypeByName runs. In practice all mod assemblies load before Harmony patching so the reflection resolves regardless, and Prepare()/IsActive degrade-SAFE if a type is somehow unresolved — so this is belt-and-suspenders, not a fix for an observed bug.

### Prior bugs learned from (their bug-fix history)

- n/a to HD — **Downed pawns no longer allowed inside vehicle cargo (they fill the least-impactful role instead, or cannot be placed)** _(VF UpdateLog.xml currentVersion 1.6.2384 'Additional Changes' (the only changelog entry in the depth-1 clone))_ — This is VF's own pawn-as-cargo loading rule (loading a downed colonist as a passenger), entirely inside VF's AddOrTransfer pawn-branch (VehiclePawn_Caravan.cs:70-85). HD only ever deposits ITEM cargo (def.category==Item; FloatMenuOptionProvider_BulkLoadVehicle.cs:176 and the GiveToPackAnimal item filter), never a pawn, so HD cannot reintroduce this class of bug.
- n/a to HD — **WorkGiver_PackVehicle repeatedly assigns a load job for a cargo-pawn that no longer has space (must filter pawns out of cargoToLoad before the job is assigned, else they retry forever)** _(VF inline fix-comment in WorkGiver_PackVehicle.GetThingsToLoad (WorkGiver_PackVehicle.cs:26-31, removes cargoToLoad entries whose pawn has GetNextAvailableHandler==null))_ — HD's loader only acts on ITEM transferables (VehicleLoadTarget.GetTransferables/AnythingToLoad keep entries with HasAnyThing && CountToTransfer>0; the deposit re-checks RemainingDemandForThing per stack and leaves fully-satisfied stock tagged). HD does not manage pawn-cargo handler-availability, so the infinite-retry class doesn't apply. HD's per-stack RemainingDemandForThing re-check is the analogous 'don't re-issue for already-satisfied demand' safeguard for items.
- n/a to HD — **Vehicles/embarked pawns must not double-count toward caravan capacity/mass, and a pawn in a vehicle must contribute 0 capacity/mass usage** _(VF Patch_CaravanHandling PawnCapacityInVehicle/PawnMassUsageInVehicle/ShouldIgnoreInventoryPawnInVehicle (:394-417) + the MassUtility.Capacity/CanEverCarryAnything prefixes)_ — HD never sums caravan capacity/mass across pawns; it reads MassUtility on the single courier colonist only. HD's MOW/ORG guard additionally skips an embarked holder via InVehicle, mirroring VF's own 'embarked pawn contributes nothing' intent. No double-count surface in HD.
- n/a to HD — **Vehicle inventory must not be auto-unloaded (UnloadEverything forced false for vehicles)** _(VF Patch_CaravanFormation.VehiclesShouldntUnloadEverything (:43-47,168))_ — HD's [UC1]/[UC2] guards already exclude vehicles from all HD unload paths, so HD never triggers or depends on vehicle UnloadEverything behavior.
- ⚠️ applies to HD — **(General class) A Harmony patch on a method inherited from a generic base with reference-type instantiations fires for ALL sibling instantiations (JIT shares the native code body)** _(Cross-mod general principle; concretely realized in VF via WorkGiver_CarryToVehicle<TransferableOneWay/ThingDefCountClass> siblings (WorkGiver_PackVehicle / WorkGiver_BringUpgradeMaterial / WorkGiver_RefuelVehicleTurret))_ — HD COULD have reintroduced this (a refuel/upgrade job hijacked into a cargo bulk-load) but PREVENTS it with the IsInstanceOfType(__instance) guard (Patch_WorkGiver_PackVehicle_Redirect.cs:73). Verified the sibling instantiations exist in current VF and target vehicles, so the guard is load-bearing, not theoretical.
- ⚠️ applies to HD — **cargoToLoad manifest decrement could over-load a def if a deposit passes more than the matched transferable's remaining count (the entry is driven negative→removed)** _(Inferred from VF AddOrTransfer decrement-by-passed-count (VehiclePawn_Caravan.cs:91) + HD's own MF1 addendum)_ — HD explicitly guards this in the explicit bulk-load (RemainingDemandForThing clamp to the single matched transferable, never a def-sum — JobDriver_LoadVehicleInBulk.DepositOne). The auto-pack-onto-vehicle path (JobDriver_LoadPackAnimal [SF4]) DELIBERATELY does NOT demand-clamp (mass/free-space clamp), accepting the negative→removed as the intended 'pack whatever fits' behavior — documented at :213-215. Both are correct; the over-load class is handled where it matters (explicit demand-driven load).

### Top risks
- Generic-base Harmony code-sharing (WorkGiver_CarryToVehicle<T>.JobOnThing) — the single genuinely tricky surface. CONFIRMED real (WorkGiver_PackVehicle<TransferableOneWay> shares native code with WorkGiver_BringUpgradeMaterial<ThingDefCountClass> and WorkGiver_RefuelVehicleTurret<ThingDefCountClass>, both targeting vehicles). HD's IsInstanceOfType(__instance) guard (Patch_WorkGiver_PackVehicle_Redirect.cs:73) is present and correct — keep it; any future widening of the postfix must preserve it.
- cargoToLoad over-load on the explicit bulk-load — HD's MF1 RemainingDemandForThing clamp mirrors VF's exact decrement matcher (TransferableMatchingDesperate + PodsOrCaravanPacking). Verified correct; the risk is only if a refactor ever replaced the single-transferable clamp with a def-sum.
- FindCarrier returning a VehiclePawn (VF's UsablePackAnimalWithTheMostFreeSpace prefix) — handled by JobDriver_LoadPackAnimal [SF4]'s AddOrTransfer deposit; the risk is only that the [SF4] branch is gated on enableVehicleFramework (when the master is OFF a raw deposit still works via Pawn polymorphism but leaves the cargoToLoad manifest cosmetically stale — a minor, documented degrade, not a data-loss bug).

### General-pattern recommendations
- Generic-base @__instance guard: any Harmony patch on a method inherited from a generic base with reference-type instantiations MUST guard the body with `TargetType.IsInstanceOfType(__instance)` (and take `object __instance`) so it doesn't fire for sibling instantiations sharing the JIT native code. Reusable for ANY mod that derives multiple WorkGiver/JobDriver/etc. from one generic base.
- Reflection-only, degrade-SAFE soft-dep shim: a single *Compat class (no `using <ForeignMod>`, no foreign type in any signature — everything held as Thing/object), lazy Init, cached IsActive that is true ONLY when every load-bearing member binds, each member null-guards + fails OPEN, exactly one permitted fail-open swallow for a pure-redundancy call. Covers ANY optional dependency.
- Skip-embarked-holder via the foreign mod's own 'is-contained' predicate (InVehicle) in per-pawn enumeration loops, while letting PARKED/normal holders pass the existing liveness+CurJob eligibility unchanged — so the desirable case (eat-from/build-from a parked vehicle's cargo) keeps working and only the unreachable case is skipped. Covers any mod whose carrier type subclasses Pawn.
- Deposit through the foreign mod's event-correct API (AddOrTransfer) — not a raw container move — when depositing into a foreign carrier, so the foreign mod's events/manifest stay consistent; clamp the deposit to the SINGLE matched transferable using the foreign mod's OWN matcher, never a def-sum. Covers any mod with a pending-load manifest decremented inside its add API.
- Defense-in-depth carrier-type skip ([UC1]/[UC2]) gated on the foreign-type check ONLY (not a feature toggle), so a safety/correctness guard fixing a pre-existing misfire can never be switched back off by a feature flag. Covers any mod whose carrier type HD's generic carrier-handling must never claim.
- Fail-on-despawn + salvage-not-drop for a disappearing deposit target (vehicle launch/despawn): FailOnDespawnedOrNull + re-validate spawned + finish-action releases claims and re-tags the carried split into inventory. Covers any mod with a deposit target that can vanish mid-job.

### Targeted patches (only where a general pattern can't cover it)
- Patch_WorkGiver_PackVehicle_Redirect — postfix on Vehicles.WorkGiver_CarryToVehicle<TransferableOneWay>.JobOnThing (resolved via AccessTools, applied through [HarmonyPatch]/Prepare). Genuinely mod-specific because it must hook VF's exact pack-vehicle workgiver and carries the generic-base @__instance guard; it is the autonomous-path enabler with no general substitute. CONFIRMED targets resolve against VF 1.6.2384.
- VehicleFrameworkCompat.ReserveVehicle/ReleaseVehicleClaims — reflective call into VF's VehicleReservationManager.Reserve<LocalTargetInfo,VehicleTargetReservation>/ReleaseAllClaimedBy. Mod-specific (VF's own MapComponent), but explicitly non-load-bearing belt-and-suspenders that fails open; CONFIRMED the generic arity/constraint still match.
- The IsInstanceOfType(__instance) sibling-instantiation guard is technically 'targeted' to VF's WorkGiver hierarchy, but the PRINCIPLE is general (see generalPatterns). No NEW targeted patch is recommended; the existing two are the minimum and are correctly scoped.

### Adversarial verification
**Confirmed real:**
- GENERIC-BASE JIT CODE-SHARING (the #1 risk) — REAL and the @__instance guard is genuinely load-bearing. Verified end-to-end: TransferableOneWay and ThingDefCountClass are BOTH reference types (confirmed via PEReader metadata read of Assembly-CSharp.dll 1.6.4518: both base=System.Object, REFERENCE TYPE), so the JIT shares the native code body of WorkGiver_CarryToVehicle<T>.JobOnThing across <TransferableOneWay> and <ThingDefCountClass>. WorkGiver_BringUpgradeMaterial does NOT override JobOnThing (grep returned no override -> it runs the shared base code body), and WorkGiver_RefuelVehicleTurret overrides but calls base.JobOnThing (WorkGiver_RefuelVehicleTurret.cs:22-25). HD patches the base method (Patch_WorkGiver_PackVehicle_Redirect.cs:46-47 AccessTools.Method walks to the base), so the postfix DOES fire for the two <ThingDefCountClass> siblings. The guard at Patch_WorkGiver_PackVehicle_Redirect.cs:73 (`if (PackVehicleType == null || !PackVehicleType.IsInstanceOfType(__instance)) return;`) correctly rejects them (a BringUpgradeMaterial/RefuelVehicleTurret instance is not a WorkGiver_PackVehicle). Without it, an upgrade-material/turret-ammo job (LoadUpgradeMaterials/CarryItemToVehicle) would be hijacked into a cargo bulk-load. Guard present, correct, necessary.
- cargoToLoad OVER-LOAD on the explicit bulk-load — REAL risk, correctly clamped. VF's VehiclePawn.AddOrTransfer (VehiclePawn_Caravan.cs:87-96) decrements the matched transferable's CountToTransfer by the PASSED `count` (line 91), not the moved `result` (line 79), and removes it when <=0. HD's JobDriver_LoadVehicleInBulk.DepositOne (line 93-94) clamps to RemainingDemandForThing = the SINGLE matched transferable's CountToTransfer (VehicleFrameworkCompat.cs:233-248) using the IDENTICAL matcher TransferableUtility.TransferableMatchingDesperate(item, typed, TransferAsOneMode.PodsOrCaravanPacking) that VF uses (VehiclePawn_Caravan.cs:88 also uses PodsOrCaravanPacking). SplitOff preserves def/stuff/quality so the matcher returns the same entry for the original stack and the split. Clamp lines up exactly -> never over-loads. Verified correct.
- VehiclePawn : Pawn leaking into per-pawn enumeration loops — self-protects by construction for the TAGGED paths. Verified: HD injects CompHauledToInventory only via Patches/HaulersDream_Pawns.xml xpath `Defs/ThingDef[thingClass="Pawn"]/comps` (exact-match), and VF's VehiclePawnBase.xml:5 sets `<thingClass>Vehicles.VehiclePawn</thingClass>` (not `Pawn`), so a vehicle NEVER gets the comp. InventoryShare (FindSharableStack/AddSharableStacksForBill/CountSharable) and CarriedHaulShare all read CompHauledToInventory (comp==null early-out) or carryTracker+HaulToCell/ToCellStorage job (a parked vehicle has neither) -> harmlessly skipped. IsLiveShareCarrier liveness clauses don't choke on a vehicle. Confirmed low-risk.
- MassUtility.Capacity / CanEverCarryAnything global patches for VehiclePawn — REAL VF patches, HD relies on them correctly. Verified Patch_CaravanHandling.cs:48-54 + CapacityOfVehicle (:319-334, returns vehicle.GetStatValue(VehicleStatDefOf.CargoCapacity)) and CanCarryIfVehicle (:339-345, __result = p is VehiclePawn). HD only ever passes a COURIER colonist to its own Capacity/GearAndInventoryMass calls; the single intentional vehicle read is VehicleLoadTarget.GetMassUsage = MassUtility.GearAndInventoryMass(vehicle) (:112) + GetMassCapacity = CargoCapacity (:108), both correctly relying on these VF patches. No misfire.
- GiveToPackAnimalUtility.UsablePackAnimalWithTheMostFreeSpace prefixed to return a VEHICLE — REAL (Patch_CaravanFormation.cs:76-85, returns CaravanHelper.UsableVehicleWithTheMostFreeSpace whenever IsFormingCaravanShipHelper(pawn) || SpawnedPawnsInFaction(faction).HasVehicle()). HD's PackAnimalLoad.FindCarrier (:26-27) calls exactly this util and CAN receive a vehicle. Handled by JobDriver_LoadPackAnimal [SF4] (:210-236): when enableVehicleFramework && IsVehicle(carrier), deposits via the event-correct VehicleFrameworkCompat.AddOrTransfer (split already detached, raw-fallback never re-reads a dead handle). The [SF4] count is mass/free-space-clamped (not demand-clamped) — intended auto-pack, documented at :213-215. Correct.
- [UC1]/[UC2] vehicle-skip in the bulk-UNLOAD paths — REAL and present, gated on IsVehicle ONLY (not the feature toggle). Verified Patch_WorkGiver_UnloadCarriers.cs:71 (BulkUnloadGate.ShouldHandle: `if (VehicleFrameworkCompat.IsVehicle(carrier)) return false;`) and FloatMenuOptionProvider_BulkUnloadCarrier.cs:47 (`if (VehicleFrameworkCompat.IsVehicle(carrier)) continue;`). IsVehicle returns false when VF is absent (gated on IsActive), so these are pure safety skips that cannot be re-enabled by a feature flag. Vehicle cargo left entirely to VF. Correct.
- VehicleReservationManager.Reserve<LocalTargetInfo,VehicleTargetReservation> reflection — signature VERIFIED against VF 1.6.2384. VehicleReservationManager.Reserve<T1,T2>(VehiclePawn, Pawn, Job, T1) where T2:Reservation<T1> (VehicleReservationManager.cs:32-33); VehicleTargetReservation : Reservation<LocalTargetInfo> with ctor (VehiclePawn, Job, int) (VehicleTargetReservation.cs:11,22). HD's MakeGenericMethod(LocalTargetInfo, VehicleTargetReservation) + invoke {vehicle,pawn,job,(LocalTargetInfo)vehicle} (VehicleFrameworkCompat.cs:133-135, 288-289) satisfies the constraint and arity. Non-load-bearing (does NOT gate IsActive; fail-open). Also note VF's Reserve already has its own try/catch returning false (VehicleReservationManager.cs:35,64-68), so HD's swallow is belt-and-suspenders on a method that already cannot propagate. Correct as is.
- FloatMenuOptionProvider_Vehicle (VF's selected-vehicle menu) vs HD's float menus — no collision. VF's SelectedPawnValid gates on `pawn is VehiclePawn` (FloatMenuOptionProvider_Vehicle.cs:20-23, the SELECTED actor being a vehicle). HD's FloatMenuOptionProvider_BulkLoadVehicle/PickUpIntoInventory read context.FirstSelectedPawn = the colonist and require CompHauledToInventory (a vehicle-as-actor self-excludes). FloatMenuOptionProvider_PickUpIntoInventory also filters clicked to category==Item and excludes IsVehicle(clicked) (:63). Correct.
- Aerial/launchable vehicle despawning mid-load — handled. JobDriver_LoadVehicleInBulk.FindTargetStillValid checks vehicle != null && vehicle.Spawned (:81-85); the base driver's FailOnDespawnedOrNull + finish-action releases VRM+ledger claim and salvages the carried split back into tagged inventory (re-tagged, rides HD's normal unload). VehicleLoadTarget caches the Map for the despawn case (:35,50). Correct.
- Reflection-only / degrade-SAFE shim — VERIFIED. Exactly 8 AccessTools.TypeByName("Vehicles...") sites (matches the map); ZERO `using Vehicles`/`Vehicles.*`/`SmashTools.*` type references in HD code (grep clean). VehicleFrameworkCompat.IsActive is true only when AddOrTransfer && getStatValue && cargoCapacityStatDef && cargoToLoadField all bound (VehicleFrameworkCompat.cs:142-145) -> partial drift reports inactive. Ext_Vehicles.InVehicle(this Thing) exists at line 1017 (the (Pawn) overload at 1006), body = `thing.ParentHolder is VehicleRoleHandler or Pawn_InventoryTracker { pawn: VehiclePawn }`. HD's main project builds clean (0 warnings, 0 errors) against the live VF 1.6.2384.

**Refuted (not real / already handled):**
- REFUTED (overstated): the map's claim that VF 'BLOCKS all non-VF FloatMenuOptionProvider.Applies when a vehicle is the SELECTED actor.' No such foreign-provider-suppression mechanism exists in VF (grep of Vehicles/ found only VF's OWN FloatMenuOptionProvider_Vehicle subclasses gating their OWN Applies on pawn is VehiclePawn; no global gate over other mods' providers). The 'none' RISK verdict is still correct, but for a DIFFERENT reason than stated: HD's providers self-exclude because a vehicle-as-actor lacks CompHauledToInventory (FloatMenuOptionProvider_BulkLoadVehicle.cs:45, PickUpIntoInventory.cs:45) — not because VF suppresses them. The map's protective mechanism is wrong; the conclusion (no collision) holds.
- REFUTED (redundancy mischaracterized as the protective mechanism): the map presents the MOW/ORG `InVehicle(holder)` skip (EatFromInventory.cs:101, OrganicInventoryShare.cs:62) as the guard that keeps HD out of embarked-pawn inventory. But InVehicle(Thing) returns true only when ParentHolder is a VehicleRoleHandler or a vehicle's Pawn_InventoryTracker (Ext_Vehicles.cs:1017-1019) — a pawn riding INSIDE a vehicle is DESPAWNED and therefore is NOT in map.mapPawns.SpawnedPawnsInFaction, so it never reaches the InVehicle check in these loops. The guard is essentially dead/redundant for embarked PASSENGERS (harmless). The behaviorally meaningful effect of these loops is on the PARKED VEHICLE itself (which IS spawned and for which InVehicle is false), which the map covered as 'desirable' — see the missed-risk note about that path being unguarded for OrganicInventoryShare.

**Missed by the first pass (folded into the plan):**
- MISSED / under-flagged: build-from-inventory (buildFromInventory, default TRUE) will source construction materials directly out of a PARKED VehiclePawn's CARGO HOLD. OrganicInventoryShare reads the WHOLE carrier.inventory.innerContainer (CountOrganicOfDef:152-165, ConsiderHolder:215-249), NOT just CompHauledToInventory-tagged stock — so unlike InventoryShare it is NOT protected by the comp==null early-out (a vehicle has no comp, but that's irrelevant here). A parked vehicle passes IsLiveShareCarrier (spawned/alive/not downed/drafted/mental), passes IsEligibleOrganicHolder (InVehicle(vehicle)==false; the HoldsUnshareablePreloadedStock job-def guard passes for a job-less parked vehicle), and the ONLY exclusion that could catch player-earmarked cargo — `carrier.IsFormingCaravan()` (OrganicInventoryShare.cs:64) — does NOT fire for a vehicle that the player loaded via VF's 'Load cargo' dialog WITHOUT a forming-caravan lord (the cargo lands in cargoToLoad then innerContainer with no lord). So a colonist will pull steel/components out of a parked, player-loaded vehicle to feed a build job (SharedInventoryPatches.cs:92-110 builds a HaulToContainer job with targetA = the in-vehicle stack; the vanilla driver's canTakeFromInventory pulls it out via the VehiclePawn's normal Pawn inventory tracker). The author DELIBERATELY decided this is 'desirable' and gated only on buildFromInventory (HaulersDreamSettings.cs:160-162), but the investigator stated it as fully-resolved-desirable without noting the tension: VF deliberately forces UnloadEverything=false on vehicles (cargo never auto-dumps) and the player explicitly loads vehicle cargo for trips, so HD raiding that cargo can silently undo a player's loadout. Same logic applies to MealsOnWheels eating from a parked vehicle's food cargo. Not a crash/data-loss bug, but a real behavior-judgment gap worth an explicit decision (e.g. a VehicleFrameworkCompat.IsVehicle skip in IsEligibleOrganicHolder, mirroring [UC1]/[UC2], or a dedicated guard for vehicles with a non-empty cargoToLoad manifest). Severity: medium (default-on, silently competes with the player's trip-loadout intent).
- MISSED nuance on the [SF4] master-OFF degrade: the map calls it 'cosmetically stale manifest, minor.' Verified slightly worse than cosmetic: with VF PRESENT but enableVehicleFramework=FALSE, VF's UNCONDITIONAL UsablePackAnimalWithTheMostFreeSpace patch still hands HD a VehiclePawn carrier, and JobDriver_LoadPackAnimal then takes the `else` raw inner.TryTransferToContainer (line 239) into the vehicle's innerContainer. That raw move (a) does NOT fire VF's CargoAdded event, (b) does NOT decrement cargoToLoad, AND (c) bypasses any cargo-capacity gate (a Pawn innerContainer is uncapped), so the auto-divert/opportunistic pack-animal path can physically push items into a vehicle's cargo with no event and beyond CargoCapacity (VF's mass system then flags it over-encumbered). Still not data loss (items are in the vehicle), but it is an unintended physical deposit (not merely a stale label) on the master-OFF path. Bounded to the pack-animal auto-divert/opportunistic path (the explicit bulk-load redirect is fully disabled when the master is off via Prepare()). Worth either gating FindCarrier to skip a vehicle when the master is off, or documenting the physical (not cosmetic) nature.
- MISSED (low, no action needed but should be stated): the map's 'low' verdict for the MassUtility.Capacity/StatPart paths omits that StatPart_Overload is added to the vanilla MoveSpeed StatDef and therefore RUNS for a VehiclePawn's MoveSpeed (vehicles have MoveSpeed). Verified safe: StatPart_Overload.TryGetFactor (StatPart_Overload.cs:46) enters on `req.Thing is Pawn` (a VehiclePawn qualifies) but OverloadPolicy.AppliesSpeedPenalty requires (isHumanlike || isMechanoid) (OverloadPolicy.cs:67-72) — a VehiclePawn is neither, so the factor is never applied and vehicle MoveSpeed is unaffected. The PawnMassCache read at :66 is only reached after that gate. Conclusion holds (no vehicle slowdown), but the map should have noted the StatPart is actually invoked for vehicles and is guarded by the race predicate, rather than implying it's never reached.

**Verdict:** The risk map is largely accurate and well-grounded. All three headline risks are REAL and CONFIRMED correctly handled by HD's existing guards, verified against the actual VF 1.6.2384 source (the exact version present) and HD's current code, with the JIT code-sharing premise hard-verified via PEReader metadata (TransferableOneWay and ThingDefCountClass are both reference types). HD builds clean against live VF; the reflection-only/degrade-safe shim is exactly as described (8 TypeByName sites, zero hard refs). Two map claims are REFUTED as mechanistically wrong though conclusion-correct (no VF foreign-provider suppression exists — HD self-excludes via the missing comp; and the InVehicle holder-skip is dead/redundant for embarked passengers since they aren't in SpawnedPawnsInFaction). The most substantive MISS is that build-from-inventory and meals-on-wheels (both default-ON) source from a PARKED vehicle's player-loaded cargo hold via OrganicInventoryShare reading the WHOLE innerContainer — guarded by neither IsVehicle nor IsFormingCaravan when the vehicle isn't in a forming-caravan lord. The author intended this as 'desirable,' but it tensions against VF's deliberate no-auto-dump design and silently competes with a player's trip loadout; it deserves an explicit decision rather than the map's unqualified 'desirable, works unchanged.' No crash/data-loss bug found; the remaining items are behavior-judgment and degrade-severity nuances.

---

## Family: `abilities` — Better Autocasting for VPE (`dev.tobot.vpe.betterautocast`)

Auto-casts psycasts on an interval and can force-interrupt the current job to cast. Patches `VEF.Abilities.CompAbilities.CompTick` + ability `CanAutoCast` getters — a surface HD does **not** touch (zero patch overlap, verified by cloning).

### Feature risk map
- `LOW` **Better Autocast — interrupt a pawn mid-HD-job to cast**
  - Mechanism: `CompTick` postfix → `PawnHelper.CanPsycast` (HD job defs not on its blocklist) → VEF `Ability.StartAbilityJob` → `EndCurrentJob(InterruptForced)` + cast job.
  - HD overlap: BulkHaul / LoadInBulk* / BatchCraft / BillPrepGather / en-route pickup.
  - Fix (general / no-op): no action. HD carries loot in **tagged inventory** (not the carry tracker, so VEF `keepCarryingThing` can't drop it); HD finish actions + `Patch_BulkLoadLifecycle` re-queue the unload + release claims on the forced-end path. The haul pauses for the cast, then completes. Remedy for the pause is Better Autocast's own in-game Blocked Jobs list.

### Verdict
No HD change needed — and the interruption-safety **generalizes** to any mod that force-ends a pawn's job (autocasters, drafted-interrupt, mental breaks). Confirmed by construction + decompiled VEF cast path (not in-game).

---

## Family: `advanced_power_plus`

Advanced Power Plus (APP; packageId `Meltup.AdvancedPowerPlus`, workshop 2032409628, GitHub Meltup/Advanced-Power-Plus) is a **pure content mod**: it adds power-generation buildings and three research projects. Its *entire* C# surface is three files (`Sources/MyClass.cs`, `Sources/Patch.cs`, `Sources/PatchCompPowerPlantWater.cs`): a render/power-only `CompPowerPlantSolar` subclass, and two postfixes on vanilla `CompPowerPlantWater.RebuildCache`/`ForceOthersToRebuildCache` that flag the advanced watermill's double-water-use cache — **no work, hauling, job, or WorkGiver code anywhere**. Its three fueled generators (`ASRG` 2×2, `large_ASRG` 4×4, `advanced_nuclear_generator` 6×6) use *vanilla* `CompProperties_Refuelable` (uranium) with **no custom compClass and no custom WorkGiver**, so they flow through vanilla `WorkGiver_Refuel`/`CompRefuelable` exactly. The ONLY HD↔APP contact point is therefore the refuelable path, and its single real bug — an NRE refueling APP's *impassable* generators — is already closed by HD's `BulkRefuel.TryGiveBulkRefuelJob` search-anchor fix (`pawn.Position`, not `refuelable.Position`), a **general** fix covering any impassable refuelable. Everything else is `NONE`: APP's solar comp and watermill cache postfixes touch only rendering/power/water-cache, on methods HD never patches or calls (HD has zero `CompPowerPlant`/power/`listerBuildings` code, grep-confirmed).

### Feature risk map

- `LOW` (fixed) **Fueled generators — vanilla CompRefuelable on IMPASSABLE buildings (ASRG 2×2, large_ASRG 4×4, advanced_nuclear_generator 6×6; all `<passability>Impassable</passability>`, uranium fuelFilter, non-atomic)**
  - Mechanism: each fueled generator (`1.6/Defs/ThingDefs/RTG_Buildings.xml`: ASRG :17, large_ASRG :108, advanced_nuclear_generator :276 — all `Impassable`, `<size>` 2×2/4×4/6×6) carries a stock `CompProperties_Refuelable` with `compClass` unset (= vanilla `CompRefuelable`) and a Uranium-only `fuelFilter`, refuelled by vanilla `WorkGiver_Refuel` (non-atomic; `atomicFueling` not set).
  - HD overlap: `BulkRefuel.TryGiveBulkRefuelJob` (`BulkRefuel.cs`) + the autonomous `Patch_WorkGiver_Refuel_Redirect` prefix on `WorkGiver_Refuel.JobOnThing` + the player `FloatMenuOptionProvider_BulkRefuel`. HD reuses vanilla `RefuelWorkGiverUtility.FindEnoughReservableThings` to pick the fuel sweep set.
  - Fix (general / no-op — already shipped): `FindEnoughReservableThings` (decompiled `RimWorld/RefuelWorkGiverUtility.cs:92-98`) does `region = rootCell.GetRegion(pawn.Map)` then immediately `rootCell.GetThingList(region.Map)` **with no null check** — an impassable `rootCell` returns a null `region` → NRE. APP's generators occupy impassable footprints, so `refuelable.Position` has no passable region (issue #34, the 6×6 nuclear reactor; large_ASRG and ASRG are equally impassable and were hit by the same bug on the autonomous path). HD now anchors the sweep at `pawn.Position` (`BulkRefuel.cs`, always passable, plus a defensive null-region decline) — the SAME reason vanilla never NREs (see "general-pattern recommendations"). Covers **any** impassable refuelable, not just APP's. No APP-specific code.
- `NONE` **Autonomous bulk-refuel redirect probing APP generators (work-scan / route-resolve)**
  - Mechanism: HD's `Patch_WorkGiver_Refuel_Redirect` is a prefix on the EXACT base `WorkGiver_Refuel.JobOnThing` (subclass-guarded `__instance.GetType() == typeof(WorkGiver_Refuel)`), fail-open. APP uses the unmodified base WorkGiver, so the redirect fires on its generators; it routes to `TryGiveBulkRefuelJob` (now `pawn.Position`-anchored, NRE-safe). `WorkKindResolver.Resolve` also probes every `directOrderable` `WorkGiver_Scanner` (and `WorkGiverDef.directOrderable` defaults `true`), so `WorkGiver_Refuel` IS probed via `HasJobOnThing`/`JobOnThing(forced:true)`.
  - HD overlap: route-plan resolve + autonomous refuel both invoke `WorkGiver_Refuel.JobOnThing` on APP buildings.
  - Fix (general / no-op): None. The route-resolve probe is wrapped in a per-giver try/catch that surfaces (never swallows) and skips one bad giver; and the underlying call is now NRE-safe anyway. For the non-atomic APP refuelable, vanilla `WorkGiver_Refuel.JobOnThing → RefuelJob → FindBestFuel(pawn.Position)` (decompiled) **does NOT call** the NRE-prone `FindEnoughReservableThings` at all, so even the bare vanilla probe is safe on APP's impassable buildings. A route is also nonsensical for a refuelable (no harvest/mine/deconstruct designation), so the resolver returns null for it — harmless.
- `NONE` **Advanced solar generator — custom `sd_adv_powergen_CompAdvPowerPlantSolar` (CompPowerPlantSolar subclass)**
  - Mechanism: `Sources/MyClass.cs` overrides only `DesiredPowerOutput` (sky-glow × roofed-fraction) and `PostDraw` (a fillable power bar). `PassThroughOnly`, not refuelable, no fuel. Pure rendering + power-output math.
  - HD overlap: none — HD has no `CompPowerPlant`/power code (grep: zero hits for `PowerPlant|CompPower|skyManager|roofGrid` in HD source).
  - Fix (general / no-op): No overlap. Power output and a draw bar are invisible to every HD subsystem.
- `NONE` **Advanced watermill — two `CompPowerPlantWater` postfixes (double-water-use cache)**
  - Mechanism: `Sources/PatchCompPowerPlantWater.cs` postfixes `CompPowerPlantWater.RebuildCache` (also flags `___waterDoubleUsed` when an *advanced* watermill's `WaterUseRect` overlaps) and `ForceOthersToRebuildCache` (dirties advanced watermills' caches). The watermill is Impassable, `CompPowerPlantWater`, **no fuel**.
  - HD overlap: none — HD never patches or calls `CompPowerPlantWater`, `RebuildCache`, `WaterUseCells/Rect`, or `listerBuildings.AllBuildingsColonistOfDef` (grep-confirmed).
  - Fix (general / no-op): No overlap. Water-cache bookkeeping on a non-refuelable building is orthogonal to all HD features.
- `NONE` **Non-fueled generators — advanced wind (`PassThroughOnly`), advanced geothermal (Impassable, on geyser)**
  - Mechanism: `1.6/Defs/ThingDefs/SD_Buildings.xml` (wind, `CompPowerPlantWind`) and `RTG_Buildings.xml` (geothermal :186, `CompPowerPlantSteam`). No `CompRefuelable`, no fuel.
  - HD overlap: none (no fuel → no refuel path; HD has no power code).
  - Fix (general / no-op): No overlap.
- `NONE` **Hauling APP build materials (Steel/Plasteel/Components/Gold/WoodLog) and uranium fuel — vanilla items**
  - Mechanism: APP buildings cost standard vanilla materials; the fuel is vanilla `Uranium`. All are ordinary haulables.
  - HD overlap: HD's bulk-haul / scoop / construct-delivery treat them like any vanilla stack.
  - Fix (general / no-op): No issue — APP adds no new ThingDef for materials or fuel, so HD's existing storage/haul handling applies unchanged. (The only fuel-delivery nuance is the impassable-refuelable NRE above, already fixed.)

### Prior bugs learned from (their bug-fix history)

- **None found.** APP's GitHub commit history (Meltup/Advanced-Power-Plus) and `About/About.xml` show only content/version work: "Updates for Rimworld 1.5 and 1.6", a `basePowerConsumption` tweak, and upload maintenance — **zero** commits mentioning refuel/fuel/hauling/WorkGiver/jobs/region/NullReference/crash. There is no `Changelog`/`News` file in `About/`. The relevant bug is HD-side (the impassable-refuelable NRE, HD issue #34), not an APP bug — APP just happens to ship the most prominent impassable refuelable (the 6×6 nuclear reactor), which is what surfaced it.

### Top risks

- **(Resolved) Impassable-refuelable NRE on APP's generators.** Before the fix, HD's `BulkRefuel` fed `refuelable.Position` to `FindEnoughReservableThings`, NRE-ing on every impassable refuelable (all 3 APP fueled generators qualify). Now anchored at `pawn.Position`; bounded and closed. No residual.
- No other live risk. APP contributes no work/hauling/job logic, so there is nothing else for HD's patch surface to collide with.

### General-pattern recommendations

- **ANCHOR REGION SWEEPS AT A PASSABLE CELL (the shipped fix, and it is general):** vanilla `FindEnoughReservableThings` (`RefuelWorkGiverUtility.cs:94-98`) derefs `rootCell.GetRegion(map).Map` with no null guard. Vanilla itself only ever passes this function `refuelable.Position` on the ATOMIC path (`FindAllFuel`) — and shipped atomic content sits on passable footprints — while the NON-atomic path uses `FindBestFuel(pawn.Position)` which never calls it. HD reuses the *atomic* finder for its bulk sweep over *non-atomic* refuelables, so it must (and now does) anchor at `pawn.Position`, which is always passable. This makes HD safe for **any** impassable refuelable from **any** mod (deep drills, large reactors, modded turrets on impassable bases), not just APP — no per-mod patch.
- **CONTENT-ONLY MODS NEED NO HD CODE:** a mod whose entire C# is render/power/cache postfixes on methods HD neither patches nor calls (here `CompPowerPlantSolar.DesiredPowerOutput`/`PostDraw`, `CompPowerPlantWater.RebuildCache`/`ForceOthersToRebuildCache`) has zero compat surface. The audit reduces to: does it add a `CompRefuelable` on an impassable footprint? If yes, the passable-anchor pattern already covers it; if no, `NONE` across the board.
- **EXACT-TYPE-GUARD + FAIL-OPEN on the refuel redirect** already prevents hijacking modded/turret refuel WorkGivers; APP uses the unmodified base, so it benefits from the bulk redirect with no special handling.

### Verdict

APP is the simplest possible case for HD: a content mod with no work/hauling/job code. The only interaction is the refuelable path, and its sole bug (NRE on impassable refuelables — HD #34, exemplified by APP's 6×6 nuclear reactor) is already closed by HD's `pawn.Position` search-anchor in `BulkRefuel`, a general fix covering every impassable refuelable. All 3 fueled generators are impassable (verified in `RTG_Buildings.xml`) and were subject to the same bug on the autonomous redirect path; the fix covers all three identically. APP's custom solar comp and watermill cache postfixes are `NONE` — orthogonal to every HD subsystem (HD has no power code, grep-confirmed). **No APP-specific patch is needed, now or anticipated.**

---

## Family: `construction`

Smarter Construction (SC; packageId `dhultgren.smarterconstruction`, workshop 2202185773, closed-source — decompiled from `v1.6/Assemblies/SmarterConstruction.dll` with ilspycmd for this entry) makes builders finish wall frames in an order that does not seal a colonist (or a pending blueprint) into a closed region. Its whole C# surface is five patches applied by a `[StaticConstructorOnStartup]` `PatchAll` (`SmarterConstruction/SmarterConstruction.cs:12-18`). Four of them are `NONE` for HD (HD owns no construction WorkGiver, no `JobDriver_ConstructFinishFrame`, and no work-giver priority logic). The fifth is a genuine `HIGH`, and it is the patch HD issue #235's investigation examined: SC's postfix on `WorkGiver_ConstructFinishFrames.JobOnThing` **can** throw a `NullReferenceException`, and it would throw from the ONE place in vanilla's work scan that has no guard — the tail call after the per-giver try/catch. The blast radius is not "construction breaks"; it is "the pawn's ENTIRE work think-node is skipped every scan", so colonists do no work at all while still eating, sleeping and obeying forced orders.

→ GOTCHA: **this entry documents a decompiled POSSIBILITY, not an established cause of #235.** That attribution was retracted. Two unguarded `MapComponent` dereferences do exist in SC's v1.6 assembly (below), but the precondition — a missing MapComponent — was never established, and the trace could not have shown it either way: Harmony's frame annotations list who PATCHES a method, not who threw; several mods' code runs inside that one patched method; and HD's own `Exception`-returning finalizer truncated the trace at the patched method, erasing the callee frames. HD itself held two postfixes and a finalizer there, so the same reasoning would have named another mod even if HD's code had thrown. What is established is the amplifier, which is vanilla's (an unguarded tail call), and HD's share, which is that it is the mod holding a Finalizer on that method and therefore the only one positioned to stop the colony bricking. HD's fix is deliberately GENERAL — contain any foreign throw at that seam, then quarantine the repeatedly-faulting work giver for the session with a visible alert — and applies to any mod that throws from any work giver's `JobOnThing`/`JobOnCell`, not to SC specifically. HD ships no SC-aware code and no workaround for SC's internals.

### Feature risk map

- `HIGH` **Smarter Construction — "don't wall yourself in": veto a wall-frame job that would enclose a pawn or a pending blueprint**
  - Mechanism: `SmarterConstruction.Patches/WorkGiver_ConstructFinishFrames_JobOnThing.cs:9` `[HarmonyPatch(typeof(WorkGiver_ConstructFinishFrames), "JobOnThing")]`; the `Postfix` (`:18-44`) fires when `__result != null && t.def.entityDefToBuild.passability == Impassable && !forced`, and calls `ClosedRegionDetector.WouldEncloseThings(t, pawn, maxCacheLength)` (`:33`), nulling `__result` when the frame would enclose something. `WouldEncloseThings` (`SmarterConstruction.Core/ClosedRegionDetector.cs:12-69`) then dereferences two `MapComponent`s with NO null check: `EncloseThingsCache.GetCache(target.Map).GetIfAvailable(target, maxCacheLength)` (`:45-46`, where `GetCache` is a bare `map.GetComponent<EncloseThingsCache>()`, `EncloseThingsCache.cs:52-55`) and `ClosedRegionCreatedByAddingImpassable(target.Map.GetComponent<WalkabilityHandler>(), addedBlockers)` (`:53`), whose flood fill calls `walkabilityHandler.Walkable(start)` unguarded (`:111`). Either missing component is an NRE, per scan, for every wall-class frame.
  - HD overlap: `Patch_JobGiver_Work_OpportunisticUnload`'s `Finalizer` on `JobGiver_Work.TryIssueJobPackage` — the only HD code on the path. HD owns no construction work giver and does not call `JobOnThing` here; the throw travels through HD's seam, it does not originate in it. Vanilla is the amplifier: decompiled `JobGiver_Work.TryIssueJobPackage` wraps only the scan in `try { … } catch (Exception ex) { Log.Error(pawn + " threw exception in WorkGiver " + …) }`, and the job-producing tail `scannerWhoProvidedTarget.JobOnCell/JobOnThing(…)` sits AFTER that block, unguarded. (Corroborated by the #235 log: 2673 throws, zero `threw exception in WorkGiver` lines — every one escaped outside the try.) `ThinkNode_Priority`/`ThinkNode_PrioritySorter` then catch-and-skip the whole work node, while `JobGiver_GetFood` (a sibling node) and forced orders (`ThinkNode_QueuedJob`) are untouched.
  - Fix (general): shipped, and mod-agnostic. (1) The Finalizer classifies the fault STRUCTURALLY via `HDFault.InvolvesHaulersDream` (frame objects, never rendered trace text — the loaded 0Harmony deduplicates repeat renders into a placeholder): HD's own fault still logs + RETHROWS, anything else is reported with `HDFault.DescribeOrigin` naming the innermost MOD-OWNED foreign frame and CONTAINED (`HDGuard.SeamContained`), so the pawn finds no work that scan instead of losing all work. (2) Containment alone would be near-cosmetic — throw-and-skip and contain-and-return-NoJob both leave the pawn idle — so `WorkGiverBlocklist` counts faults per work-giver TYPE and, at 3, quarantines it: `Patch_JobGiver_Work_WorkGiverResilient`'s prefix makes `PawnCanUseWorkGiver` return false, vanilla `continue`s the whole loop iteration, and the poisoned giver can never become `scannerWhoProvidedTarget` again. HD's own givers are exempt (bugs stay loud), nothing is persisted, and `Alert_WorkGiverQuarantined` (High priority) names the disabled giver — and a "most likely source" mod ONLY when the error itself identifies one; otherwise it says the source could not be identified and points at the log (the #235 correction, `3d18466`) — so a silently-missing work type cannot become the next false report against HD. (3) ATTRIBUTION IS THE DANGEROUS PART, and the decision therefore lives in the Verse-free `WorkGiverQuarantinePolicy` with a named NUnit test per refusal. Two facts are gathered and BOTH attribution routes clear the same bar, because naming a giver says nothing about whose fault it is. (a) WHICH giver: the frame walk cannot see a Harmony-patched one at all (the patched method runs as a `DynamicMethod`, whose `DeclaringType` is always null — i.e. exactly this SC case), so the fallback route (the giver vanilla last cleared) is the common one; it is scoped to a single `TryIssueJobPackage` call — captured in the Finalizer, cleared in its `finally` on every path, success included — so a throw from before the loop, or from a mod postfixing `TryIssueJobPackage` after it, sees null rather than a stale name. (b) WHETHER A MOD IS IMPLICATED IN THAT GIVER: either a non-HD Harmony owner patches its `JobOnThing`/`JobOnCell`/`HasJobOnThing` (`Harmony.GetPatchInfo`, cached per giver type), or the exception carries a mod-owned frame (`DescribeOrigin`, which excludes vanilla, the runtime, Unity AND Harmony's own plumbing — brrainz ships `0Harmony.dll` inside a mod's `Assemblies/`, so ownership alone would let it satisfy the bar). Asking instead "does the giver TYPE belong to a mod?" was tried and is wrong in both directions: it refuses SC (a vanilla giver type) outright, and it would let a real `RimWorld.WorkGiver_*` frame get a vanilla work type disabled because some mod's DATA poisoned it. Consequence to state plainly: while SC is the quarantined giver, wall frames are not finished AUTOMATICALLY until SC is updated or removed. A player right-click "Prioritize" still issues one job — that order is built by `FloatMenuOptionProvider_WorkGivers` (`:123` `JobOnThing(..., forced: true)`, started at `:178` via `TryTakeOrderedJobPrioritizedWork`) and never consults `PawnCanUseWorkGiver`, which is `private` (`JobGiver_Work:268`) and so could not be consulted from there; SC's postfix is `!forced`-gated, so the manual order does not even reach the fault. Only the automatic scan (`:89`) and the emergency/`priorityWork` SUSTAIN follow-up (`:308`, reached from `:65`) are blocked. The alert says exactly this.
- `NONE` **Smarter Construction — make finish-frames jobs prioritized (transpiler on the same vanilla method HD finalizes)**
  - Mechanism: `SmarterConstruction.Patches/PatchMakeFinishFramesPrioritized.cs:12-15` `[HarmonyPatch(typeof(JobGiver_Work), "TryIssueJobPackage")]` `Transpiler` — ORs `scanner is WorkGiver_ConstructFinishFrames` into the `WorkGiver_Scanner.Prioritized` check, and redirects the `GenClosest.ClosestThing_Global_Reachable` call to its own `CustomGenClosest.ClosestThing_Global_Reachable_Custom`.
  - HD overlap: the SAME method carries HD's two postfixes + the seam Finalizer. Different patch kinds (transpiler vs postfix/finalizer) compose by construction — Harmony applies the transpiler to the original body, then wraps it — and HD reads only `__result`/`pawn`, none of the locals SC rewrites.
  - Fix (general / no-op): none. Worth recording only because it means SC and HD are co-resident on this method: SC's transpiler is what makes the construction giver reach the prioritized branch, and HD's Finalizer is what observes the fault when the resulting tail call throws.
- `NONE` **Smarter Construction — priority nudge for construction targets**
  - Mechanism: `SmarterConstruction.Patches/Patch_WorkGiver_Scanner_GetPriority.cs:10-31` postfixes `WorkGiver_Scanner.GetPriority(Pawn, TargetInfo)`, adjusting `__result` only when `__instance is WorkGiver_ConstructFinishFrames` (cached per cell, 3000-tick TTL).
  - HD overlap: none — HD neither patches nor calls `GetPriority`, and owns no construction scanner.
  - Fix (general / no-op): no overlap.
- `NONE` **Smarter Construction — fail a build toil when the pawn is stuck / when completing would enclose**
  - Mechanism: `Patch_PawnsGettingStuck.cs:11-16` postfixes `JobDriver_ConstructFinishFrame.MakeNewToils` to add a stuck-detector fail condition; `Patch_JobDriver_MakeNewToils.cs:11-17` transpiles that driver's `<MakeNewToils>b__1` display-class lambda to bail out before `Frame.CompleteConstruction`.
  - HD overlap: none — HD does not patch `JobDriver_ConstructFinishFrame`, and its construction *delivery* jobs are separate drivers. HD's yield hook (`GenPlace.TryPlaceThing`) is downstream of whether the frame completes at all.
  - Fix (general / no-op): no overlap. A failed build toil is an ordinary job end HD's finish-action/queue handling already tolerates.

### Top risks

- The `HIGH` above is the only one, and its severity comes from WHERE the throw lands, not from what SC's feature does. Any mod postfixing any work giver's `JobOnThing`/`JobOnCell` and throwing gets the identical blast radius, because vanilla's unguarded tail call is the amplifier. Treat SC as the worked example, not as the scope.
- Residual after the fix: the affected work type stops happening for the session (wall frames, in SC's case). That is a deliberate trade — one work type off beats every work type off — and is surfaced by a high-priority alert precisely so it cannot degrade silently.
- The fallback attribution path is approximate, and it is the ROUTE MOST FAULTS TAKE (a Harmony-patched giver runs as a `DynamicMethod` with a null `DeclaringType`, so the frame walk cannot name it). Two guards keep it from becoming HD's own version of #235: the recorded giver lives for exactly one `TryIssueJobPackage` call (cleared in the Finalizer's `finally` on every path), so a throw from before the loop or from a postfix after it sees null rather than a stale name; and a giver — by EITHER route — is only quarantined when a mod is demonstrably implicated in it (a non-HD Harmony patch on its job entry points, or a mod-owned frame in the trace). Residual worst case: quarantining a giver that a mod really does patch while the true culprit is a different mod also on the path — never an untouched vanilla giver, and never on no evidence.
- A quarantine stops that work happening AUTOMATICALLY, and additionally stops the emergency/`priorityWork` SUSTAIN follow-up (`JobGiver_Work:308` via `:65`, which consults the same `PawnCanUseWorkGiver` and then `priorityWork.Clear()`s at `:76`). It does NOT block the player's right-click "Prioritize": that order is built and started by `FloatMenuOptionProvider_WorkGivers` (`:123`/`:178`) without ever consulting `PawnCanUseWorkGiver` — which is `private` at `JobGiver_Work:268`, so it could not. The manual escape hatch is therefore real and is advertised in the alert text. The trap for a future reader is that `PawnCanUseWorkGiver` DOES appear on a priority path, just not the one a right-click uses; both call sites are cited in `WorkGiverBlocklist` so the wrong conclusion is not re-derived.

### General-pattern recommendations

- **CONTAIN AT THE SEAM, THEN REMOVE THE SOURCE FROM THE ROTATION.** Containing a foreign throw at a work-selection seam is necessary but not sufficient: skip-the-node and return-no-job leave the pawn equally idle. A containment fix is only complete when the faulting unit stops being re-entered.
- **CLASSIFY BLAME FROM FRAME OBJECTS, NEVER FROM RENDERED TRACE TEXT.** The 0Harmony runtime the game loads deduplicates repeat renders of the same exception into a placeholder, so any "does the trace contain my namespace?" string probe is unsound for most players (issue #236). `new StackTrace(ex, false).GetFrames()` is not interceptable that way.
- **ONLY NAME A CULPRIT A RUNNING MOD ACTUALLY OWNS.** "The innermost frame that isn't vanilla or mine" will happily name `mscorlib` for a `KeyNotFoundException` thrown inside `Dictionary\`2.get_Item`, or a LINQ iterator for a fault in modded LINQ code — and then tell the player to report it there. Resolve the assembly through `LoadedModManager.RunningModsListForReading` and skip anything no mod owns; fall back to a bare assembly name only when nothing is mod-owned.
- **SCOPE "CURRENT SCAN" STATE TO THE CALL THAT RECORDED IT.** Ambient state used as fallback attribution must be cleared in a `finally` on the observing seam, on the success path too. A value that outlives its scan does not degrade to "no answer" — it degrades to a CONFIDENT WRONG answer, which is strictly worse than having no fallback at all.
- **PUT A DESTRUCTIVE, MULTI-FACT DECISION IN A TESTED POLICY.** Anything that can permanently remove a capability from a save (here: disabling a work giver) should be a pure function in `HaulersDream.Core` with a named test per refusal, so the refusals are pinned artefacts rather than early returns someone can reorder.
- **A FOREIGN-FAULT CONTAINMENT MUST NEVER COVER HD'S OWN FAULT.** Every containment path here tests HD's assembly first and rethrows, and the quarantine refuses HD's own work givers — the same rule `Patch_JobGiver_Work_WorkGiverResilient` (issue #7) already followed.
- **AN INVISIBLE DEGRADE IS A FUTURE FALSE BUG REPORT.** Any "HD switched something off to keep going" decision needs a player-visible alert naming what was switched off — and who caused it only when the evidence actually identifies a cause (saying "unknown, see the log" beats a confident guess; #235's retracted attribution is the cautionary case) — or the silence becomes HD's fault in the next report.

### Verdict

One `HIGH`, four `NONE`. A crash of this shape is *possible* from Smarter Construction (two unguarded `MapComponent` dereferences in `ClosedRegionDetector.WouldEncloseThings`), but it was never shown to be what threw in #235 and the evidence available could not have shown it — see the GOTCHA above. What IS established is the reason such a throw bricks a colony, and that is vanilla's: an unguarded tail call in `JobGiver_Work.TryIssueJobPackage`. HD patches neither and ships no SC-specific code; what it ships is a general containment + session quarantine + alert at the seam it already held, which covers every mod with this failure shape. Whichever work type is quarantined stops happening on its own — stated in `COMPATIBILITY.md` so the trade is not a surprise.

---

## Family: `drugs`

Investigated for HD issue #232: right-clicking a colonist onto a modded alcohol (`「中性私酿」`, from *Rimsenal Xenotype Pack – Harana*) throws `ArgumentException: Value does not fall within the expected range.` out of `FloatMenuOptionProvider_PickUpItem`, with the trace stopping at `JobGiver_DropUnusedInventory.ShouldKeepDrugInInventory` and naming HD's patches on it. There is no mod-vs-mod patch collision here to map: the throw is RimWorld's own, it happens before any HD code, and the mod that ships the def is not installed on this machine, so the def could not be read. What the family DOES have is a vanilla data invariant ("every drug def has a `DrugPolicyEntry`") that modded content can apparently violate, a lookup that reacts to the violation by throwing with no message, and two unguarded callers — one of which runs every think pass for every colonist in the Home area. HD's share is that it holds the only guard position on that predicate, plus one instance of the same unguarded lookup in code of its own.

### Feature risk map

- `MEDIUM` **RimWorld `DrugPolicy` — per-`ThingDef` entry lookup throws for a drug the policy has no entry for**
  - Mechanism: `DrugPolicy`'s `ThingDef` indexer scans `entriesInt` and, on no match, ends in a bare `throw new ArgumentException();` — no message, so .NET supplies "Value does not fall within the expected range." and the error names no drug, pawn or mod. `Pawn_DrugPolicyTracker.AllowedToTakeScheduledEver` does `CurrentPolicy[thingDef].allowScheduled` on it, and that is the SECOND clause of `JobGiver_DropUnusedInventory.ShouldKeepDrugInInventory`. Blast radius is two callers, neither guarded: `FloatMenuOptionProvider_PickUpItem.GetOptionsFor` (the visible half — the float-menu build aborts, so the "Pick up" options vanish) and `JobGiver_DropUnusedInventory.TryGiveJob` (the invisible half — the whole drop-unused routine fails, per drug, per think pass, for every undrafted colonist in the Home area). Root cause of the missing entry is UNCONFIRMED; the three surviving candidates are vanilla `DrugPolicy.CopyFrom` (`entriesInt.Clear()` then re-copy, no re-initialisation — session-scoped, would heal on reload), the def being absent from `DefDatabase<ThingDef>` when the policy was built or load-repaired, and a mod editing entries directly.
  - REFUTED mechanisms (each decompiled, each eliminated — recorded so they are not re-derived): (1) the admit/query predicate mismatch — `DrugPolicy.InitializeIfNeeded` admits `category == ThingCategory.Item && IsDrug` while the query sites test `IsDrug` alone — is a REAL latent vanilla defect but cannot be this report, because `FloatMenuOptionProvider_PickUpItem.GetOptionsFor` short-circuits on `category != ThingCategory.Item` FIRST, so reaching the throw proves the def satisfies the admit predicate; (2) a drug def with no `CompProperties_Drug` NREs `InitializeIfNeeded`'s sort comparer, but every missing entry is ADDED BEFORE the in-place sort and `PostLoadIniter` wraps each `ExposeData` in try/catch, so such a def loses only its ordering, never its entry; (3) *[KV] Save Storage Settings* (installed locally, decompiled) — its `LoadDrugPolicy` overwrites matching entries IN PLACE and creates new policies via `MakeNewDrugPolicy()`; it never clears the list and never removes an entry.
  - HD overlap: **NON-CAUSAL on the reported path.** HD's postfix on `ShouldKeepDrugInInventory` never ran (Harmony skips postfixes when the original throws) and could not have thrown if it had — on the float-menu path the stack is a GROUND stack, so HD's own "must be inside THIS pawn's inventory" guard returns first. HD appeared in the trace for two reasons that are not evidence: Harmony annotates a patched frame with every mod that hooked the method regardless of whether its code ran, and HD 1.21.0.0's finalizer returned the exception, flipping Harmony to `throw ex` and RESTAMPING the trace onto HD's hook (fixed in #236, same release — the finalizer is now `void`). HD's genuine share is one instance of the same unguarded lookup in its OWN code: the #229 withdrawal-access scan called `drugPolicy[drug.def]` on a def taken from an arbitrary colonist's inventory. It was contained by that patch's `SeamDegraded` boundary, so it never crashed — it emitted a red error and silently killed the #229 feature for that pawn on that scan. That file is untracked, so it never shipped; this is a defect fixed before release, not a regression.
  - Fix (general, three parts, no upstream repair): (1) CONTAINMENT — a `Finalizer` on the existing `Patch_JobGiver_DropUnusedInventory_ShouldKeepDrug` class. HD's own fault (`HDFault.InvolvesHaulersDream`, frame objects not rendered text) logs and RETHROWS; anything else sets `__result = true` and reports through `HDGuard.SeamContained`. The degrade VALUE is the crux: a thrown call leaves `__result` at `default(bool)` = `false`, which is the DESTRUCTIVE answer ("drop it") and would have the colonist dump the stack at their feet every think pass, whereas `true` is vanilla's own fall-through (the method returns `false` only when a six-clause conjunction holds), stops the drop, restores the menu, and is stable across BOTH callers so they cannot oscillate. It does not weaken #62/#81/#197 either — for a tagged or player-kept drug the postfix would have forced `true` anyway. Dedupe is per drug DEF (`defName`, never `Label` — a label lookup on the data that just faulted could throw again and escape the guard), which also lands the def name in HDGuard's format-free disk breadcrumb, the line #235 proved survives when the rich message build fails. Honest over-reach, stated in the code: a finalizer cannot distinguish "the original threw" from "a FOREIGN postfix threw after the original returned `false`", so in that sub-case a legitimate `false` becomes `true`; a thread-static "original completed" latch was considered and rejected as unreliable under patch ordering and as re-entrancy surface on a UI-thread-reachable path. (2) SHARED ACCESSOR — `DrugPolicyLookup` (game layer, integer indexer only) plus the Verse-free `Core.DrugAllowancePolicy`, which answers what a MISSING entry means: `MissingEntryAllowedForAddiction = true`, reconstructing the value `InitializeIfNeeded` would have created and the only value the player cannot have overridden (a def with no entry has no row in the drug-policy dialog; `false` would silently arm the #229 rehab lever for a drug nobody marked). PAIRED WITH A SECOND, OPPOSITE-DIRECTION RULE — `DrugAllowancePolicy.MayRouteToDrug`, which REFUSES a no-entry def outright, and the two must never be collapsed. "The policy permits it" is not "vanilla can finish the job": the #229 leg does not merely allow a drug, it MOVES a dose into the seeker's own inventory, and vanilla re-validates it there next think via `JobGiver_SatisfyChemicalNeed`'s private `DrugValidator` (`:120-121`, `drugPolicy != null && !drugPolicy[drug.def].allowedForAddiction && …`), reached from `FindDrugFor:75` over `pawn.inventory.innerContainer` — the identical throwing indexer. Answering only the policy question would therefore have converted "HD's enhancement quietly stands down" (the pre-fix behaviour, via that patch's own `SeamDegraded` boundary) into "HD walks the addict over, hands it a dose it can NEVER ingest (vanilla throws every think; `ThinkNode_Priority` skips the whole drug node) and can never shed (`ShouldKeepDrugInInventory`'s `!IsAddicted` clause is false), and the pawn loses drug satisfaction every scan" — with `drugsForWithdrawal` defaulting to true. Scope stated honestly in the code: the strictly-necessary case is a NON-null policy with no entry (vanilla's validator short-circuits on `drugPolicy != null &&`), and the gate is deliberately the broader "no entry, full stop" as one indivisible rule. The accessor deliberately has TWO members — `EntryFor` returns the FIRST match (vanilla's own indexer semantics, so the #229 clause stays byte-identical for every def that HAS an entry) and `TakeToInventoryTotal` SUMS every match, which is what `InventorySurplus.KeepCountOf` has always counted and must stay, since `CopyFrom` can propagate duplicate entries. (3) BUILD GUARD — `scripts/check-drug-policy-access.ts` bans `CurrentPolicy[…]`, `<drugPolicy>[…]`, all FOUR `Pawn_DrugPolicyTracker` sites that reach the `ThingDef` indexer (`AllowedToTakeScheduled{Ever,Now}(`, `AllowedToTakeToInventory(`, `ShouldTryToTakeScheduledNow(`), `DrugAIUtility.IngestAndTakeToInventoryJob(` (vanilla's own job builder, which does `drugPolicy[drug.def]` unguarded — and is the helper this patch's header cites as "vanilla's own construction", so a "just call vanilla's builder" simplification would reintroduce #232 through the front door) and calls to `ShouldKeepDrugInInventory(` in real code outside the accessor, and positively pins every piece of the fix including the no-entry routing gate. Explicit NON-GOALS, all rejected: adding the entry, calling `InitializeIfNeeded`, touching `entriesInt`, swapping or repairing a policy, patching `InitializeIfNeeded`/`CopyFrom`/the indexer/`AllowedToTakeScheduledEver`, prefixing the predicate to skip vanilla, or memoising "this def throws". A `DrugPolicy` is shared, SCRIBED state and one caller is the clicking client's UI thread, so mutating it there is a save mutation and an MP desync — the hazard `Patch_JobGiver_DropUnusedInventory.cs` already documents for the tag-set self-heal.

### Top risks

- The `MEDIUM` above is the only one, and it is rated by HD's CONTRIBUTION, not by its symptom. The symptom is severe (a whole per-think routine failing silently, plus a dead float menu), but HD neither causes it nor can fix it: the missing entry is in someone else's saved data, and the mechanism that produced it is unconfirmed. HD's rating is MEDIUM because it held the only guard position and shipped the same unguarded call once itself.
- Residual after the fix: the throw STILL happens on every call. HD contains it, it does not prevent it, and it never repairs the policy. What is bounded is the cost — no drop, no lost float menu, one log line per drug def per session.
- Residual sub-case (accepted, documented): a foreign postfix throwing after a legitimate `false` gets that `false` turned into `true`, so a drug the colonist should shed stays in the pack until whatever threw is fixed. Safe direction (keep beats dump-forever) and not distinguishable from inside a finalizer.
- Residual on the #229 leg (accepted, deliberate): the no-entry routing gate is broader than strictly necessary — it also refuses a seeker with NO drug policy at all, a case where vanilla's validator short-circuits and would not throw. The cost is that such a colonist loses only this optional leg (never vanilla's own spawned-drug search), and the benefit is one indivisible rule instead of a two-clause condition a later edit could get subtly wrong. The narrower condition is written down in the code comment so the choice is reversible on evidence.
- A separate latent VANILLA bug found in passing, not HD's and with no HD workaround: `AllowedToTakeScheduledEver` dereferences `CurrentPolicy` with no null check while `ShouldKeepDrugInInventory`, two clauses later, DOES guard it — so a Biotech mutant with `disablePolicies` NREs there. Recorded only so it is not re-derived as an HD fault.

### General-pattern recommendations

- **A VANILLA GETTER THAT THROWS IS A SEAM, NOT A CONTRACT.** Any vanilla lookup whose no-match path is a bare `throw` (no message, no context) is reachable the moment modded content violates the data invariant behind it. Route every such read through ONE audited accessor that answers instead of throwing, and pin the accessor with a build guard — the unsafe spelling is always shorter, compiles clean, passes every test and only fails on someone else's save.
- **CHOOSE THE DEGRADE VALUE FROM WHAT THE CALLER DOES WITH IT, NOT FROM WHAT LOOKS NEUTRAL.** `default(bool)` is what a thrown call already yields, so containing a throw is only worth doing if the substituted answer differs from it — and here `false` means "drop it, every think pass". The right value is the one vanilla itself falls through to, and it must be stable across every caller or two callers will fight.
- **CONTAIN, NEVER REPAIR, SCRIBED SHARED STATE.** Fixing the upstream data (adding the entry, re-initialising the list) is the tempting move and is wrong: it mutates the player's save from a path that can run on the UI thread, which is also an MP desync. Contain at the seam, name the drug in the log, and say plainly in the message that HD does not repair it.
- **MAKING A THROWING QUESTION SAFE IS NOT THE SAME AS MAKING THE RESULT USABLE — CHECK WHAT THE REST OF THE CHAIN STILL ASKS.** Answering an unsafe lookup safely lets HD's own code past the fault, but if HD then ACTS on that answer (moves an item, issues a job), whatever vanilla does next may ask the identical unsafe question with no guard of its own. The failure mode is worse than the original: instead of standing down, the enhancement commits the pawn to a state only the broken lookup can get it out of. Whenever a hardened predicate FEEDS AN ACTION rather than just a report, decompile the downstream re-validation and refuse anything it cannot evaluate.
- **A NON-CAUSAL MOD IN A STACK TRACE IS THE DEFAULT, NOT A CLUE.** Harmony lists every mod that hooked a method whether or not its code ran. Before accepting "HD is in the trace" as involvement, check whether HD's hook could even have executed (postfixes are skipped when the original throws) and whether its own guards would have returned first.
- **WHEN THE REPORTED MECHANISM CANNOT BE CONFIRMED, ENUMERATE AND REFUTE.** Record which candidates were decompiled and eliminated, and exactly which fact eliminated each. Three of the four candidates here look compelling and are wrong; without the refutations written down, the next investigation re-derives them.

### Verdict

One `MEDIUM`, and it is a vanilla-plus-modded-data problem rather than a mod-vs-mod one. The throw is RimWorld's, the missing entry is in the player's saved policy, the mechanism that produced it is unconfirmed, and the mod shipping the def is not installed here so it could not be inspected. HD ships no drug-mod-specific code and no repair of upstream data: a containment finalizer at the seam it already patched (answering RimWorld's own default, "keep it"), one shared read-only accessor that cannot reach the throwing indexer, a tested Core answer for what a missing entry means, and a build guard so the unsafe spelling cannot come back. HD's own instance of the bug — an unguarded `drugPolicy[drug.def]` in the #229 withdrawal scan — is fixed in the same change, before it ever shipped. What a future report needs in order to close the INFERRED half is listed in `COMPATIBILITY.md`.

---

## Synthesized compatibility plan

### General patterns

- **Keep-count provider registry (extend KeepCountOf with reflection-detected per-pawn 'wants N of def' providers)** — _covers: inventory-keepers: Item Policy (closes the ONE genuinely unprotected keeper — the high risk); inventory-keepers: any future loadout/stock mod with a per-pawn keep-N-of-def policy; general: makes HD's keep model count-aware (keep N, unload the surplus) which the HD #21 wrong-keep-count history proved is the correct behavior_
  - Change: In InventorySurplus.KeepCountOf (Source/HaulersDream/InventorySurplus.cs:289-305), after the existing drug-policy + inventoryStock + CECompat.LoadoutKeepCount aggregation, ADD a small list of reflection-detected keep-count providers. The first concrete provider is Item Policy: a new ItemPolicyCompat shim (mirroring the existing *Compat idiom) reflects _ItemPolicy.ItemPolicyUtility.GetItemPolicyEntry(Pawn,ThingDef)->int and adds its count. CRITICAL implementation guard (verifier MISSED item): GetItemPolicyEntry routes through GetPawnPolicy which auto-INSERTS an empty ItemPolicy into ItemPolicyUtility.policies for any unseen pawn (and that dict is scribed) — so the shim MUST first reflect the static `policies` Dictionary and check ContainsKey(pawn), returning 0 for pawns with no policy, never calling GetItemPolicyEntry on them. Provider list is extensible so future per-pawn keep-N mods plug in as one registration, not a new SurplusOf branch.
  - Why: Item Policy is the only keeper with no shim; with unloadAllSurplus=ON, HD adopts and ships its stocked items (drug AND non-drug) and Item Policy re-fetches via JobGiver_TakeItemForInventoryStock -> classic unload<->refetch loop. Item Policy stores its policy in its OWN static Dictionary (not vanilla inventoryStock), so no general vanilla signal exposes a non-drug per-pawn keep count — a reflection provider feeding the GENERAL KeepCountOf path covers drug + non-drug in one place and generalizes to any similar mod. Risk is gated behind default-OFF unloadAllSurplus, so it is a real-but-opt-in gap; the provider closes it cleanly. The dict-mutation side-effect guard is mandatory or HD pollutes Item Policy's saved data for every pawn it scans.
- **Honor the vanilla coordination hook: call JobGiver_DropUnusedInventory.ShouldKeepDrugInInventory for drug items** — _covers: inventory-keepers: Item Policy stocked DRUGS (its ShouldKeepDrugInInventory postfix is then honored); inventory-keepers: any future drug-keeper mod that postfixes ShouldKeepDrugInInventory; crafting-bills / haul-redirectors: vanilla addiction parity preserved_
  - Change: In InventorySurplus.IsManagedKeepItem (Source/HaulersDream/InventorySurplus.cs:259-261), for a non-HD-swept (!hdSwept) DRUG item, replace/backstop the inline AddictionUtility.IsAddicted/HasChemicalDependency mirror with an actual call to the vanilla public static JobGiver_DropUnusedInventory.ShouldKeepDrugInInventory(pawn, thing). Keep the inline addiction/chem-dep check as a same-result backstop so behavior is unchanged when no mod patches the vanilla method.
  - Why: Drug-keeper mods coordinate through the shared vanilla JobGiver_DropUnusedInventory.ShouldKeepDrugInInventory(pawn,drug) (decompile-verified public static). HD currently inline-mirrors that method's body instead of calling it, so it silently misses every keeper mod's postfix on it. NOTE the verifier REFUTED the overstated framing that this misses Smart Medicine — SM/CE/SS/DBH/Yayo drugs are already kept via their own DIRECT shim calls, so the ONLY drug-keeper actually missed today is Item Policy (same gap as provider #1). This is therefore low-urgency polish/backstop, NOT a live SM hole; valuable as a durable general rule (where vanilla exposes the exact decision method other mods patch, call it, don't duplicate it).
- **Never override a FOREIGN custom unload job (def-guard HD's unload substitution)** — _covers: haul-redirectors: Common Sense CompUnloadChecker + UnloadMarkedItems (don't clobber a CS-marked equipment/apparel unload HD's driver can't service); haul-redirectors: PUAH-style custom unload jobs (unsupported co-install, still fail-safe); general: the documented unload-cycle / '10 jobs/tick' bug class for any mod owning the unload seam_
  - Change: In Patch_JobGiver_UnloadYourInventory.Postfix (Source/HaulersDream/HarmonyPatches.cs:90-125), add a guard right after the __result null-check: substitute HD's HaulersDream_UnloadInventory ONLY when __result.def == JobDefOf.UnloadInventory (the vanilla job). When __result is a foreign custom unload job (Common Sense's UnloadMarkedItems, PUAH's UnloadYourHauledInventory, etc.) HD must leave it untouched. The same def-guard belongs on the en-route/opportunistic unload substitution in Patch_JobGiver_Work_OpportunisticUnload (HarmonyPatches.cs:150-191) where it replaces __result with an unload job.
  - Why: Verified gap: HD's postfix only returns on __result==null; it has no __result.def check. CS's JobGiver_UnloadYourInventory PREFIX produces an UnloadMarkedItems job, and if the pawn also carries an HD-tagged surplus stack HD substitutes its OWN inventory-only unload job OVER CS's — so a CS-marked equipment/apparel item (HD's driver only services inventory innerContainer items) is left unserviced that scan. Low severity (a delayed/skipped unload, not a cycle — HD's anyUnloadable gate prevents the no-progress re-issue loop) but a clean, mod-agnostic guard that protects the whole family's documented unload-cycle class.
- **Run-LAST on the shared work-selection seam (pin HD's two JobGiver_Work.TryIssueJobPackage postfixes to Priority.Last)** — _covers: haul-redirectors: While You Are Nearby (its equal-priority postfix REPLACES __result with a nearby job); haul-redirectors: any 'prefer nearby work' / job-substituting mod that postfixes the same method; general: react-after-replace ordering on the universal work-selection seam_
  - Change: Add [HarmonyPriority(Priority.Last)] to the two HD postfixes on JobGiver_Work.TryIssueJobPackage: Patch_JobGiver_Work_OpportunisticUnload (Source/HaulersDream/HarmonyPatches.cs:147-150) and Patch_OpportunisticLoadDeposit (Source/HaulersDream/Patch_OpportunisticLoadDeposit.cs:45). Neither currently sets a priority.
  - Why: Verified: WYAN postfixes JobGiver_Work.TryIssueJobPackage at Priority.Normal(400) with no explicit priority and OVERWRITES __result; HD's two postfixes on the same method also carry NO HarmonyPriority. At equal priority cross-mod order is unspecified, so HD's REACTIVE postfixes (decide run-over/divert against the chosen job) may observe the pre-substitution far job instead of WYAN's substituted near job. Low harm (a suboptimal divert decision, never a loop/crash — verified) but the pin is cheap and makes the ordering deterministic. General rule: a postfix that REACTS to the chosen job runs after postfixes that REPLACE it. Scope note: verified none of the four haul-redirector mods patch JobGiver_Haul, so the animal-bulk postfix (Patch_JobGiver_Haul_AnimalBulk) is out of scope for this pin.
- **Compose-don't-replace on additive UI list-builders (BillRepeatModeUtility.MakeConfigFloatMenu)** — _covers: crafting-bills: Everybody Gets One (modes appear via EGO's own transpiler instead of HD's EGO-specific bridge); crafting-bills: any future mod that adds repeat-mode dropdown entries by transpiling MakeConfigFloatMenu; general: never `return false` from a prefix on a method other mods additively transpile_
  - Change: Replace HD's full-replace Prefix (return false) on BillRepeatModeUtility.MakeConfigFloatMenu (Source/HaulersDream/Patch_BillRepeatMode_Batch.cs:90) with a composing approach: let vanilla (and thus every foreign transpiler on that method, e.g. Everybody Gets One's option-inserter) run to build the full base option list, then APPEND HD's three batch entries + the size option via a Postfix that augments the returned/opened menu. This lets EverybodyGetsOneCompat.TryInsertModes be retired (EGO's modes appear via EGO's own transpiler) and makes HD a peer rather than a menu monopolist.
  - Why: Verified mechanism: a Harmony Prefix returning false skips the original body AND every transpiler attached to it, so HD's full-replace hides the repeat-mode entries of ANY mod that transpiles MakeConfigFloatMenu. Today EGO is the only such mod and it IS re-surfaced via the EGO-specific TryInsertModes bridge, so the realized impact is ZERO — this is a latent forward-looking pattern fix, not an active bug. Lower priority than the keeper/unload/priority fixes; do it to remove the per-mod bridge debt and be robust to a second such mod. (If the postfix-augment proves fiddly with how the FloatMenu is consumed, an acceptable interim is to keep the bridge; the principle stands.)
- **Skip a foreign carrier type HD must not manage, gated on the type-check ONLY (not a feature toggle) — and extend it to the cargo-RAID paths** — _covers: vehicles: build-from-inventory + meals-on-wheels raiding a parked vehicle's player-loaded cargo (verifier MISSED, default-ON, medium); general: never have HD's generic carrier-handling claim/consume a foreign carrier type the owning mod manages_
  - Change: The bulk-UNLOAD guards [UC1] (Patch_WorkGiver_UnloadCarriers) and [UC2] (FloatMenuOptionProvider_BulkUnloadCarrier) already skip a VehiclePawn gated on VehicleFrameworkCompat.IsVehicle ONLY — keep them. EXTEND the same IsVehicle-only skip to OrganicInventoryShare.IsEligibleOrganicHolder (Source/HaulersDream/OrganicInventoryShare.cs:54) and EatFromInventory's holder loop (EatFromInventory.cs:101): currently they gate on InVehicle(carrier)+IsFormingCaravan, which does NOT exclude a PARKED, player-loaded vehicle (the holder IS the vehicle, so InVehicle is false; no forming-caravan lord). Add `if (VehicleFrameworkCompat.IsVehicle(carrier)) return false;` so HD does not raid a vehicle's player-loaded cargo for builds/meals.
  - Why: Verifier-confirmed gap: OrganicInventoryShare reads the WHOLE innerContainer of any eligible holder; a parked VehiclePawn passes IsLiveShareCarrier + IsEligibleOrganicHolder (InVehicle false for the vehicle itself, no forming-caravan lord), so a colonist pulls steel/components/food out of a vehicle the player deliberately loaded for a trip — silently undoing the loadout, and tensioning with VF's deliberate no-auto-dump design (UnloadEverything forced false). This is a behavior-JUDGMENT call (see open question), but the safe default mirrors the [UC1]/[UC2] precedent: treat a vehicle as VF-owned storage HD does not source from. Gating on IsVehicle ONLY (returns false when VF absent) makes it a pure safety skip that no feature flag can switch back off.

### Targeted patches

- **[Item Policy]** Create ItemPolicyCompat.cs mirroring the existing reflection-only *Compat idiom (no hard ref, AccessTools.TypeByName precondition, lazy Init, IsActive cached, fail-open, warn-once on drift). Reflect _ItemPolicy.ItemPolicyUtility: the static `policies` Dictionary<Pawn,ItemPolicy> AND GetItemPolicyEntry(Pawn,ThingDef)->int. Expose KeepCount(pawn,def) that returns 0 unless policies.ContainsKey(pawn), else GetItemPolicyEntry(pawn,def). Wire its result into the general KeepCountOf provider list (general pattern #1).
  - Why: Genuinely mod-specific because Item Policy stores its per-pawn keep counts in its OWN static Dictionary, not vanilla inventoryStock, and no general vanilla signal exposes a non-drug per-pawn keep count. The shim is the data-source; it FEEDS the general KeepCountOf path rather than adding a new SurplusOf branch, so the durable logic stays general. The ContainsKey pre-check is the mandatory guard against GetPawnPolicy's auto-insert side effect bloating Item Policy's scribed save data.
- **[Keyz' Allow Utilities / Allow Tool (Haul Urgently)]** KEEP the existing Patch_HaulUrgently_BulkHaul (postfix on KeyzAllowUtilities.WorkGiver_HaulUrgently.JobOnThing AND AllowTool.WorkGiver_HaulUrgently.JobOnThing, resolved by string, Prepare()-gated, running BulkHaul.TryBuildBulkJob). No change.
  - Why: The urgent-haul WorkGiver is a distinct type OFF HD's WorkGiver_HaulGeneral.JobOnThing funnel, so the general bulk-sweep postfix never sees it — a targeted hook on the urgent giver is required to bulk-upgrade urgent hauls. Verified targets resolve and the upgrade declines non-HaulToCell jobs (e.g. genebank CarryGenepackToContainer) automatically. Already-handled / no-op.
- **[Common Sense]** KEEP the cede — RE-KEYED 2026-08-09 (issue #243): CommonSenseCedePolicy.CommonSenseGathersIngredients (reflection-reads CommonSense.Settings.adv_haul_all_ings; HD cedes its DoBill ingredient-gather conversions exactly when CS will gather; fail-closed). adv_cleaning is still read, but only to feed CommonSenseOwnsDoBillDriver, which the "Batch even with Common Sense active" opt-in keys on. (The generalization once suggested here — detect 'a non-HD prefix returns false on JobDriver_DoBill.MakeNewToils' — is retracted: a foreign prefix proves ownership, and #243 proved ownership is the wrong cede key.)
  - Why: CS's haul-all option re-deposits HD's in-inventory ingredients; without the cede that is an infinite loop. As synthesized, this entry claimed the cede "byte-matches CS's own prefix gate" and that was the defect, not the virtue: CS's prefix gate answers "do I own the driver" (adv_cleaning||adv_haul_all_ings) while the cede needed "will anyone gather" (adv_haul_all_ings) — with cleaning alone CS hands collecting back to vanilla's CollectIngredientsToils, and HD's stand-down left nobody gathering (issue #243, field-reported). Detection is intrinsically CS-field-based, so it is appropriately targeted.
- **[Vehicle Framework]** KEEP the IsInstanceOfType(__instance) generic-base guard in Patch_WorkGiver_PackVehicle_Redirect.cs:73 and the reflection-only VehicleFrameworkCompat (8 TypeByName sites, RemainingDemandForThing single-transferable clamp, [SF4] AddOrTransfer deposit, VRM reserve). No change to these. The only VF code edits are the GENERAL cargo-raid skip (general pattern #6) and the optional degrade fixes in targetedPatches/open-questions.
  - Why: The generic-base JIT code-sharing trap is REAL and hard-verified (TransferableOneWay and ThingDefCountClass are both reference types, so WorkGiver_PackVehicle shares JobOnThing's native code with WorkGiver_BringUpgradeMaterial and WorkGiver_RefuelVehicleTurret, both targeting vehicles). The IsInstanceOfType(__instance) guard is load-bearing (without it a refuel/upgrade job is hijacked into a cargo bulk-load) and is the reusable pattern, but it is technically scoped to VF's WorkGiver hierarchy. Already-handled / no-op for the core; the loader has no general substitute because it must hook VF's exact pack-vehicle workgiver.

### Priority order

1. [GENERAL #1 + TARGETED Item Policy shim] Add ItemPolicyCompat + wire it into KeepCountOf as a keep-count provider, WITH the policies.ContainsKey(pawn) pre-check guarding GetPawnPolicy's auto-insert side effect. HIGHEST confidence + highest impact: closes the ONE genuinely unprotected inventory-keeper (the only confirmed high risk across all four families). Covers drug + non-drug Item Policy stock in one place.
2. [GENERAL #3] Def-guard HD's unload substitution: substitute only when __result.def == JobDefOf.UnloadInventory in Patch_JobGiver_UnloadYourInventory.Postfix (and the en-route unload substitution). VERIFIED gap (HD only null-checks __result today). Low severity but cheap, mod-agnostic, protects the documented unload-cycle class against Common Sense / PUAH.
3. [GENERAL #4] Pin HD's two JobGiver_Work.TryIssueJobPackage postfixes to [HarmonyPriority(Priority.Last)]. VERIFIED both lack a priority; makes ordering vs While You Are Nearby deterministic. Cheap, low-harm-if-skipped, removes a real nondeterminism.
4. [GENERAL #6] Extend the IsVehicle-only carrier skip to OrganicInventoryShare.IsEligibleOrganicHolder + EatFromInventory holder loop so HD stops raiding a parked vehicle's player-loaded cargo for builds/meals. VERIFIED behavior gap (default-ON). DECISION-GATED — confirm the maintainer wants vehicle cargo treated as off-limits (see open questions) before shipping; it is a behavior change, not a crash fix.
5. [GENERAL #2] Route HD's drug-keep through vanilla JobGiver_DropUnusedInventory.ShouldKeepDrugInInventory (keep the inline addiction check as backstop). Low urgency: verifier REFUTED that this fixes a live Smart Medicine hole; today it only backstops Item Policy drugs (already covered by #1). Durable correctness polish.
6. [GENERAL #5] Replace the full-replace MakeConfigFloatMenu prefix with a compose/append approach; retire EverybodyGetsOneCompat.TryInsertModes. LATENT/forward-looking only — EGO (the sole current instance) is already re-surfaced, so realized impact is ZERO today. Lowest priority; do it to remove per-mod bridge debt.
7. [ALREADY-HANDLED / NO-OP — keep, do not touch] Patch_HaulUrgently_BulkHaul (Keyz/Allow Tool), CommonSenseCedePolicy.CommonSenseGathersIngredients, Patch_WorkGiver_PackVehicle_Redirect IsInstanceOfType guard + VehicleFrameworkCompat clamp/[SF4]/VRM, all storage-family validate-through-vanilla + deposit-re-gate behavior, Simple Sidearms / Combat Extended / Smart Medicine / DBH / Yayo keeper shims, and Perfect Pathfinding (inert on 1.6 — read-only FindPathNow consumption, HD == vanilla). All verified correct against live mod sources. (One of these "verified correct" verdicts aged badly: the Common Sense cede was re-keyed 2026-08-09 after issue #243 — see the [Common Sense] targeted-patch entry above.)
8. [DOC-ONLY] Add Perfect Pathfinding (no 1.6 build, inert), While You're Up / Jobs of Opportunity, and While You Are Nearby to COMPATIBILITY.md (none are currently documented). Optionally add <loadAfter>SmashPhil.VehicleFramework</loadAfter> to About.xml (belt-and-suspenders; reflection resolves regardless and the shim degrades safely).

### Open questions (at synthesis time)

- VEHICLE CARGO RAID (general pattern #6 / priority #4) is a behavior-JUDGMENT call, not a crash fix. Today HD's build-from-inventory (default ON) and meals-on-wheels can source materials/food out of a PARKED, player-loaded VehiclePawn's cargo hold — which silently undoes a player's trip loadout and tensions with VF's deliberate no-auto-dump design. Do you want HD to treat a vehicle's cargo as off-limits (the proposed IsVehicle skip, matching the bulk-unload [UC1]/[UC2] precedent), or only skip vehicles that have a non-empty cargoToLoad manifest (a narrower 'don't touch what the player queued for loading, but a fully-loaded idle vehicle is fair game' rule), or keep the current raid-it behavior as intended?
- Item Policy re-fetch wiring was inferred from the assembly (the workspace copy is assembly-only, no XML/ThinkTree patch present), not read from a def. The unload<->refetch loop is highly likely real (JobGiver_TakeItemForInventoryStock would be dead code otherwise), but should the ItemPolicyCompat shim be considered final, or do you want to confirm JobGiver_TakeItemForInventoryStock is actually inserted into the colonist think tree in the installed mod's Defs/ before treating the loop as certain? (The keep-count fix is harmless either way.)
- [SF4] master-OFF physical-deposit nuance (verifier MISSED): when VF is PRESENT but enableVehicleFramework is OFF, VF's UNCONDITIONAL UsablePackAnimalWithTheMostFreeSpace patch still hands HD's FindCarrier a VehiclePawn, and JobDriver_LoadPackAnimal then takes the raw TryTransferToContainer branch (HarmonyPatches/JobDriver_LoadPackAnimal.cs:239) into the vehicle's UNCAPPED innerContainer — no CargoAdded event, no cargoToLoad decrement, and beyond CargoCapacity (VF then flags over-encumbered). Not data loss, but an unintended physical deposit on the master-OFF path. Do you want FindCarrier (PackAnimalLoad.FindCarrier) to skip a VehiclePawn when enableVehicleFramework is OFF, or is the documented degrade acceptable?
- MakeConfigFloatMenu compose-vs-replace (general pattern #5): the postfix-augment approach depends on how the opened FloatMenu is consumed after MakeConfigFloatMenu returns. If augmenting the returned list cleanly isn't feasible (e.g. the menu is opened inside the method), is keeping the EGO-specific TryInsertModes bridge acceptable as the interim, given EGO is the only current instance and impact is zero today?
- Confirm scope: the four investigations cover storage (ASF/LWM/Storage Network), inventory-keepers (SS/CE/Smart Medicine/Item Policy/DBH/Yayo), haul-redirectors (Common Sense/Keyz Allow Utilities+Allow Tool/While You Are Nearby/While You're Up), crafting-bills (EGO/Cook Carefully/Common Sense), pathfinding (Perfect Pathfinding), and vehicles (Vehicle Framework). The LWM mass-limited-DSU transient over-mass and Cook Carefully poison-discard were REFUTED as live 1.6 risks (LWM self-corrects via re-haul + never loss; Cook Carefully ships no 1.6 assembly so its transpiler never loads) and are intentionally DROPPED from the action list — confirm you agree they need no code change (optional: correct COMPATIBILITY.md:107's absolute 'HD never over-fills a deep-storage cell' to 'never count-overfills; may transiently mass-overfill an LWM mass-limited DSU by up to one carried stack, self-correcting via re-haul').

---

## Implementation status (this `feat/compat` branch)

**Landed (general patterns first):**
- Item Policy keep-count provider (`ItemPolicyCompat` → count-aware `KeepCountOf`) — closes the one confirmed HIGH risk; generalizes to any per-pawn keep-N-of-def mod.
- Foreign-unload def-guard — HD only replaces vanilla `UnloadInventory`, never another mod's custom unload (Common Sense, Bulk Load For Transporters).
- `HarmonyPriority(Priority.Last)` on HD's two `JobGiver_Work.TryIssueJobPackage` postfixes — deterministic vs job-substituting mods (While You Are Nearby).
- Vehicle cargo off-limits — HD won't source builds/meals from a parked vehicle's cargo; with VF support off, HD ignores vehicles entirely (selection + in-flight re-resolution).
- Bulk-refuel passable search anchor — `BulkRefuel` now anchors its fuel sweep at `pawn.Position` (not the refuelable's impassable cell), fixing the NRE on impassable refuelables (issue #34, Advanced Power Plus's generators) and reviving bulk-refuel for every impassable refuelable; generalizes to any mod's deep drill / generator / reactor.
- `[StaticConstructorOnStartup]` on the three HD types holding static `Texture2D`/`Material` fields (`Patch_Pawn_GetGizmos`, `HaulersDreamSettings`, `MapComponent_RoutePreview`) — clears the startup debug-log warnings; textures still load lazily on the main thread (no behavior change).
- (Earlier on this branch: Everybody Gets One repeat-mode bridge; Storage Network opt-in bulk-load.)

**Deferred (with rationale):**
- Route drug-keep through `JobGiver_DropUnusedInventory.ShouldKeepDrugInInventory` — verifier refuted necessity; redundant with the Item Policy fix; would over-keep count-scheduled drugs (regression).
- Compose-instead-of-replace the bill repeat-mode menu — zero functional impact today (EGO already works via its bridge) + a feasibility risk; pure tech-debt cleanup.

**Verified no action needed:** storage frameworks (ASF / LWM / Storage Network — validate-through-vanilla + deposit re-gate); Vehicle Framework core guards (the generic-base `IsInstanceOfType` patch guard, reflection clamps); Haul Urgently (Allow Tool / Keyz); Common Sense do-bill cede (later re-keyed for issue #243 — cede on the gather fact, not driver ownership); the Simple Sidearms / Combat Extended / Smart Medicine / DBH / Yayo keeper shims; Perfect Pathfinding (inert on 1.6 — HD only consumes `FindPathNow`); Better Autocasting (disjoint surface + interruption-safe); Advanced Power Plus (pure content mod — its solar comp + watermill cache postfixes have zero HD overlap; its only contact point, refueling its impassable generators, is closed by the bulk-refuel passable-anchor fix above).

